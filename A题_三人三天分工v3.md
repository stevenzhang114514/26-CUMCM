# A 题「药材的烘干问题」三人三天分工（v2）

> **v2 修订依据**：《A题_求解框架_修订版.md》与《A题_代码与插图清单_v2.md》（v3 清单）。
>
> **配套文件**：《A题_代码与插图清单_v2.md》（编号、验收标准、模块所有权）
>
> **成员代称**：甲 · 乙 · 丙。Day 1—Day 3 为**相对日期**，不代表官方比赛起止时间。
> **本文只安排工作，不含实现代码。**

---

## 一、角色与职责边界

| 成员 | 主责 | 独立交付 | 交叉复核 |
|---|---|---|---|
| **甲** | 数值内核、问题1—3、事件定位 | 固定域求解结果、误差与迭代记录 | 复核乙的材料坐标算子与固定半径对照 |
| **乙** | **入口检查**、方程与单位审核、半径数据、问题4、验证 | 入口检查报告、收缩模型、result4 源数据、基准与对照、**几何—密度诊断** | 复核甲的边界、公式与阈值条件 |
| **丙** | 附件/模板、导出、图表、论文整合 | 四个 Excel、题设六表、图集、论文 | 核对源结果、图表、论文与文件一致性 |

**文件所有权**

- 甲维护 `numerics/` 与 `models/p1~p3`
- 乙维护 `entry/`、`models/p4`、`validation/`
- 丙维护 `data_prep/`、`io/`、`figs/`、`report/`

> 乙需要修改公共接口时，**先与甲确定再实施**。

---

## 二、Day 1 上午：入口检查 → 接口冻结

> ⚠️ **v2 的最大流程变更**：**入口检查（`CODE-34`）升为第一优先，
> 必须在冻结接口之前完成。** 上一版把它当成开工后的杂项，实际它是所有下游判断的前提。

### 2.1 入口检查 6 项（乙主 / 丙佐 / 甲佐）

| # | 核实项 | 主责 | 产出 |
|---|---|---|---|
| 1 | 附件1 的"水分浓度"是**药材等效平衡含水率**还是**空气含湿量 Y**？ | 乙 | 结论 + 证据 |
| 2 | 附录2/3/4 中 **ρ 的物理含义**（湿物料体积密度？干基？热学等效参数？） | 乙 | 结论 + 证据 |
| 3 | 附录3/4 的 D 公式中 **T 的单位** | 甲 | 单位约定 |
| 4 | **长时环境外推**的合理方式（附件1 只到 4 h） | 乙 | 外推方案 |
| 5 | **半径数据覆盖范围**与问题4 达标时刻的关系 | 乙 | 是否启用 `CODE-05` |
| 6 | **模板规则**（已实测确认，需定 21 列展开方式） | 丙 | `output_spec.json` |

> **产出**：`TAB-05` 入口检查结论表。**未完成不得进入建模。**

### 2.2 接口冻结（三人共同确认）

| 类别 | 冻结项 |
|---|---|
| **变量** | $C=m_w/m_d$（干基含水率）与 $Y=m_v/m_{da}$（空气含湿量）**严格区分**；引入 $C_\infty^{eq}$ 并声明基准 |
| 单位 | 时间 **s**；距离 **m**；温度接口单位（**°C 还是 K，写死**） |
| 数组 | 场数组轴顺序；**节点型 / 单元中心型**约定 |
| 函数 | 半径函数 $R(t)$；边界函数 $T_\infty(t)$、$C_\infty^{eq}(t)$ |
| 输出 | 采样规则；result 文件 sheet / 列 / 格式 |
| **层次** | **M0 / M1 / M2 标签**；扩展结果与题设基线**分开存放** |
| 运行 | **配置标识**（每次运行唯一，写入所有结果文件） |

**对外结果的最小字段**：时间轴 · 空间轴 · 温度 · 含水率 · 半径 · 收敛状态 · 停止原因 · **层次标签**

**每次结果交接必须附带**：配置与版本标识 · 计算网格 · 时间精度设置 · 覆盖时间 · 最大残差 · 验证状态

> ⚠️ 丙**只用已标记的结果**生成正式图表。
> **模型修改后，受影响结果全部作废重跑。**

---

## 三、甲 —— 数值计算负责人

| 时段 | 代码 | 图 | 交付物 |
|---|---|---|---|
| **D1 上午** | `CODE-01` 配置中心与单位约定（含**四档 D 关键值**核对）<br>`CODE-06` FVM 内核（**单一可靠实现，中心半控制体**）<br>`CODE-07` 物性求值 | — | 可被乙调用的公共内核；接口规格 |
| **D1 下午** | `CODE-08` 隐式时间推进<br>`CODE-09` 非线性迭代<br>跑常系数基准 | `FIG-08` 离散示意图 | 内核基准运行记录 |
| **D1 晚间** | `CODE-10` 问题1 温度场<br>`CODE-11` 问题1 水分场（**先水后温**） | `FIG-41` 迭代/步长历史 | **result1 源数据** |
| **D2 上午** | `CODE-12` 问题2 双向耦合<br>`CODE-13` 边界模块（**M0 形式，潜热默认关闭**）<br>`CODE-15` 阈值通过时刻求解器<br>启动长时积分 | `FIG-16` 无量纲数图 | 问题2 稳定运行记录 |
| **D2 下午** | 获取问题3 首版 $t_*$<br>检查**最大值位置**与末时刻场<br>启动细网格验证 | `FIG-18` $D(C,T)$ 等值线 | **result3 源数据**；问题3 首版 $t_*$ |
| **D2 晚间** | `CODE-19` 空间/时间误差**分开细化**<br>`CODE-25` D 倍率、$h_m$ 情景扰动 | `FIG-15/40` 收敛图（**空间、时间分成两幅，斜率 +2**） | 误差检查记录；情景初版 |
| **D3 上午** | 最终配置补跑<br>`CODE-19` 误差表（**空间/时间分列**）<br>确认 $t_*$ | `TAB-04` 收敛表 | 冻结的数值结果；版本标识 |
| **D3 下午** | 核对论文数值方法、参数、最终数值 | — | 方法章节定稿意见 |
| **D3 晚间** | 配合 `CODE-30` 复核复现入口 | — | 复现验证记录 |

### 甲的关键提醒

- **D1 上午**：**四档 D 关键值必须逐点打印核对**
  （附录2 @C=0.05 → 1.3021×10⁻¹⁶；附录2 @C=2.55 → 4.94×10⁻⁹；
  附录3 @C=2.55,T=300K → 5.3718662×10⁻⁹；附录3 @C=0.15,T=323K → 7.9570613×10⁻¹⁰）
- **D1 下午**：稳定性上限**按选定算子确定**——半控制体 FVM 中心处约 $\Delta r^2/(4\alpha)$，
  Δr=0.5 mm 时约 **0.37 s**。
  ✅ 验收是**检查稳定性条件并拒绝不满足的设置**，**不是**"必须观察到发散"。
- **D1 下午**：**输出间隔 ≠ 计算步长**。**不设"预热段强制 dt ≤ 1 s"**。
- **D2 晚间**：**空间、时间分开细化**。Backward Euler **时间一阶**，CN 在光滑条件下**二阶**，
  **不要求所有误差都二阶**。双对数图横轴为**步长 h** 时斜率应为 **+2**。
- **D2 晚间**：**不预设"D 最敏感"**，作为待检验猜想。
  三种区间**分开命名**（情景 / 参数扰动分位 / 统计置信）。

---

## 四、乙 —— 物理模型与验证负责人

| 时段 | 代码 | 图 | 交付物 |
|---|---|---|---|
| **D1 上午** | **`CODE-34` 入口检查 6 项**（**第一优先**）<br>核对原始方程、边界、浓度与密度基准<br>推导材料坐标关系 | — | **入口检查报告 `TAB-05`**；坐标变换推导 |
| **D1 下午** | `CODE-04` 半径拟合（**单指数趋近模型**/PCHIP/样条）<br>搭建 `CODE-14` 材料坐标模块（**材料导数形式**） | `FIG-31` 材料坐标变换示意<br>`FIG-33` $R(t)$ 曲线 | 半径拟合函数；问题4 骨架 |
| **D1 晚间** | `CODE-20` 通量收支（**面积口径一致**）<br>复核 `CODE-06` 中心/边界离散<br>`CODE-14` 初步运行 | — | 离散与通量检查报告 |
| **D2 上午** | `CODE-14` 问题4 联调<br>**先用相同物性做固定半径一致性检验**<br>再运行收缩模型 | — | 材料坐标模型稳定运行记录 |
| **D2 下午** | 获取问题4 首版 $t_*$<br>检查**域外位置**与**表面重构**<br>`CODE-24` 运行 B 方案 | — | **result4 源数据**；问题4 首版 $t_*$ |
| **D2 晚间** | `CODE-18` 解析基准（**三条件齐备**）<br>`CODE-21` 物理合理性<br>**`CODE-23` 五项退化与一致性检验**<br>**`CODE-35` 几何—密度诊断** | `FIG-14` 数值 vs Bessel<br>`FIG-44/45` 一致性/退化汇总<br>**`G11` 诊断曲线** | 基准与对照报告；**诊断结果 `TAB-07`** |
| **D3 上午** | `CODE-20` 守恒口径复核<br>`CODE-05` 半径外推敏感性（**仅 $t_*$ 超范围时**）<br>`CODE-24` A/B/C 完成 | `FIG-39` 能量守恒残差 | 最终验证记录 |
| **D3 下午** | 核对物理假设、结论边界、A/B/C 解释 | — | 物理章节定稿意见 |
| **D3 晚间** | 抽查四问关键点及终止判据 | — | 抽查记录 |

### 乙的关键提醒

- **D1 上午**：**入口检查是所有下游判断的前提**。
  特别是第 1 项——附件1 的"水分浓度"若确认是**空气含湿量 Y**，
  则把它当药材表面平衡 $C$ 需要**吸附等温关系**，**不能直接相减**。
- **D1 下午**：$R_\infty+(R_0-R_\infty)e^{-kt}$ 是**单指数趋近模型，不是双指数**。
  求导方式不限，**唯一禁止**对原始点直接差分；
  **删除"导数相对差 < 10%"这类不稳健断言**，改用**达标时间对拟合选择的敏感性**。
- **D1 晚间**：**面积口径必须一致**——整根圆柱侧面积是 $2\pi R L$；
  用 $2\pi R$ 时**必须明确是"单位长度总量"**。
  **变物性模型不宜直接把 $\rho c_pT$ 的差当作完整内能变化。**
- **D2 上午**：`CODE-14` 主模型用**材料导数形式**（无 Ṙ 对流项），
  这与均匀仿射收缩假设自洽。**两种写法不能在未交代材料运动的情况下互相替换。**
- **D2 晚间**：**五项退化检验各自在正确的对照目标上通过**：
  ①问题2→问题1（**还要把 D 换成附录2 的 $D(C)$**，附录2 的 D 也不是常数）
  ②问题4 固定半径 + **保留附录4 物性 → 退化为 B 方案，不等于问题3**
  ③纯导热基准（固定物性 + 关闭蒸发 + 满足解析解边界）
  ④**坐标变换一致性**（两种形式应给出相同结果；不等价说明推导或离散有误，**不是"模型不同"**）
  ⑤物理合理性
- **D2 晚间**：**`CODE-35` 必须命名为「几何—密度一致性诊断」，不叫「质量守恒检验」。**
  它是**条件性诊断**。报告时：
  ✅ 说"提示密度定义、数据误差、几何假设或干物质守恒的**联合使用需要核查**"
  ✅ 说"若 ρ 只是热学等效参数，则不应强行用于严格干质量检验"
  ❌ **不下"数据有误"的结论**，也**不下"假设已验证"的结论**
  ❌ **不得为通过诊断而调整题设参数**

---

## 五、丙 —— 数据交付与论文负责人

| 时段 | 代码 | 图 | 交付物 |
|---|---|---|---|
| **D1 上午** | `CODE-31` 模板解析（**确认 21 列展开方式**）<br>`CODE-02` 环境插值（**忠实插值 + 受控平滑双版本**）<br>`CODE-03` 阶段识别（解释性）<br>**配合入口检查第 6 项** | — | `output_spec.json`；数据规格表 |
| **D1 下午** | `CODE-16` 采样（**验证采样误差与无过冲**）<br>`CODE-17` 导出器（**含 M0/M1/M2 标识**）<br>用**模拟结果**测试整条输出管线 | `G1` 几何/边界/路线合图（**标注三层**）<br>`G2` 环境数据（**须显示波动**）<br>`G3` 半径数据与拟合 | 可独立验收的输出管线；论文骨架 |
| **D1 晚间** | 生成 result1<br>`CODE-32` 题设表1/2<br>撰写问题1 模型与求解说明 | `G4` 问题1 温度与含水率演化双图 | **result1 首版**；表1/2 |
| **D2 上午** | 导出 result2、题设表3/4<br>撰写问题2 章节<br>**核实完整输出覆盖时长** | `G5` 问题2 前3小时径向结果<br>`FIG-13` 插值 vs 平滑对照 | result2；表3/4 |
| **D2 下午** | 导出 result3、result4、题设表5/6<br>统一四问表格与单位 | `G6` 问题3（**含阈值线与达标点**）<br>`G7` 问题4（**物理坐标 + 移动边界**） | **四个结果文件齐全**；表5/6 |
| **D2 晚间** | 核对四问数据一致性<br>形成完整论文初稿 | `G8` 基准误差与收敛（**空间/时间两幅**）<br>**`G12` 中心/表面/最大值三曲线** | **完整论文初稿** |
| **D3 上午** | 从**冻结结果**重建全部 Excel、六表、主文图<br>核对文件规格 | `G9` A/B/C 顺序差分<br>`G10` 情景区间<br>全部图统一字体/单位，导出 PDF+PNG | 最终 Excel 与图表 |
| **D3 下午** | 完成摘要、结论、图题及排版<br>检查表格、引用与符号 | — | 完整论文 |
| **D3 晚间** | `CODE-30` 最小复现材料与运行记录<br>回读四个 Excel；整理、备份、交付包 | — | 交付包 |

### 丙的关键提醒

- **D1 上午**：**读模板文件反向确认规格**。已实测：
  result1/2 = `温度`+`水分浓度` 双 sheet，A 列从 **1** 起；
  result3/4 = `Sheet1`，A 列从 **60** 起；
  result4 末列为字符串 **`药材表面`**；表头为 `时间\到药材的中心距离`。
  **仍需确认 `…` 处的 21 列展开方式。**
- **D1 上午**：`CODE-02` 要出**两个版本**。
  附件1 共 241 点，温度差分**变号 116 次**、水分**119 次**——**波动遍布全程**。
  默认用**忠实插值**（实测波动是真实边界，**不天然属于噪声**）；
  受控平滑仅作对照，**须说明平滑依据**。
  🔴 **不得推断控制器类型**（不能说"这是 PD 超调"）。
- **D1 下午**：**输出插值不自动守恒**；验证**采样误差与无过冲**；**三次插值不天然保正**。
  **M0/M1/M2 结果分开标识**，扩展结果不得混入题设基线。
- **D2 下午**：result4 **域外位置留空，绝不填 0**；
  **「药材表面」列随 $R(t)$ 变化**，不能固定取 2 cm。
- **D3 上午**：从**冻结结果**重建，不手工改表。
  **结果变更时：先更新配置标识和结果 → 自动重建表图 → 最后改论文**；
  **禁止只手改论文中的干燥时间。**

---

## 六、每日共同检查点

| 日 | 检查点 |
|---|---|
| **D1** | **入口检查 6 项完成且未阻塞计算** · 冻结变量基准（C vs Y）、单位、接口与 M0/M1/M2 层次 · 公共内核可被乙调用 · 输出管线可独立验收 · **result1 可打开且与源数据一致** · 问题4 不再只是待办事项 |
| **D2** | 固定域与收缩域均有稳定运行记录 · **四个结果文件齐全** · **五项退化检验与几何—密度诊断已完成** · 明确哪些结果可靠、哪些需重跑 · **冻结基础模型** |
| **D3** | 冻结最终数值结果并记录版本 · 完整论文交叉通读 · **删除无结果支持的结论** · 三人按自检表签核 |

**每天两次简短同步**：上午确认依赖与阻塞；晚间按实际交付物验收。

> **进度以「可运行、可读取、通过何种检查」描述，不以"完成百分之多少"代替证据。**

### Day 1 交付
**入口检查报告** · 接口规格 · result1 首版 · 表1/2 · 问题4 推导及初步模块 · 论文骨架
> 若基准不通过，**先修内核，不扩大实验**。

### Day 2 交付
四个 Excel 首版 · 题设六表 · 完整论文初稿 · **五项退化与一致性检验记录** ·
**几何—密度诊断结果** · A/B/C 对照初版
> 此时**优先解决错误，不增加新的物理扩展**。

### Day 3 交付
最终论文 · result1—4.xlsx · 题设六表 · 采用的图文件 · 最小复现材料与运行记录
> 最后至少留出约 **3 小时**用于修复、备份和提交准备，**不安排新的长耗时实验**。

---

## 七、依赖关系与延期处理

**主路径**：
**入口检查** → 接口冻结 → 公共内核 → 问题1 验证 → 问题2/3 与问题4 **并行** →
四份结果 → **退化检验 + 几何—密度诊断** → 冻结数值 → 图表与论文最终核对

| 风险 | 处理 |
|---|---|
| **入口检查未完成** | **不得进入建模**。乙集中完成，丙配合第 6 项，甲配合第 3 项 |
| **Day 1 内核延迟** | 甲乙共同定位错误；丙继续模板、示意图与论文文字；**取消第二套数值内核** |
| **Day 2 四问未齐** | **停止 M1/M2 工作**，三人集中完成缺失模型、验证与导出 |
| **长时计算较慢** | 自适应隐式步长，按所需时刻采样；**不因输出每 1 s 就强制全程 1 s 步长** |
| **附件缺失或含义不明** | 继续推导、接口、基准与论文骨架，**明确阻塞项**；**不得用未经核实的数字或人为数据替代正式附件结果** |
| **结果变更** | 先更新配置标识与结果 → 自动重建表图 → 再改论文；**禁止只手改论文中的干燥时间** |

---

## 八、提交前三人签核

- [ ] **甲**：数值方法、参数、最终数值与论文一致；**空间/时间误差分列报告**；复现入口可用
- [ ] **乙**：**入口检查 6 项有证据**；物理假设与结论边界无误；
      **五项退化检验在正确对照目标上通过**；**几何—密度诊断未过度解读**
- [ ] **丙**：四个 Excel 可重新打开；六表与源结果一致；
      **M0/M1/M2 标识清晰**；图表与论文显示正常
- [ ] **三人共同**：
      - [ ] 附件已实际核实；变量基准（C vs Y）已声明
      - [ ] **K/°C 转换**经独立复核
      - [ ] 达标判定用 **$C_{max}$**，基于**未舍入数据**，报告**阈值通过时刻**
      - [ ] result4「药材表面」列随半径变化，域外留空
      - [ ] **潜热 M1 仅在通量闭合后执行**；未闭合则只保留方程与数据缺口
      - [ ] **三种区间分开命名**；未将"2–3 天"当作验收区间
      - [ ] 结果与论文使用**同一配置版本**
      - [ ] 已预留最终修复与提交时间
