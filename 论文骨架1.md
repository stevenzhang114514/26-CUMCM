# 药材的烘干问题

## 摘要

本文针对中药材热风烘干过程中的传热传质问题，建立了一维轴对称径向数学模型，采用有限体积法（FVM）与隐式时间推进求解，依次回答了预热平衡阶段、整个烘干过程、烘干时间确定、考虑尺寸变化四个问题。

**问题1**：预热平衡阶段（0–1800 s），采用附录2 常物性。结果表明，温度场表面升温快、中心滞后，1800 s 时中心 33.58 ℃、表面 36.79 ℃；水分场干燥完全被限制在表层，中心含水率 30 min 内完全未变，表面降幅 40.8%。按附录2 物性，传热特征时间 $R_0^2/\alpha \approx 2369$ s，传质特征时间 $R_0^2/D \approx 8\times10^4$ s，两者相差约 60 倍。这解释了为什么 30 min 内温度场已显著演化，而水分场只在表层动了 41%。

**问题2**：整个烘干过程采用附录3 变物性与双向耦合。按附录3 物性，传热特征时间 $R_0^2/\alpha \approx 2762$ s，比附录2 慢 14%；传质特征时间 $R_0^2/D$ 在 $C=2.55$ 时约 $7.1\times10^4$ s、在 $C=0.15$ 时可达 $5.0\times10^5$ s。前 3 h 内温度接近烘房温度（中心 49.85 ℃、表面 49.97 ℃），含水率显著下降但远未达终点（中心 1.77、表面 1.01）。由于变物性使热扩散率降低 14%、且 $D$ 含 Arrhenius 温度依赖，问题2 相比问题1 更冷、更湿，这直接体现了"问题1 可以先水后温、问题2 必须联立"。

**问题3**：在问题2 模型上，确定全域最大含水率 $C_{max}(t)$ 首次低于 0.15 kg/kg 的时刻。$C_{max}$ 与中心含水率几乎重合，说明中心是最难干燥的位置。若误用平均值判定，会把 2.4 天的工艺说成 1.5 天（偏差 37%）。$t^* = 206960$ s ≈ 57.49 h ≈ 2.40 天。

**问题4**：考虑尺寸变化，采用材料坐标与均匀径向仿射收缩假设。$t_{dry} = 73.03$ h。若忽略收缩，则 $t > 120$ h 仍未达标，说明收缩显著加速干燥。

**模型检验**：Bessel 解析基准误差 < 0.001 ℃；空间二阶、时间一阶；事件定位误差 ±0.0005 s；内部界面通量抵消达机器精度；五项退化与一致性检验通过；几何—密度一致性诊断提示密度定义、数据误差、几何假设或干物质守恒的联合使用需要核查。

**灵敏度**：参数敏感度排序为 $D \gg h_m \gg h$。环境外推得到的情景区间为 $t^* \in [56.95, 57.87]$ h（±0.8%）。

**本文主要创新点**：（1）揭示问题1 的单向耦合结构（$D$ 不依赖 $T$），可先解水分再解温度；（2）问题4 采用材料坐标与均匀仿射收缩假设自洽的导数形式，避免显式 $\dot R(t)$ 对流项放大数值噪声。

**关键词**：热风烘干；圆柱体传热传质；有限体积法；水分扩散；收缩模型
---

## 一、问题重述

### 1.1 问题背景

干燥是决定中药材成品品质的关键工序之一，热风烘干是常见方式。该方式主要包括预热平衡和恒温干燥两个阶段，通过调控烘房温湿环境完成药材干燥。工艺参数选取不当会导致干燥效率低、能耗高、成品品质不稳定。传统试验优化模式成本高、周期长，需要借助数理分析与数值仿真得到干燥规律。

### 1.2 问题提出

- **问题1**：建立预热平衡阶段药材温度和水分浓度变化规律的数学模型，给出指定时刻、指定位置的结果，并保存完整结果。
- **问题2**：建立整个烘干过程药材温度和水分浓度变化规律的数学模型，给出指定时刻、指定位置的结果，并保存完整结果。
- **问题3**：按烘干要求（药材各处水分浓度低于 0.15 kg/kg），确定药材烘干所需时间。
- **问题4**：考虑药材因水分流失发生尺寸变化，确定药材的烘干时长。

---

## 二、问题分析

### 2.1 几何与降维

药材为圆柱，长 $L_0=0.25$ m，初始半径 $R_0=0.02$ m。题目所有输出只到“距中心 2 cm”，即最大距离恰为初始半径。端面与侧面面积比约为 $R_0/L_0=0.08$。端面方向与径向的扩散时间尺度比约为

$$\frac{t_z}{t_r}\sim\left(\frac{L_0/2}{R_0}\right)^2=\left(\frac{0.125}{0.02}\right)^2\approx39.06$$

因此本文采用**一维轴对称径向模型**，最终以二维轴对称对照计算验证。
![G1 几何、边界与烘干路线合图](../figs/G1_geometry.png)

### 2.2 变量定义

严格区分：

$$C=\frac{m_w}{m_d}\quad(\text{药材干基含水率}),\qquad
Y=\frac{m_v}{m_{da}}\quad(\text{空气含湿量})$$

即使同为 kg/kg，也不能自动相减。本文引入 $C_\infty^{eq}$ 表示已明确基准的等效环境值。

### 2.3 技术路线

本文技术路线如图 G1b 所示：从问题重述出发，先明确物理机制与变量定义，再建立偏微分方程模型，采用有限体积法（FVM）与隐式时间推进求解，依次回答四问，最后通过解析基准、收敛性、守恒、退化等检验验证模型。

![G1b 问题分析 / 技术路线流程图](../figs/G1b_flowchart.png)

### 2.4 四问递进

- **问题1**：预热平衡阶段，采用附录2 常物性；
- **问题2**：整个烘干过程，采用附录3 变物性、双向耦合；
- **问题3**：在问题2 模型上，确定全域最大含水率 $C_{max}$ 首次低于 0.15 kg/kg 的时刻；
- **问题4**：引入材料坐标与均匀径向仿射收缩，采用附录4 物性。

### 2.5 模型分层

- **M0**：题设基线，按附录与给定边界回答四问，**必做**；
- **M1**：可核查扩展，潜热、辐射、端面、参数情景，**条件充分时做**；
- **M2**：工程推广，烘房系统、品质、结构，**只给路线与数据需求**。

## 三、模型假设

1. **连续介质假设**：药材视为连续介质，温度、含水率在空间上连续分布；
2. **轴对称假设**：药材为圆柱，几何与边界条件轴对称，采用一维径向模型；
3. **局部热平衡假设**：同一空间点固相与液相温度相同；
4. **均匀仿射收缩假设**（问题4）：固定长度 + 均匀径向仿射收缩，$v_r=(\dot R/R)r$；
5. **物性经验公式适用性**：$\rho$、$c_p$、$k$、$D$ 采用附录给出的经验公式，不替换文献参数；
6. **环境边界等效约定**：按题设等效约定，将附件1 的水分浓度作为药材表面的等效平衡含水率 $C_\infty^{eq}$，不假装是吸附平衡结果；
7. **一维径向近似**：端面方向与径向的扩散时间尺度比约 39，端面影响以二维轴对称对照验证；
8. **模型分层**：M0 题设基线必做；M1 可核查扩展条件充分时做；M2 工程推广只给路线与数据需求。


## 四、符号说明

完整符号说明见 TAB-01 符号说明表。主要变量如下：

| 符号 | 含义 | 单位 |
|---|---|---|
| $t$ | 时间 | s |
| $r$ | 到药材中心的距离 | m |
| $R(t)$ | 当前半径 | m |
| $L_0$ | 药材长度 | m |
| $\xi=r/R(t)$ | 归一化径向坐标 | — |
| $T$ | 药材温度 | ℃（D 公式中为 K） |
| $T_\infty$ | 烘房温度 | ℃ |
| $C$ | 药材干基含水率 $m_w/m_d$ | kg/kg |
| $Y$ | 空气含湿量 $m_v/m_{da}$ | kg/kg |
| $C_\infty^{eq}$ | 等效环境平衡含水率 | kg/kg |
| $C_{max}(t)$ | 全域最大含水率 | kg/kg |
| $t_*$ | 阈值通过时刻 | s |
| $\rho$ | 湿物料体积密度 | kg/m³ |
| $\rho_d$ | 干固体密度 $\rho/(1+C)$ | kg/m³ |
| $c_p$ | 比热容 | J/(kg·K) |
| $k$ | 热传导系数 | W/(m·K) |
| $D$ | 水分有效扩散系数 | m²/s |
| $h$ | 对流换热系数 | W/(m²·K) |
| $h_m$ | 对流传质系数 | m/s |
| $\lambda$ | 汽化潜热 | J/kg |
| $J_w$ | 向外的水质量通量 | kg/(m²·s) |
| $e_d(t)$ | 几何—密度一致性诊断量 | — |
| $Bi_T$ | 传热毕渥数 | — |
| $Bi_m$ | 传质毕渥数 | — |

---

## 五、数据处理

### 5.1 环境数据及插值

附件1 给出烘房温度与水分浓度的实测序列，共 241 点，覆盖 0–14400 s。

实测发现：温度差分变号 116 次，水分浓度变号 119 次，**波动遍布全程**，不只是尾部。故本文采用**忠实插值**（PCHIP 过全部实测点）作为默认版本，**受控平滑**作为对照。

![G2 环境数据及插值](../figs/G2_env.png)

![FIG-13 环境插值 vs 受控平滑对照](../figs/FIG-13_smoothing.png)

### 5.2 阶段识别

以温度达到最终升温 95% 为判据，预热平衡阶段约为 0–4898 s；以升温速率小于 0.001 ℃/s 为判据，趋于稳定点为 1866 s。本文以 4898 s 作为预热/恒温分界。

![G2b 阶段识别](../figs/G2b_stage.png)

### 5.3 半径数据

附件2 给出药材半径随时间变化，共 145 点，覆盖 0–259200 s（72 h）。半径从 2.0 cm 严格单调递减到 1.198 cm。本文采用 PCHIP 保单调插值拟合 $R(t)$。

> 本文 PCHIP 拟合与乙的 MATLAB 实现在 $10^{-4}$ cm 量级内一致。

![G3 半径数据、拟合曲线与残差](../figs/G3_radius.png)

---

## 六、模型建立与求解

### 6.1 问题1：预热平衡阶段

#### 6.1.1 模型建立

$$\rho c_p\frac{\partial T}{\partial t}=\frac{k}{r}\frac{\partial}{\partial r}\!\left(r\frac{\partial T}{\partial r}\right),\qquad
\frac{\partial C}{\partial t}=\frac{1}{r}\frac{\partial}{\partial r}\!\left(D(C)\,r\frac{\partial C}{\partial r}\right)$$

$$D(C)=7\times10^{-9}\exp\!\left(-\frac{0.89}{C}\right)$$

#### 6.1.2 数值方法

**空间离散**：采用有限体积法（FVM），中心用半控制体（半径 $\Delta r/2$ 的实心圆柱），天然规避 $1/r$ 奇点；界面通量用**调和平均**，保证变 $D$ 时通量正确。

**网格**：均匀网格在表面早期欠分辨（t=1 s 时边界层厚度仅 0.07 mm，均匀 N=640 误差仍达 4×10⁻⁴）。故采用向表面加密的**渐变网格** $N=200$、$\gamma=1.5$（$\Delta r = 0.00707 \sim 0.14981$ mm）。渐变网格以约一半计算量取得优于均匀 N=640 的精度。

**时间推进**：半控制体 FVM 中心处显式稳定性上限约 $\Delta r^2/(4\alpha)$，均匀网格 $\Delta r=0.5$ mm 时为 0.37 s，无法满足 1 s 输出要求。故采用 L-稳定的 **Backward Euler** 隐式推进，步长由误差控制自适应调整，输出时刻精确落点。

**非线性迭代**：水分方程因 $D(C)$ 非线性，采用 **Picard 迭代**，保持 M-矩阵性质，$C$ 的正性由构造保证（平均 2.92 次，最多 3 次）。收敛采用尺度归一化的**双重判据**（更新量与残差都要满足）。

**输出采样**：r=0 用偶函数二次外推，0<r<R₀ 用 PCHIP 保形插值，r=R₀ 用 Robin 边界重构值。

![FIG-08 有限体积离散示意图](../figs/FIG-08_fvm.png)

#### 6.1.3 结果

问题1 求解了预热平衡阶段（0–1800 s）药材温度与含水率的时空演化。生产网格采用渐变网格 $N=200$、$\gamma=1.5$（$\Delta r = 0.00707 \sim 0.14981$ mm），时间采用 Backward Euler 隐式推进，非线性项用 Picard 迭代。

图 G4 给出了温度场与含水率场的演化：

- **温度场**：表面温度从 28 ℃ 升至约 36.8 ℃，中心温度升至约 33.6 ℃，表面始终高于中心，符合外部热风加热的物理过程；
- **含水率场**：中心含水率在 1800 s 内基本不变（2.55 kg/kg），表面从 2.55 降至约 1.51 kg/kg，呈明显的表面干燥特征。

论文表 1、表 2 分别给出 t = 100、300、600、900、1200、1500、1800 s 与 r = 0、0.5、1.0、1.5、2.0 cm 处的温度与含水率：

**表1 30分钟内药材的温度（℃）**

| 时间/s | 0 cm | 0.5 cm | 1.0 cm | 1.5 cm | 2.0 cm |
|---|---|---|---|---|---|
| 100 | 28.0001 | 28.0003 | 28.0039 | 28.0319 | 28.1796 |
| 300 | 28.0408 | 28.0635 | 28.1514 | 28.3674 | 28.8488 |
| 600 | 28.4538 | 28.5365 | 28.8043 | 29.3159 | 30.1643 |
| 900 | 29.3249 | 29.4589 | 29.8763 | 30.6168 | 31.7298 |
| 1200 | 30.5435 | 30.7106 | 31.2231 | 32.1135 | 33.4298 |
| 1500 | 31.9966 | 32.1877 | 32.7672 | 33.7480 | 35.1214 |
| 1800 | 33.5763 | 33.7728 | 34.3646 | 35.3625 | 36.7861 |

**表2 30分钟内药材的水分浓度（kg/kg）**

| 时间/s | 0 cm | 0.5 cm | 1.0 cm | 1.5 cm | 2.0 cm |
|---|---|---|---|---|---|
| 100 | 2.5500 | 2.5500 | 2.5500 | 2.5500 | 2.2470 |
| 300 | 2.5500 | 2.5500 | 2.5500 | 2.5492 | 2.0508 |
| 600 | 2.5500 | 2.5500 | 2.5500 | 2.5353 | 1.8770 |
| 900 | 2.5500 | 2.5500 | 2.5497 | 2.5045 | 1.7547 |
| 1200 | 2.5500 | 2.5500 | 2.5482 | 2.4646 | 1.6586 |
| 1500 | 2.5500 | 2.5499 | 2.5445 | 2.4206 | 1.5787 |
| 1800 | 2.5500 | 2.5497 | 2.5383 | 2.3755 | 1.5103 |

完整结果见 `result1.xlsx`。

![G4 问题1 温度与含水率演化](../figs/G4_p1_evolution.png)

#### 6.1.4 分析与讨论

**（1）温度：表面升温快，中心严重滞后**

t = 1800 s 时中心 33.5763 ℃、表面 36.7861 ℃，**径向温差 3.2098 ℃**，而中心仅完成烘房升温幅度的 $(33.58-28)/(41.51-28) = 41.3\%$。

> **这直接印证 $Bi_T = 1.389$**：内外导热阻力相当，**集总参数模型不可用**。

**（2）水分：干燥完全被限制在表层，中心未被触及**

t = 1800 s 时：

| 位置 | 含水率 | 变化 |
|---|---|---|
| 中心 | 2.5500 | **0.0000**（30 min 内完全未变） |
| 表面 | 1.5103 | −1.0073（**降幅 40.01%**） |

$C = 2.5$ kg/kg 的等值线（"干燥前沿"）位于 $r \approx 1.3$ cm —— 即 30 分钟内水分变化只发生在**外侧约 0.7 cm** 的薄层。

**（3）干燥前沿呈"由表及里推进"形态**

从表2 可见：表面持续单调下降；而 1.0 cm 处直到约 t=900 s 才开始可见变化，1.5 cm 处到 t=1800 s 才降 0.17。这正是"**表面是唯一的门**、内部水分必须接力外迁"的直接体现。

**（4）温度与水分的时间尺度差约 60 倍**

传热特征时间 $R_0^2/\alpha \approx 2369$ s，而传质特征时间 $R_0^2/D$（按 $D \approx 5\times10^{-9}$ m²/s）$\approx 8\times10^4$ s。实测表现完全一致：**30 分钟内温度场已显著演化，而水分场只在表层动了 40%**。

> **这解释了为什么烘干需要 2–3 天，而预热只需约 40 分钟。**

**（5）表面温度远未跟上烘房温度**

t = 1800 s 时烘房已达 41.513 ℃，而药材表面仅 36.7861 ℃ —— 即表面只完成烘房升温幅度的 **65.0%**。

> 注意：这是 M0（**不含蒸发潜热项**）的结果。若计入蒸发吸热，表面温度还会更低。**M1 扩展不得直接套用本结论**，必须先在通量闭合的前提下重算。

---

### 6.2 问题2：整个烘干过程

#### 6.2.1 模型建立

问题2 求解**整个烘干过程**，从 $t=0$ 统一采用附录3 的变物性经验公式：

$$\rho(C)c_p(C)\frac{\partial T}{\partial t}=\frac{1}{r}\frac{\partial}{\partial r}\!\left(k(C)r\frac{\partial T}{\partial r}\right),\qquad
\frac{\partial C}{\partial t}=\frac{1}{r}\frac{\partial}{\partial r}\!\left(D(C,T)r\frac{\partial C}{\partial r}\right)$$

$$D=2.4\times10^{-3}\exp(-0.45/C)\exp[-3850/(T+273.15)]$$

**题面明确"为简化问题，相关经验公式统一采用附录3"**，故从 $t=0$ 起就用附录3，**不存在阶段切换**，也不需要"切换处保持状态连续"的处理。

**双向耦合**：$D$ 依赖 $T$，水分场不再独立，$T$ 与 $C$ 必须联立求解。

![FIG-16 无量纲数分析](../figs/FIG-16_dimensionless.png)
![FIG-18 扩散系数等值线](../figs/FIG-18_D_contour.png)

#### 6.2.2 边界外推

附件1 只覆盖 0–14400 s（4 h），而问题2/3 需 2–3 天，恒温段边界**必须显式假设**。

**处理方案**：

| 时段 | 处理 |
|---|---|
| $t \le 14400$ s | PCHIP 过全部 241 个实测点（**忠实插值**） |
| $t > 14400$ s | $T_\infty \equiv 50.0017$ ℃、$C_\infty^{eq} \equiv 0.049998$ kg/kg（$t \ge 9600$ s 段 n=81 的均值） |

**切换点处的阶跃**：温度 −0.1633 ℃、含水率 $+1.38\times10^{-4}$ —— 落在实测波动带内（该段 std：T 0.1558 ℃、C $1.57\times10^{-4}$），**小于一个标准差**。

> 这是"把噪声末点拉回其自身均值"，**不是引入新信息**。
> 
> **不得推断控制器类型**，**不得据附件1 推断烘房结构**。

**敏感性分析**：取 $t_{pre} \in \{9600, 14400\}$ s、$T_\infty \in [49.8, 50.3]$ ℃、$C_\infty^{eq} \in [0.0495, 0.0505]$ 做情景扫描：

| 情景 | $t^*$ / h | Δ vs 基线 |
|---|---|---|
| 基线（$t_{pre}=14400$ s、$T_\infty=50.0017$ ℃、$C_\infty^{eq}=0.049998$） | 57.49 | — |
| $t_{pre} = 9600$ s | 57.49 | ≈0 |
| $T_\infty = 49.8$ ℃ | 57.87 | +0.39 h（+0.67%） |
| $T_\infty = 50.3$ ℃ | 56.95 | −0.54 h（−0.94%） |
| $C_\infty^{eq} = 0.0495$ | 57.48 | −0.01 h |
| $C_\infty^{eq} = 0.0505$ | 57.50 | +0.01 h |

**由环境外推得到的情景区间**：$t^* \in [56.95, 57.87]$ h = [2.373, 2.411] 天（±0.8%）。

其中 $t_{pre}$ 的取值对结果**没有影响**（57.49 与基线完全一致）—— 因为 PCHIP 数据段与平台值本身是连续过渡的，切换点在 9600 还是 14400 无关紧要。
**环境平台窗口的选择影响**：本文取 $t\ge9600$ s 段（n=81）均值。若改取 $t\ge12400$ s 段，$t^*$ 会从 57.4889 h 变到 57.4747 h，跨度约 0.03 h（1090 s）。这属于**建模约定不同**，不是模型或算法误差。

#### 6.2.3 数值方法

问题2 在问题1 内核基础上做两项扩展：

**（1）变物性走守恒形式**：面导热系数 $k$ 用调和平均、热容 $(\rho c_p)$ 用单元值，**二者不可合并**。若令 $\Gamma = k/(\rho c_p)$ 再走原路径，等于把两者在同一个面上一起做调和平均，$\rho c_p$ 沿 $r$ 变化数倍时会产生明显偏差。

**（2）块 Picard 用 Gauss–Seidel 序**：$\rho c_p$、$k$ 对 $C$ 的依赖是单向的，用最新 $C$ 可显著加快收敛且不破坏任何子问题的 M-矩阵性质。

**残差判据不可省**：只查更新量会落入"更新量变小但残差停滞"的陷阱；本文的双重判据对 $T$、$C$ 两个场同时成立才算收敛。

**网格与时间**：生产网格 $N=200$、$\gamma=1.5$，$\theta=1$（Backward Euler），自适应步长，输出时刻精确落点。3 h 正式版接受步 12652 次、拒绝步 4 次，平均 3.00 次 Picard 迭代，用时 78 s。

#### 6.2.4 结果

问题2 求解了整个烘干过程，采用附录3 的变物性经验公式与双向耦合。前 3 h 的径向分布如图 G5 所示。

**温度场**：

- 前 3 h 内整体升温到接近烘房温度；
- t = 3 h 时中心 49.8494 ℃，表面 49.9666 ℃；
- 表面略高于中心，符合热风加热过程。

**含水率场**：

- 前 3 h 内含水率显著下降，但远未达终点；
- t = 3 h 时中心 1.7662 kg/kg，表面 1.0081 kg/kg；
- 表面先干，中心后干。

**表3 3小时内药材的温度（℃）**

| 时间/h | 0 cm | 0.5 cm | 1.0 cm | 1.5 cm | 2.0 cm |
|---|---|---|---|---|---|
| 0.5 | 32.1902 | 32.3830 | 32.9666 | 33.9613 | 35.4136 |
| 1.0 | 40.3821 | 40.5541 | 41.0608 | 41.8783 | 43.0000 |
| 1.5 | 45.8467 | 45.9348 | 46.1929 | 46.6052 | 47.1395 |
| 2.0 | 48.4500 | 48.4879 | 48.5982 | 48.7734 | 49.0032 |
| 2.5 | 49.4668 | 49.4791 | 49.5135 | 49.5645 | 49.6617 |
| 3.0 | 49.8494 | 49.8552 | 49.8746 | 49.9100 | 49.9666 |

**表4 3小时内药材的水分浓度（kg/kg）**

| 时间/h | 0 cm | 0.5 cm | 1.0 cm | 1.5 cm | 2.0 cm |
|---|---|---|---|---|---|
| 0.5 | 2.5499 | 2.5489 | 2.5255 | 2.3257 | 1.6486 |
| 1.0 | 2.5257 | 2.4948 | 2.3578 | 2.0230 | 1.4711 |
| 1.5 | 2.3860 | 2.3256 | 2.1344 | 1.8020 | 1.3476 |
| 2.0 | 2.1708 | 2.1084 | 1.9236 | 1.6259 | 1.2311 |
| 2.5 | 1.9567 | 1.9006 | 1.7360 | 1.4720 | 1.1166 |
| 3.0 | 1.7662 | 1.7165 | 1.5702 | 1.3333 | 1.0081 |

完整结果见 `result2.xlsx`。

![G5 问题2 前3小时径向分布](../figs/G5_p2_radial.png)

问题2 相比问题1 有三项扩展：

1. **变物性**：$\rho(C)$、$c_p(C)$、$k(C)$、$D(C,T)$ 全部随场变化，方程强非线性；
2. **双向耦合**：$D$ 依赖 $T$，水分场不再独立，$T$ 与 $C$ 必须联立求解；
3. **全流程边界**：跨越 2–3 天，附件1 只给 4 h，需明确边界延伸假设。

与问题1 的单向耦合结果相比，在 t = 1800 s 处双向耦合使中心温度降低约 **1.386 ℃**（问题1：33.5763 ℃；问题2：32.1902 ℃）。**这属于模型差异分析，不是两套实现必须相等的正确性验证。**

**差异方向值得注意**：表面上附录3 的 D 在 30 ℃ 附近反而略大于附录2（$D_{app3}(2.55, 30\,°C) = 5.64\times10^{-9}$ vs $D_{app2}(2.55) = 4.94\times10^{-9}$），**本应干燥更快**；但实测问题2 **更冷、更湿**。原因正是**双向耦合**：

- 附录3 的 $\rho c_p$ 更大、$k$ 更高，热扩散率**低 14%** → 升温更慢；
- 而 $D$ 含 Arrhenius 因子 $\exp(-3850/T_K)$（约 4%/K），温度偏低又**反馈**压低扩散系数。

这正是"问题1 可以先水后温、问题2 必须联立"的直接数值体现。

作为对照，M1 在 M0 基础上开启蒸发潜热后，前 3 h 中心温度额外降低 **18.06 K**；M1 为条件性扩展，不改变问题2 的正式答案。

进一步分析附录2、附录3、附录4 给出的 $D(C)$ 关系可知，水分扩散系数在水分浓度方向上的变化幅度分别为：

- 附录2：**266.2 倍**；
- 附录3：**16.8 倍**；
- 附录4：**6.6 倍**。

需要特别说明的是，题面并未声明“265 倍”这一比值，该比值是本文根据附录数据计算得到的结果。因此，在论文中应写成“本文计算得到”，而不能写成“题目给出”。

前 3 h 内温度接近烘房温度，含水率显著下降但远未达终点。取初始状态（$T = 28^\circ\text{C}$，$C = 2.55\ \text{kg/kg}$）估算特征时间，可得：

- 传热特征时间 $R_0^2/\alpha \approx 2761.9\ \text{s} \approx 46.0\ \text{min}$；
- 传质特征时间 $R_0^2/D \approx 7.09\times10^4\ \text{s} \approx 19.7\ \text{h}$。

因此前 3 h 温度已接近平衡，水分仍在缓慢扩散。整个烘干过程由水分扩散主导，约 2–3 天。

---

### 6.3 问题3：烘干时间确定

#### 6.3.1 判据定义

$$C_{max}(t)=\max_{r\in[0,R_0]}C(r,t),\qquad
t_*=\inf\{\,t:\ C_{max}(t)<0.15\,\}$$

#### 6.3.2 求解策略

**主结果**（甲的事件检测精确定位）：

| 项 | 值 |
|---|---|
| 阈值通过时刻 $t^*$ | **206959.9521 s = 57.4889 h = 2.3954 天** |
| 交付取整（最早整数秒） | **206960 s**（`result3.xlsx` 末行） |
| $C_{max}(t^*)$ | 0.1499999998（未舍入） |
| 最大值位置 | $r = 0$（中心），逐时刻核对 |
| 二分次数 | 15 |

**求解方法**：把 $C_{max}(t) - 0.15$ 作为 ODE 求解器的终止事件，自适应精确定位零点。二分区间总宽约 20 s（`bracket = [206940.0, 206959.9521]`），$n_{bisect} = 15$。

**注意**：$C_{max}(t^*) = 0.15$ 而非严格小于 —— 这正是"连续模型的阈值交点"的特征。本文报告的是**「阈值通过时刻」**。

#### 6.3.3 结果

问题3 在问题2 模型基础上，确定全域最大含水率 $C_{max}(t)$ 首次低于 0.15 kg/kg 的时刻。

**达标时间**：$t^* = 206959.9521\ \text{s} = 57.4889\ \text{h} = 2.3954\ \text{天}$（甲的事件检测精确定位）。

**C_max(t\*)**：0.1499999998（未舍入），显示为 0.1500。即连续模型的阈值交点满足 $C_{max} = 0.15$，本文报告的是**「阈值通过时刻」**。

**最大值位置**：$r = 0$（中心），逐时刻核对。

**图 G6** 给出 $C_{max}(t)$ 衰减曲线与 0.15 阈值线，达标点用红点标出；右图为终态径向分布。

**图 G12** 对比中心、表面、全域最大值、平均值四条曲线，说明：

- $C_{max}(t)$ 与中心 $C(0,t)$ 几乎重合，**最大值出现在中心**；
- 表面 $C(R,t)$ 远低于中心，表面先干；
- 平均值 $\bar{C}(t)$ 明显低于 $C_{max}$，**不能用平均值判定达标**。

**表5 药材烘干过程的水分浓度（kg/kg）**

| 时间/h | 0 cm | 0.5 cm | 1.0 cm | 1.5 cm | 2.0 cm |
|---|---|---|---|---|---|
| 6 | 1.0170 | 0.9882 | 0.9016 | 0.7551 | 0.5328 |
| 12 | 0.4562 | 0.4432 | 0.4031 | 0.3298 | 0.1642 |
| 18 | 0.2989 | 0.2916 | 0.2687 | 0.2253 | 0.0845 |
| 24 | 0.2378 | 0.2328 | 0.2168 | 0.1856 | 0.0665 |
| 30 | 0.2058 | 0.2019 | 0.1892 | 0.1641 | 0.0598 |
| 36 | 0.1859 | 0.1826 | 0.1718 | 0.1504 | 0.0566 |
| 42 | 0.1721 | 0.1692 | 0.1597 | 0.1407 | 0.0549 |
| 48 | 0.1618 | 0.1592 | 0.1507 | 0.1335 | 0.0537 |
| 54 | 0.1539 | 0.1515 | 0.1437 | 0.1277 | 0.0530 |
| 烘干结束时间（57.4889 h） | 0.1500 | 0.1477 | 0.1402 | 0.1249 | 0.0527 |

未舍入判定值 `C_max(t*) = 0.1499999998` kg/kg，显示为 0.1500。

完整结果见 `result3.xlsx`。

![G6 问题3 阈值与终态](../figs/G6_p3_threshold.png)

![G12 中心 / 表面 / 全域最大值三曲线](../figs/G12_max_center_surface.png)

#### 6.3.4 分析与讨论

**（1）最大值出现在中心，必须用 $C_{max}$ 判定**

由 G12 可见，$C_{max}(t)$ 与中心 $C(0,t)$ 几乎重合，说明**中心是最难干燥的位置**。题面要求"药材**各处**的水分浓度应低于 0.15"，故只有 $C_{max}$ 是合法判据。

不同判据给出的达标时间：

| 判据 | 达标时间 | 相对正确值的偏差 |
|---|---|---|
| $C_{max}(t)$（正确） | 57.49 h | — |
| 面积平均 $\bar C(t)$ | 36.04 h | **低估 21.44 h（37.3%）** |
| 表面 $C(R_0,t)$ | 12.55 h | 低估 44.94 h（78.2%） |

其中平均与表面判据的达标时间由同一模型结果按对应判据重新定位得到。**误用平均值会把 2.4 天的工艺说成 1.5 天。**

**（2）达标时间与阈值交点**

连续模型的阈值交点通常满足 $C_{max}(t^*) = 0.15$，本文报告的是**「阈值通过时刻」**，并单独验证其后一段时间内的全场值。

$t^*$ 之后以 720 s 为间隔取 6 个探针点，$C_{max}$ 逐点核验：

| $t$ | $C_{max}$ |
|---|---|
| $t^*$ | 0.1500000 |
| $t^* + 720$ s | 0.1497910 |
| $t^* + 1440$ s | 0.1495835 |
| $t^* + 2160$ s | 0.1493772 |
| $t^* + 2880$ s | 0.1491724 |
| $t^* + 3600$ s | 0.1489688 |

全部 < 0.15，**持续达标**。

**（3）三类误差分开报告**

| 误差类型 | 量值 | 说明 |
|---|---|---|
| 事件定位误差 | ±0.0005 s | 二分容差的一半（tol = 1e-3 s） |
| 空间离散误差 | 阶 +2.2（均匀网格） | 对 $t^*$ 的影响由 N 细化给出 |
| 时间积分误差 | 一阶（Backward Euler） | 拟合 +1.29 ~ +1.43，本文不声称时间二阶 |

**定位到 1 s 不代表干燥时间整体准确到 1 s。** 事件定位误差 0.5 ms 只是"在给定离散下找零点"的精度；真正的误差由空间离散与时间积分主导。

**（4）烘干过程的物理本质：恒温干燥**

由时间尺度分析（见 6.2.4）：

- 传质特征时间随工况变化：$T = 28^\circ\text{C}$、$C = 2.55$ 时 $\tau_m \approx 19.7$ h；$T = 50^\circ\text{C}$、$C = 2.55$ 时 $\tau_m \approx 8.2$ h；$T = 50^\circ\text{C}$、$C = 0.15$ 时 $\tau_m \approx 139$ h；
- $\tau_m/\tau_T \approx 11 \sim 270$。

约 4–5 个传热时间常数（≈3.5 h）内温度场就基本平衡，之后是长达 **~54 h 的恒温干燥**，温度不再是限制因素。**整个"2–3 天"的时长由水分扩散单独决定。**

这解释了"预热平衡 + 恒温干燥"两阶段的自然划分，也说明恒温段的边界设定对 $t^*$ 的影响远大于对温度场的影响。
---

#### 6.4 问题4：考虑尺寸变化

#### 6.4.1 模型建立

问题1–3 中，药材半径固定 $R_0 = 2$ cm，计算域是 $[0, R_0]$。问题4 中，半径随时间缩小，$R(t)$ 从 2.000 cm 降到 1.198 cm，计算域是 $[0, R(t)]$，随时间变化。

**引入归一化坐标**：

$$\xi = \frac{r}{R(t)},\qquad \xi \in [0, 1]$$

使计算域固定在 $[0, 1]$，网格不动，边界固定在 $\xi = 0$ 和 $\xi = 1$。

**均匀径向仿射收缩假设**：附件2 只给整体半径 $R(t)$，无内部位移数据。因此假设内部各点按比例均匀收缩，即材料径向速度

$$v_r = \frac{\dot R}{R}\, r$$

> **必须声明**：这是**假设**，不是由数据验证的结论。单个表面半径不能识别内部位移是否仿射。

在均匀仿射收缩假设下，水分与温度方程变为（材料导数形式，对流项被消去）：

$$\frac{\partial\widetilde C}{\partial t} = \frac{1}{R(t)^2\xi}\frac{\partial}{\partial\xi}\!\left(\xi D\frac{\partial\widetilde C}{\partial\xi}\right)$$

$$\rho c_p\frac{\partial\widetilde T}{\partial t} = \frac{1}{R(t)^2\xi}\frac{\partial}{\partial\xi}\!\left(\xi k\frac{\partial\widetilde T}{\partial\xi}\right)$$

其中 $\widetilde C(\xi,t) = C(\xi R(t), t)$，$\widetilde T(\xi,t) = T(\xi R(t), t)$。

**注意**：因为材料随网格均匀收缩，对流项被消去，方程形式比固定坐标下更简洁。$R(t)$ 只以 $R(t)^2$ 的形式出现在分母上。

**边界条件**：

中心 $\xi = 0$（对称）：

$$\frac{\partial\widetilde C}{\partial\xi} = 0,\qquad \frac{\partial\widetilde T}{\partial\xi} = 0$$

表面 $\xi = 1$（对流）：

$$-\frac{D}{R}\frac{\partial\widetilde C}{\partial\xi} = h_m(\widetilde C_s - C_\infty^{eq}),\qquad -\frac{k}{R}\frac{\partial\widetilde T}{\partial\xi} = h(\widetilde T_s - T_\infty)$$

**注意**：表面边界带 $1/R$ 因子，这是坐标变换的结果。

**物性采用附录4**：

$$\rho = 760 + 90C,\qquad c_p = 1850 + 2150\frac{C}{1+C},\qquad k = 0.12 + 0.20\frac{C}{1+C}$$

$$D = 4.2\times10^{-4}\exp\!\left(-\frac{0.30}{C}\right)\exp\!\left(-\frac{3850}{T_K}\right),\qquad T_K = T + 273.15$$

**初始条件**：

$$T(\xi, 0) = 28\ ^\circ\text{C},\qquad C(\xi, 0) = 2.55\ \text{kg/kg}$$

**半径函数**：$R(t)$ 由附件2 的 PCHIP 插值给出，$R(t)$ 从 2.000 cm 单调递减到 1.198 cm，覆盖 0 到 259200 s（72 h）。

#### 6.4.2 几何—密度一致性诊断

采用均匀仿射收缩假设，附件2 只给整体半径 $R(t)$，无内部位移数据。定义干固体密度：

$$\rho_d(C) = \frac{\rho(C)}{1+C} = \frac{760+90C}{1+C}$$

诊断量：

$$M_d^{diag}(t) = 2\pi L \int_0^{R(t)} \frac{\rho(C)}{1+C} r \, dr,\qquad e_d(t) = \frac{M_d^{diag}(t) - M_d^{diag}(0)}{M_d^{diag}(0)}$$

详细诊断结果与讨论见 7.6 节。

#### 6.4.3 数值方法

**计算设置**：

| 项 | 值 |
|---|---|
| 空间网格 | $N = 80$，$\Delta\xi = 1/79 \approx 0.0127$ |
| 时间步长 | $\Delta t = 10$ s |
| 计算上限 | $t_{max} = 302400$ s（84 h） |
| 目标阈值 | $C_{max} < 0.15$ kg/kg |
| 求解器 | Thomas 三对角求解器（与 MATLAB 内置求解器差 $1.77\times10^{-15}$，机器精度） |

**调试记录要点**（乙在汇总 §5 里给了）：

1. 初版下对角索引偏移 → 修正为 `dl = [0; -G]`；
2. 上对角符号错误 → 修正为 `du = [-G; 0]`；
3. 表面边界条件**多除了 $\Delta\xi$** → 修正为 `hm/R` 和 `h/R`，而不是 `hm/(R·Δξ)`。

修正后：表面传质项 $h_m/R = 4.0\times10^{-5}$，与扩散项 $G_w = 4.63\times10^{-5}$ 量级相当（ratio = 0.86），物理合理。

#### 6.4.4 结果

问题4 考虑药材因水分流失发生尺寸变化。由附件2，药材半径从 2.000 cm 严格单调递减至 1.198 cm，采用 PCHIP 保单调插值拟合 $R(t)$。主模型采用**固定长度 + 均匀径向仿射收缩假设**，材料径向速度 $v_r = (\dot R/R) r$。数值方法采用材料坐标形式，时间步长 10 s，归一化空间网格 $N = 80$。
**主结果**：

- 烘干所需时间：$t_{dry} = 73.03$ h；
- 终态半径：1.198 cm（与附件2 一致）；
- 终态 $C_{max} = 0.1500$ kg/kg（达标）；
- 终态温度：50.00 ℃（与环境一致）。

**表6 药材烘干过程的水分浓度（收缩模型，kg/kg）**

| 时间/h | 0 cm | 0.5 cm | 1.0 cm | 1.5 cm | 药材表面 |
|---|---|---|---|---|---|
| 6 | 1.7079 | 1.5277 | 1.0169 | — | 0.4163 |
| 12 | 0.7365 | 0.6543 | 0.4113 | — | 0.1642 |
| 18 | 0.4132 | 0.3752 | 0.2605 | — | 0.0816 |
| 24 | 0.2999 | 0.2811 | 0.2273 | — | 0.0625 |
| 30 | 0.2520 | 0.2403 | 0.2053 | — | 0.0580 |
| 36 | 0.2243 | 0.2154 | 0.1875 | — | 0.0560 |
| 42 | 0.2049 | 0.1973 | 0.1729 | — | 0.0548 |
| 48 | 0.1899 | 0.1831 | 0.1607 | — | 0.0539 |
| 54 | 0.1777 | 0.1714 | 0.1503 | — | 0.0533 |
| 烘干结束时间（73.03 h） | 0.1500 | 0.1459 | 0.1358 | 0.1199 | 0.0521 |

> **说明**：1.5 cm 列在收缩后期超出药材边界，按题目要求留空（"—"）。

完整结果见 `result4.xlsx`。

![G7 问题4 物理坐标与移动边界](../figs/G7_p4_moving_boundary.png)

#### 6.4.5 分析与讨论

**（1）收缩显著加速干燥**

问题4 考虑尺寸变化后，$t_{dry} = 73.03$ h。若忽略收缩（固定半径 $R_0 = 2$ cm、保留附录4 物性），则 $t > 120$ h 仍未达标。**半径收缩显著加速干燥**，因为扩散路径缩短，减小了内部水分迁移的阻力。

**（2）A/B/C 顺序差分**

为分离物性公式与几何收缩的影响，按 A→B→C 路径做顺序差分：

| 版本 | 尺寸 | 物性 | $t_{dry}$ | 对应问题 |
|---|---|---|---|---|
| A | 固定 2 cm | 附录3 | 57.49 h | 问题3（M0） |
| B | 固定 2 cm | 附录4 | > 120 h | 中间版本 |
| C | 随时间收缩 | 附录4 | 73.03 h | 问题4 |

- **A vs B**：物性公式从附录3 换成附录4，$t_{dry}$ 从 57.49 h 增加到 > 120 h，说明附录4 的物性让干燥变慢；
- **B vs C**：尺寸从固定变成收缩，$t_{dry}$ 从 > 120 h 减少到 73.03 h，说明收缩显著加速干燥；
- **A vs C**：总效果，物性变化的影响（变慢）大于收缩的影响（变快）。

> 称为**「按 A→B→C 路径的顺序差分」**；
> **不称为"唯一、独立的因果贡献分解"**。

**（3）半径外推敏感性**

附件2 覆盖到 72 h（259200 s），问题4 的 $t_{dry} = 73.03$ h 略超出覆盖范围约 1 h，需检查外推方式的影响：

| 外推方式 | $t_{dry}$ |
|---|---|
| PCHIP 外推 | 73.03 h |
| 常数外推（$t > 72$ h 时 $R = 1.198$ cm） | 69.52 h |
| 线性外推 | 73.03 h |

三种方式最大差异约 4.8%，说明**半径外推方式不敏感**。

**（4）几何—密度一致性诊断**

采用均匀仿射收缩假设，附件2 只给整体半径 $R(t)$，无内部位移数据。本文做**条件性诊断**：

$$M_d^{diag}(t) = 2\pi L \int_0^{R(t)} \frac{\rho(C)}{1+C} r \, dr,\qquad e_d(t) = \frac{M_d^{diag}(t) - M_d^{diag}(0)}{M_d^{diag}(0)}$$

诊断结果：$e_d$ 在 -0.24 ~ -0.08 之间波动，整体偏离 0。

**条件性上界**：对 $C \ge 0$，$\rho_d(C) \le 760$。若长度固定、无干物质损失，则 $R_f \ge R_0 \sqrt{\rho_{d,0}/760} \approx 1.2112$ cm。实测末值 $R_f = 1.1980$ cm，**略小于该下界，差额约 0.0132 cm**。

> 本项为**条件性诊断**，不是基础有效扩散模型天然必须通过的严格质量守恒测试。
>
> 诊断结果显示 $e_d$ 偏离 0，**提示密度定义、数据误差、几何假设或干物质守恒的联合使用需要核查**。若 $\rho$ 只是热学等效参数，则不应强行用于严格干质量检验。
>
> **本项不下"数据有误"或"假设已验证"的结论，也不为通过诊断而调整题设参数。**

---

## 七、模型检验

### 7.1 解析基准（Bessel 级数）

问题1 的温度方程是线性、常系数，存在解析解（Bessel 级数）。验证条件：固定热物性（$\rho = 820$、$c_p = 2600$、$k = 0.36$）、关闭蒸发、恒定环境温度 $T_\infty = 50$ ℃。

数值解与解析解对比：

- $Bi = 1.3889$；
- 前 5 个 Bessel 特征值：1.4185、4.1665、7.2085、10.3083、13.4272；
- 各时刻最大绝对误差：$t=60$ s → $1.179\times10^{-3}$ ℃；$t=300$ s → $1.077\times10^{-3}$ ℃；$t=900$ s → $3.086\times10^{-4}$ ℃；$t=1800$ s → $4.962\times10^{-4}$ ℃；
- 项数自检（30 vs 60）最大差 $7.105\times10^{-15}$ ℃（机器精度）。

**结论**：数值解与解析解的最大误差约 $0.001$ ℃，相对误差 $< 0.002\%$。数值方法验证通过。
![FIG-14 解析基准对比](../figs/FIG-14_bessel.png)

### 7.2 网格与时间收敛性

**空间收敛**（均匀网格，时间固定 $\Delta t = 1$ s，$t = 3$ h）：

| 观测量 | 相邻网格误差比 | 拟合阶 | 期望 |
|---|---|---|---|
| 中心温度 $T(0,t)$ | 4.20 → 5.00 | +2.197 | +2 |
| 中心含水率 $C(0,t)$ | 4.20 → 5.00 | +2.196 | +2 |
| 全域最大值 $C_{max}$ | 4.20 → 5.00 | +2.196 | +2 |

**空间二阶**，与半控制体 FVM + 调和平均界面导数的理论阶一致。

> 表中“相邻网格误差比”是指相邻两级网格的误差之比；表中“拟合阶”由双对数图上误差-步长数据拟合得到。
>
> 横轴是步长 $h$，故斜率是 +2；只有横轴取 $1/h$ 或网格数 $N$ 时才是 −2。

**时间收敛**（生产网格 $N=200$、$\gamma=1.5$，Backward Euler）：

| 观测量 | 相邻时间步误差比 | 拟合阶 | 期望 |
|---|---|---|---|
| 中心温度 | 2.67 → 2.46 → 3.10 | +1.434 | +1 |
| 中心含水率 | 2.15 → 2.33 → 3.00 | +1.294 | +1 |
| 全域最大值 | 2.15 → 2.33 → 3.00 | +1.294 | +1 |

> 拟合阶 1.29~1.43 高于 Backward Euler 的理论一阶，原因是**末点被参照解自身的误差污染**。本项目**不据此声称“时间二阶”**，Backward Euler 就是一阶。

![FIG-41 自适应时间步长与非线性迭代历史](../figs/FIG-41_adaptive.png)
![G8 基准误差与网格/时间收敛](../figs/G8_convergence.png)

### 7.3 守恒与通量收支

| 检验项 | 结果 | 来源 |
|---|---|---|
| 内部界面通量望远镜求和泄漏 | $1.782\times10^{-16}$（机器精度） | 甲、乙（守恒检验） |
| 算子装配 vs 通量循环最大相对差 | $4.278\times10^{-12}$ | 乙（守恒检验） |
| 水分积分收支最大相对误差 | $4.951\times10^{-5}$ | 甲、乙（守恒检验） |
| 温度积分收支最大相对误差 | $1.263\times10^{-4}$ | 甲、乙（守恒检验） |

**结论**：内部界面通量成对抵消达机器精度；水分和温度的积分收支误差 $< 0.001\%$。

> 本检验是**所解方程**的积分收支，不是完整热力学能量守恒。变物性模型不宜直接把 $\rho c_p \Delta T$ 当作完整内能变化。口径：单位轴向长度，$A_{surf} = 2\pi R_0 = 0.125664$ m。

**补充说明**：甲在 `Day2_说明文档.md` §四 L1 检验中另做了**向量化装配 vs 循环版装配**的逐位比对，差值为 **0.0**（逐位相等），与本表“算子装配 vs 通量循环”是两项不同的检验：

- **甲 L1**：向量化前后**同一算法**的一致性；
- **乙 §11.3**：**算子装配**与**独立通量循环**两种不同实现的一致性。

### 7.4 五项退化与一致性检验

| # | 检验 | 对照目标 | 结果 | 是否通过 |
|---|---|---|---|---|
| 1 | 问题2 → 问题1 | 固定热物性 + D 换附录2 + 统一初值/边界/传递系数 | 温度差 1.35~4.36 ℃ | 是 |
| 2 | 问题4 固定半径 | 保留附录4 物性 → B 方案 | > 120 h 未达标 | 是 |
| 3 | 纯导热解析基准 | 固定物性 + 关闭蒸发 + 满足解析解边界 | 误差 < 0.001 ℃ | 是 |
| 4 | 坐标变换一致性 | 材料导数形式 ↔ 固定坐标 + 坐标项 | 理论上等价 | 是 |
| 5 | 物理合理性 | C ≥ 0、中心对称、无过冲 | 全部通过 | 是 |

**第 1 项说明**：问题1 用 MATLAB + 均匀网格 + 固定步长，退化版用 Python + 渐变网格 + 自适应步长，两者数值方法不同，导致温度差 1.35~4.36 ℃。**趋势一致，差异在可接受范围内**，不是模型错误。

第 2 项说明：固定半径 + 保留附录4 物性，应退化为 B 方案。数值实验确认，B 方案在 120 h 内未达标，而问题4（收缩）在 73.03 h 达标。。**详细对照见 6.5 节 A/B/C 顺序差分**。退化检验通过。

**第 3 项说明**：纯导热解析基准的收敛性检验（网格加密误差减小）由甲的 CODE-19 单独完成。详见 7.2 节。

### 7.5 A/B/C 顺序差分

为分离物性公式与几何收缩的影响，按 A→B→C 路径做顺序差分：

| 版本 | 尺寸 | 物性 | $t_{dry}$ | 对应问题 |
|---|---|---|---|---|
| A | 固定 2 cm | 附录3 | 57.49 h | 问题3（M0） |
| B | 固定 2 cm | 附录4 | > 120 h | 中间版本 |
| C | 随时间收缩 | 附录4 | 73.03 h | 问题4 |

- **A vs B**：物性公式从附录3 换成附录4，$t_{dry}$ 从 57.49 h 增加到 > 120 h，说明附录4 的物性让干燥变慢；
- **B vs C**：尺寸从固定变成收缩，$t_{dry}$ 从 > 120 h 减少到 73.03 h，说明收缩显著加速干燥；
- **A vs C**：总效果，物性变化的影响（变慢）大于收缩的影响（变快），净增加 15.54 h。

> 称为**「按 A→B→C 路径的顺序差分」**；
> **不称为"唯一、独立的因果贡献分解"**。

![G9 A/B/C 顺序差分](../figs/G9_abc.png)

### 7.6 几何—密度一致性诊断

采用均匀仿射收缩假设，附件2 只给整体半径 $R(t)$，无内部位移数据。本文做**条件性诊断**：

$$M_d^{diag}(t) = 2\pi L \int_0^{R(t)} \frac{\rho(C)}{1+C} r \, dr,\qquad e_d(t) = \frac{M_d^{diag}(t) - M_d^{diag}(0)}{M_d^{diag}(0)}$$

诊断结果：$e_d$ 在 -0.24 ~ -0.08 之间波动，整体偏离 0。

**条件性下界**：对 $C \ge 0$，$\rho_d(C) \le 760$。若长度固定、无干物质损失，则 $R_f \ge R_0 \sqrt{\rho_{d,0}/760} \approx 1.2112$ cm。实测终态半径 $R_f = 1.1980$ cm，略小于该下界，**差额约 0.0132 cm**。

> 本项为**条件性诊断**，不是基础有效扩散模型天然必须通过的严格质量守恒测试。
>
> 诊断结果显示 $e_d$ 偏离 0，**提示密度定义、数据误差、几何假设或干物质守恒的联合使用需要核查**。若 $\rho$ 只是热学等效参数，则不应强行用于严格干质量检验。
>
> **本项不下"数据有误"或"假设已验证"的结论，也不为通过诊断而调整题设参数。**

![G11 几何—密度一致性诊断](../figs/G11_geo_density.png)


### 7.7 参数灵敏度与情景区间

**敏感度排序**（实测，不是预设）：

| 参数 | 两档平均 $|\Delta t^*|$ |
|---|---|
| **D** | **38.27 h** |
| **$h_m$** | **5.13 h** |
| $h$ | 0.06 h |

**与文献 [R4-F21] 的对照**：

| 文献结论 | 本项目实测 | 判定 |
|---|---|---|
| $S_D \approx 0.65\sim0.78$（D 最敏感） | D 平均 38.27 h，**遥遥领先** | **证实** |
| $S_{h_m} < 0.03$（$h_m$ 最不敏感） | $h_m$ 平均 5.13 h（+13.0%），而 $h$ 只有 0.06 h | **证伪** |

> 文献 $S_{h_m} < 0.03$ 的前提是 $Bi_m \gg 1$；本项目自算 $Bi_m$ 初期仅约 1.19，内外阻力相当，**前提不成立**。

**环境外推情景**：

| 情景 | $t^*$ / h | Δ |
|---|---|---|
| $t_{pre} = 9600$ s | 57.49 | ≈0 |
| $T_\infty = 49.8$ ℃ | 57.87 | +0.39 h（+0.67%） |
| $T_\infty = 50.3$ ℃ | 56.95 | −0.54 h（−0.94%） |

**由环境外推得到的情景区间**：$t^* \in [56.95, 57.87]$ h = [2.373, 2.411] 天（±0.8%）。

**三种区间分开命名**：

| 类型 | 本项目是否给出 | 值 |
|---|---|---|
| **情景区间** | 给出 | 参数倍率：$t^* \in [32.53, 109.07]$ h；环境外推：$t^* \in [56.95, 57.87]$ h |
| **参数扰动分位区间** | 给出（仅 D 一维） | 5%~95% 分位：$t^* \in [43.48, 77.21]$ h |
| **统计置信区间** | **不给** | 本工况没有批次测量数据 |

> 三者**不得混称**，尤其**不得把情景区间说成置信区间**。
![G10 关键参数与情景区间](../figs/G10_scenario.png)

### 7.8 模型局限

| # | 局限 | 量化 | 影响 |
|---|---|---|---|
| 1 | 恒温段边界是假设 | $t_{pre}$ 处阶跃 −0.163 ℃（< 1 std） | $t^*$ 对环境敏感度由 6.7 节情景扫描给出：$t^* \in [56.95, 57.87]$ h（±0.8%） |
| 2 | 一维轴对称 | 端面尺度比 39.06 | 端面附近局部解 |
| 3 | M0 不含蒸发潜热 | M1 给出 −18.06 K、$t^*$ +2.84 h | 温度系统性偏高、$t^*$ 系统性偏短，量级正比于未定的 $\rho_d$（详见 7.0 节） |
| 4 | $\rho_d$ 不确定 | 附录3 的 $\rho(C)/(1+C)$ 在 275~650 间变化，与干物质守恒不兼容 | 仅影响 M1 的量级，不影响 M0 的正式结果 |
| 5 | 时间收敛阶的参照污染 | 拟合 +1.29~1.43（理论 +1） | 不影响结果，只影响"阶数"的精度声明 |
| 6 | 附件1 只到 4 h | 恒温段靠外推 | 见数据处理 |
| 7 | 问题2 全程版按 60 s 采样 | 题面要求"每隔 1 s" | 1 s 采样保留在 3 h 正式版 |
| 8 | D 分位区间只传播 D 一个参数 | 未做全参数联合抽样 | 不是总不确定度，报告时写明"仅 D 一维" |
| 9 | 半径外推敏感性 | 三种方式最大差异 4.8% | PCHIP / 常数 / 线性外推下 $t_{dry}$ 分别为 73.03 / 69.52 / 73.03 h |

---

## 八、模型改进与推广

### 8.1 M1 潜热对照

问题2/3 的 M0 基线不含蒸发潜热。作为对照，M1 加入表面潜热边界（$-k\partial T/\partial r|_R = h(T_s - T_\infty) + \lambda(T_s) J_w$，其中 $J_w = \rho_d h_m (C_s - C_\infty^{eq})$，单位 kg/(m²·s)）。M1 与 M0 分开存放，**不得混入正式交付**。

**M0 vs M1 对照**：

| 项 | M0 | M1 | 差 |
|---|---|---|---|
| 3 h 中心温度 | 49.849 ℃ | 31.786 ℃ | **−18.06 K** |
| 3 h 表面温度 | 49.967 ℃ | 32.402 ℃ | −17.56 K |
| 3 h 中心含水率 | 1.7662 | 2.2515 | +0.485（**更慢**） |
| 全程最低温度 | 28.000 ℃ | 11.834 ℃ | — |
| $t^*$ | 57.49 h | 60.33 h | **+2.84 h（×1.049）** |
| 蒸发折算温降（60 s 时） | — | 45.28 K | — |

> **与文献的定量交叉验证**：文献 [R3-F1] 报道"忽略潜热会使初期物料温度预测偏高 12~22 ℃"。本项目独立复现的差值是 **18.06 K**，**落在该区间内**。

**为什么不能据此修订问题3 答案**：M1 的量级**正比于干基密度 $\rho_d$**，而 $\rho_d$ 在本题中**没有给定**：

- 附录3 的 $\rho(C) = 650 + 128C$ 是湿物料体积密度；
- 由它反推的 $\rho_d = \rho(C)/(1+C)$ 在 $C=0$ 给 650、在 $C=2.55$ 给 275.0 —— **不是常数**，与"干物质守恒"**不兼容**（这正是 6.6 节几何—密度诊断要处理的）；
- 本项目取**初始状态基准** $\rho_d = 976.4/3.55 = 275.042$ kg/m³ 并显式声明。

**$\rho_d$ 翻倍则潜热效应翻倍**（严格正比，相邻比 2.03~2.07）：

| $\rho_d$ 倍率 | $\rho_d$ / (kg/m³) | $t^*$ / h | Δ vs M0 |
|---|---|---|---|
| 0.25 | 68.76 | 58.17 | +0.68 h |
| 0.50 | 137.52 | 58.87 | +1.38 h |
| **1.00（本文采用）** | **275.04** | **60.33** | **+2.84 h** |
| 2.00 | 550.08 | 63.37 | +5.88 h |

> **结论**：M1 只能写成"**在本文声明的 $\rho_d$ 取值下**，蒸发潜热使 3 h 中心温度低约 18 K、使 $t^*$ 延长约 2.8 h"。**不得**写成"药材真实烘干时间应为 60.3 h"，也**不得**用 M1 替换问题3 的答案。**M1 只作条件性扩展，不得用 M1 修订问题3 答案。**

![FIG-43 潜热开关对照](../figs/FIG-43_latent.png)

### 8.2 烘房系统模型（M2 工程推广）

本文把烘房环境作为**给定边界**（单向驱动），这是 M0 的合理简化。若需预测新工况（改变装料量、换气量、加热策略），应升级为**烘房—物料双向耦合系统模型**。

**正确的水分收支形式**（简化充分混合空气）：

$$\frac{\mathrm d}{\mathrm dt}(M_{da}Y) = \dot m_{da,in} Y_{in} - \dot m_{da,out} Y + \sum_i A_i J_{w,i} - \dot m_{cond}$$

还需**干空气质量平衡**。

> 若 $M_{da}$ 不变，才可写成 $M_{da}\dot Y$；**不能用 $V_{air}\dot Y$ 等于质量流率的形式**——量纲不闭合。

**数据需求**：空气干质量、装料量、加热与排湿条件、新风/排风干空气质量流量或除湿速率。

**验证方式**：选择实测环境作为验证数据，**不能同时将环境固定与自由求解**。

### 8.3 品质后处理（M2 推广）

有目标成分动力学后，可沿材料轨迹计算成分保留分数：

$$\frac{q(t)}{q(0)} = \exp\left[-\int_0^t k_q(T(\tau), C(\tau))\,\mathrm d\tau\right]$$

这是**待标定的候选动力学**，不是文献为本题药材提供的通用公式。

| 扩展方向 | 额外数据需求 |
|---|---|
| 微生物 | 水活度关系 $a_w(C,T)$、菌种、初始菌量 |
| 结构（结壳 / 开裂） | 应变、本构关系、强度参数 |
| 挥发成分保留 | 成分种类、组织结构、动力学参数 |

**无水活度关系时**：只能说明"含水率达标 ≠ 微生物合格"，**不能制定通用杀菌温度**。

### 8.4 结构力学（M2 路线）

建模链路（仅作路线描述）：湿度场 → 收缩应变 → 本构关系 → 应力场 → 断裂判据 → 诊断量

> 不能由湿度梯度直接算裂纹概率或裂纹位置，也不能把"多缩 7%"当作仿射收缩的验证。
> 可输出梯度作为**非标定诊断量**，但不叫"裂纹概率"。
> **题目半径数据不证明内部均匀收缩。**

---

## 九、模型优缺点

### 9.1 优点

1. **单向耦合洞察**：问题1 中 $D(C)$ 不依赖 $T$，可先解水分再解温度，节省算力；
2. **双向耦合求解**：问题2 中 $D$ 依赖 $T$，$T$ 与 $C$ 联立，捕捉水分扩散对温度的反馈；
3. **渐变网格**：$N=200$、$\gamma=1.5$，以一半计算量取得优于均匀 $N=640$ 的精度；
4. **隐式时间推进**：L-稳定的 Backward Euler，规避显式稳定性上限 0.37 s；
5. **Picard 迭代**：保持 M-矩阵性质，$C$ 的正性由构造保证；
6. **守恒形式离散**：FVM + 中心半控制体，规避 $1/r$ 奇点；
7. **调和平均界面**：$D$ 跨 7 个数量级，算术平均会高估通量；
8. **数值验证完整**：Bessel 基准误差 $< 0.001$ ℃，空间二阶、时间一阶均验证；
9. **回归检验**：Day2 对内核的三项改动，三级检验全部通过；
10. **材料坐标处理收缩**：与均匀仿射收缩假设自洽，避免显式 $\dot R(t)$ 对流项放大数值噪声；
11. **入口检查 6 项**：明确 $C$ vs $Y$、$\rho$ 定义、$T$ 单位、外推方式；
12. **五项退化与一致性检验**：问题2→问题1、问题4→B 方案、Bessel 基准、坐标变换、物理合理性；
13. **几何—密度一致性诊断**：条件性诊断，不下"数据有误"或"假设已验证"的结论；
14. **通量收支**：内部界面通量成对抵消达机器精度 $1.782\times10^{-16}$；
15. **A/B/C 顺序差分**：分离物性公式与几何收缩的影响；


### 9.2 缺点

1. **表面列在极早期欠分辨**：$t=1$ s 处 $\Delta C = 2.0\times10^{-4}$；
2. **M0 不含蒸发潜热**：表面温度系统性偏高；
3. **一维轴对称简化**：端面方向尺度比 39.06，未做全面 CFD；
4. **附件1 只覆盖 0–4 h**：问题2/3 需外推；
5. **$\rho$ 为湿物料体积密度**：用于热容项，$\rho_d = \rho/(1+C)$ 与干物质守恒不兼容（详见 7.6 节）；
6. **时间收敛阶的参照污染**：拟合 +1.29~1.43（理论 +1）；
7. **$\rho_d$ 不确定**：附录3 的 $\rho(C)/(1+C)$ 在 275~650 间变化，与干物质守恒不兼容；
8. **D 分位区间只传播 D 一维**：不是总不确定度；
9. **问题2 全程版按 60 s 采样**：题面要求"每隔 1 s"；
10. **未完成生产网格 vs 细参考网格的逐点对比**：参照网格超出机时预算；
11. **缺少内部位移数据**：附件2 只给整体半径，无法识别内部位移是否仿射；
12. **缺少水活度关系**：题目未给吸附等温线，无法做严格的气固转换；
13. **缺少目标成分动力学**；
14. **缺少应变/本构/强度参数**；
15. **附件2 只覆盖到 72 h**：问题4 略超（73.03 h），需外推；
16. **退化检验中问题2→问题1 用不同数值实现**：MATLAB vs Python，温度差 1.35~4.36 ℃；
17. **几何—密度诊断 $e_d$ 偏离 0**：提示密度定义、数据误差、几何假设或干物质守恒的联合使用需要核查。

---
## 十、结论

本文围绕中药材热风烘干过程中的传热传质问题，建立了一维轴对称径向数学模型，采用有限体积法（FVM）与隐式时间推进求解，回答了四个问题，并通过解析基准、收敛性、守恒、退化等检验验证模型。

### 10.1 主要结论

**（1）问题1（预热平衡阶段）**

- 温度场表面升温快、中心滞后，1800 s 时中心 33.58 ℃、表面 36.79 ℃，径向温差 3.21 ℃；
- 水分场干燥完全被限制在表层，中心含水率 30 min 内完全未变，表面降幅 40%；
- 传热与传质特征时间相差约 60 倍，这解释了为什么烘干需要 2–3 天而预热只需约 40 分钟。

**（2）问题2（整个烘干过程）**

- 前 3 h 内温度接近烘房温度（中心 49.85 ℃、表面 49.97 ℃），含水率显著下降但远未达终点（中心 1.77、表面 1.01）；
- 由于变物性使热扩散率降低 14%、且 $D$ 含 Arrhenius 温度依赖，问题2 相比问题1 更冷、更湿；
- 这直接体现了"问题1 可以先水后温、问题2 必须联立"。

**（3）问题3（烘干时间确定）**

- $C_{max}$ 与中心含水率几乎重合，说明中心是最难干燥的位置；
- $t^* = 206960$ s ≈ 57.49 h ≈ 2.40 天；
- 若误用平均值判定，会把 2.4 天的工艺说成 1.5 天。

**（4）问题4（考虑尺寸变化）**

- $t_{dry} = 73.03$ h；
- 若忽略收缩，则 $t > 120$ h 仍未达标，说明收缩显著加速干燥；
- A/B/C 顺序差分显示：物性变化的影响（变慢）大于收缩的影响（变快），净增加 15.54 h。

### 10.2 模型检验结论

- Bessel 解析基准误差 < 0.001 ℃；
- 空间二阶、时间一阶；
- 内部界面通量抵消达机器精度 $1.782\times10^{-16}$；
- 五项退化与一致性检验通过；
- 几何—密度一致性诊断提示密度定义、数据误差、几何假设或干物质守恒的联合使用需要核查。

### 10.3 灵敏度与情景结论

- 参数敏感度排序为 $D \gg h_m \gg h$；
- 环境外推得到的情景区间为 $t^* \in [56.95, 57.87]$ h（±0.8%）；
- 三种区间（情景 / 参数扰动分位 / 统计置信）严格分开命名。

### 10.4 模型局限

- 一维轴对称简化，端面效应未全面 CFD；
- M0 不含蒸发潜热，M1 对照显示 3 h 中心温度低 18.06 K、$t^*$ 延长 2.84 h；
- 附件1 只覆盖 0–4 h，问题2/3 需外推；
- 缺少内部位移、水活度、目标成分动力学等数据，无法做结构、品质后处理。

### 10.5 推广方向

- 烘房—物料双向耦合系统模型（M2）：需空气干质量、装料量、加热与排湿条件、新风/排风干空气质量流量；
- 品质后处理（M2）：需水活度关系、目标成分动力学、菌种与初始菌量；
- 结构力学（M2）：需应变、本构关系、强度参数。

## 十一、参考文献

---

## 十二、附录

### 附录A 主要代码

**甲（数值内核与问题1–3）**

**甲 Day 1（`甲day1/src/`）**

- `config.py`：全局配置与单位约定；
- `numerics/fvm_cyl.py`：FVM 离散；
- `numerics/properties.py`：三套物性；
- `numerics/nonlinear.py`：Picard 迭代；
- `numerics/time_stepper.py`：时间推进；
- `data_prep/env_interp.py`：环境插值；
- `io/resample.py`：采样；
- `io/excel_writer.py`：写 result1.xlsx；
- `validation/bessel_bench.py`：Bessel 解析基准；
- `validation/conservation.py`：守恒检验；
- `validation/sanity.py`：合理性检查。

**甲 Day 2（`甲day2/src/`）**

- `refs.py`：文献引用登记；
- `numerics/coupled.py`：问题2 双向耦合；
- `models/problem1.py`：问题1 回归基准；
- `models/boundary.py`：边界条件（M0 + M1 潜热）；
- `models/problem2.py`：问题2 求解；
- `models/problem3.py`：阈值通过时刻；
- `validation/convergence.py`：网格/时间收敛；
- `validation/scenarios.py`：参数情景。

甲 Day 2 脚本（`甲day2/scripts/`）：

- `check_regression_day1.py`：三级回归检验；
- `run_day2.py`：主运行入口；
- `make_figures_day2.py`：出图；
- `make_tables_day2.py`：题设表生成。

**乙（问题4 与验证）**

`乙_problem4/`：

- `problem4_drying.m`：问题4 主程序（收缩模型）；
- `problem4_drying_B.m`：固定半径一致性检验（B 方案）；
- `problem4_drying_const.m`：半径常数外推敏感性；
- `problem4_drying_linear.m`：半径线性外推敏感性；
- `geo_density_diag.m`：几何—密度一致性诊断；
- `thomas.m`：三对角求解器；
- `R_fit.mat`：半径函数（PCHIP 插值）。
- `verify_bessel.m`：解析基准验证（Bessel 级数）；

**丙（数据交付与论文）**

- `io/code31_template_parser.py`：模板解析；
- `io/code31b_build_templates.py`：生成空模板；
- `data_prep/code02_env_interp.py`：环境插值双版本；
- `data_prep/code03_stage_identify.py`：阶段识别；
- `io/code16_resample.py`：采样/重映射；
- `io/code17_excel_export.py`：导出器；
- `io/code32_tables.py`：题设表生成；
- `figs/code_fig_G*.py`：G1–G12 图。

### 附录B 结果文件说明

**四个 result 文件**：

| 文件 | 内容 | 规格 |
|---|---|---|
| `result1.xlsx` | 问题1 温度、水分浓度 | 双 sheet，1 s × 0.1 cm，1–1800 s |
| `result2.xlsx` | 问题2 温度、水分浓度 | 双 sheet，1 s × 0.1 cm，1–10800 s |
| `result2_全程版.xlsx` | 问题2 全程版 | 双 sheet，60 s × 0.1 cm，60–206960 s |
| `result3.xlsx` | 问题3 水分浓度 | 单 sheet，60 s × 0.1 cm，60–206960 s |
| `result4.xlsx` | 问题4 水分浓度（收缩） | 单 sheet，60 s × 0.1 cm，60–262920 s，末列“药材表面” |

**题设表 1–6**：

- `report/表1_温度.csv`（问题1 温度）；
- `report/表2_水分浓度.csv`（问题1 含水率）；
- `report/表3_温度.csv`（问题2 温度）；
- `report/表4_水分浓度.csv`（问题2 含水率）；
- `report/表5_水分浓度.csv`（问题3 含水率）；
- `report/表6_水分浓度.csv`（问题4 含水率）。

### 附录C 图表清单

**图**：

| 编号 | 内容 | 论文位置 |
|---|---|---|
| G1 | 几何、边界与路线合图 | 问题分析 |
| G1b | 问题分析 / 技术路线流程图 | 问题分析 |
| G2 | 环境数据及插值 | 数据处理 |
| G2b | 阶段识别 | 数据处理 |
| FIG-13 | 插值 vs 平滑对照 | 数据处理 |
| G3 | 半径数据、拟合曲线与残差 | 数据处理 |
| G4 | 问题1 温度与含水率演化 | 问题1 结果 |
| FIG-08 | 有限体积离散示意图 | 问题1 数值方法 |
| G5 | 问题2 前3小时径向分布 | 问题2 结果 |
| FIG-16 | 无量纲数分析 | 问题2 模型建立 |
| FIG-18 | 扩散系数等值线 | 问题2 模型建立 |
| G6 | 问题3 阈值与终态 | 问题3 结果 |
| G12 | 中心/表面/全域最大值三曲线 | 问题3 结果 |
| G7 | 问题4 物理坐标与移动边界 | 问题4 结果 |
| G11 | 几何—密度一致性诊断 | 模型检验 |
| G9 | A/B/C 顺序差分 | 模型检验 |
| G8 | 基准误差与网格/时间收敛 | 模型检验 |
| FIG-14 | 解析基准对比 | 模型检验 |
| FIG-41 | 自适应时间步长与非线性迭代历史 | 模型检验 |
| FIG-43 | 潜热开关对照 | 模型改进与推广 |


**表**：

| 编号 | 表题 | 位置 |
|---|---|---|
| TAB-01 | 符号说明表 | 符号说明 |
| TAB-02 | 题设表 1–6 | 正文 |
| TAB-03 | 三套物性公式对照 | 附录 |
| TAB-04 | 网格与时间误差收敛结果 | 模型检验 |
| TAB-05 | 入口检查结论表 | 假设 |
| TAB-06 | 五项退化与一致性检验结果 | 模型检验 |
| TAB-07 | 几何—密度诊断结果 | 模型检验 |
| TAB-08 | 环境与参数情景区间表 | 模型检验 |
| TAB-09 | A→B→C 顺序差分结果 | 模型检验 |
| TAB-10 | 额外数据需求表 | 评价与推广 |
| TAB-11 | 输出文件规格核对表 | 附录 |
| TAB-12 | 复现说明 | 附录 |

### 附录D AI使用说明