# 2026 CUMCM A 题「药材的烘干问题」求解框架（修订版 v2）

> **修订依据**：《A题_修订思路_21因素审查与文献匹配.md》。凡与该审查冲突之处，**一律以审查意见为准**。
>
> **本文定位**：给出四问的完整建模思路、方程、参数、数值路线、检验方案与图表清单。
> **只差代码和图**——所有方程可直接翻译成程序，所有图给出应画什么。
>
> **数据状态**：附件1—3 已实际读取核实（见 §1），本文所引数值均出自实测，非转述。

---

## 0. 修订摘要（v1 → v2）

### 0.1 删除的错误论断（不得进入论文）

| # | v1 的表述 | 问题 | v2 处置 |
|---|---|---|---|
| 1 | "50 °C、0.05 kg/kg 对应**湿球温度约 40.5 °C**，药材表面干燥前期约 40 °C" | **方法错误**。给定压力下寻找满足 $Y_{sat}(T)=Y$ 的温度，得到的是**露点**，不是湿球。湿球须满足绝热加湿的能量关系 | **整段删除**。只在计算完成后讨论表面温度的实际走势 |
| 2 | "**Luikov 数 Lu ≪ 1** 即可忽略 Soret/Dufour 效应" | **判据错误**。Lu 通常表征扩散率比，不能单独判定交叉项大小 | 改为：**须比较交叉通量与主通量的比值**，本题未做该比较，故只声明缺系数、不宣称已证明可忽略 |
| 3 | "**50 °C 杀不死虫卵，需 55–60 °C**" | 通用阈值无依据，热处理效应须对应虫种、虫态与暴露时间 | 删除。因素17 降为**非必写**，若写只用一句话说明适用边界 |
| 4 | "霉菌最适 25–30 °C，**物料 28 °C 起步即危险**" | 未计算、未标定的风险时间表 | **删除风险表**。改为：干基含水率须经吸附关系转为**水活度**才能连接微生物模型；仅说明"含水率达标 ≠ 微生物合格" |
| 5 | "**烘干本质上等于不受控的水蒸气蒸馏**" | 表述过强，且该方向非中药材独有 | 改为：**干燥方式、温度与挥发成分的分泌结构会影响保留率**；仅对特定芳香药材有价值，本题未指明物种 |
| 6 | "**多缩 7%** 验证了仿射收缩假设基本站得住" | **双重错误**：①单个表面半径**不能识别内部位移是否仿射**；②该差额与干物质守恒存在**方向性矛盾**（见 §7.3） | 改为**「几何—密度一致性诊断」**，报告 $e_d(t)$，并列出三个未决问题 |
| 7 | "所有输出**都应给置信区间**" | 三种区间性质不同，混用会误导 | 分开命名：**情景区间**（人为设定）/ **参数扰动分位区间**（数值传播）/ **统计置信区间**（需实测分布） |

### 0.2 修正的建模判断

| 项 | v1 | v2 |
|---|---|---|
| **潜热项** | "四问都必须含 λj" | **M0 不含**（按题设）；**M1 在通量闭合后**做对照。⚠️ **不能把 $h_m(C_s-C_\infty)$ 直接乘潜热当热通量**——它单位是 kg/(kg·s) 不是 kg/(m²·s) |
| **收缩方程** | 主模型含 Ṙ 对流项 | 主模型用**材料导数**形式（无对流项，见 §7.2），这是与均匀仿射收缩假设**自洽**的形式 |
| **密度基准** | `rho_basis` 可切换 | **不可切换**。明确 ρ 用于热容/导热，$\rho_d=\rho/(1+C)$ 用于几何—密度诊断 |
| **一维简化依据** | "长径比 12.5 ≫ 1，端面影响 < 2–3%" | **删除"< 2–3%"**。改为尺度分析 + 二维对照。⚠️ 端面尺度须用**半长 $L/2$**，$t_z/t_r\sim39.06$ |
| **附件1 尾部抖动** | "像 PD 控制器超调" | **不推断控制器类型**。实测 241 点中温度差分变号 **116 次**、水分 **119 次**，抖动遍布全程而非仅尾部 |
| **"2–3 天"** | 作为结果合理性判据 | **仅为题设背景描述**，不作为强制范围，**不为了落入该范围改参数** |
| **问题2 输出时长** | 推荐 3 h | 保留该判断，但须**先核实模板**再定（见 §5.5） |

### 0.3 新增的内容

- **模型三层结构 M0/M1/M2**（§2.1），明确哪些必做、哪些是扩展、哪些只给路线
- **变量 C 与 Y 的严格区分**（§2.2）——这是审查点出的第一顺位修正
- **几何—密度一致性诊断**（§7.3），含条件性上界检查
- **潜热的通量闭合前置条件**（§9.1）
- **烘房系统模型的正确收支形式**（§10.1）
- **文献落位表**（§11.3）

---

## 1. 数据核实结果（实测）

### 1.1 附件1（烘房环境 = 边界条件）

| 项 | 实测值 |
|---|---|
| 规模 | **241 行 × 3 列**（时间、温度、水分浓度） |
| 时间 | 0 → 14400 s，**步长恒为 60 s**，严格单调递增 |
| 温度 | 28.0000 → 50.1650 °C，**min 28.0000，max 50.2460** |
| 水分浓度 | 0.019630 → 0.049860 kg/kg，**min 0.019630，max 0.050250** |
| $t\ge9600$ s 段均值（n=81） | T = **50.0017 °C**，C = **0.049998 kg/kg** |
| 尾部 10 点均值 | T = 50.0465 °C，C = 0.049928 kg/kg |

**关键节点**

| t / s | 0 | 300 | 600 | 900 | 1200 | 1500 | 1800 |
|---|---|---|---|---|---|---|---|
| T∞ / °C | 28.000 | 30.834 | 33.202 | 35.524 | 37.882 | 39.595 | **41.513** |
| C∞ / (kg/kg) | 0.01963 | 0.02191 | 0.02428 | 0.02660 | 0.02891 | 0.03098 | **0.03307** |

> ⚠️ **重要实测发现**：温度序列**不是单调递增**，差分变号 **116 次**；水分浓度变号 **119 次**。
> **抖动遍布全程，不只是尾部。** 这直接影响建模决策（见 §5.4），
> 也意味着**不能推断为控制器振荡**——只能说是"真实边界含有波动"。

### 1.2 附件2（半径，问题4 专用）

| 项 | 实测值 |
|---|---|
| 规模 | **145 行 × 2 列**（时间、半径） |
| 时间 | 0 → 259200 s（= 72 h），**步长恒为 1800 s**（= 0.5 h） |
| 半径 | 2.0000 → 1.1980 cm，**严格单调递减**（差分全 ≤ 0） |
| 最大单步降幅 | −0.1270 cm（出现在最早期） |
| 首次 ≤ 1.199 cm | t = **208800 s = 58 h** |
| 后期 | 129600 s 起长期停在 1.200/1.199，末端降为 1.198 |

**采样点（每 12 点取 1）**

| t / h | 0 | 6 | 12 | 18 | 24 | 30 | 36 | 42 | 48 | 54 | 60 | 66 | 72 |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| R / cm | 2.000 | 1.374 | 1.248 | 1.214 | 1.204 | 1.201 | 1.200 | 1.200 | 1.200 | 1.200 | 1.199 | 1.199 | 1.198 |

> 半径序列**严格单调**，这对拟合有利——**保单调插值不会与数据冲突**。

### 1.3 附件3 模板（输出规格）

| 文件 | sheet 名 | A 列首行 | A 列起点 | 表头行 | 末列 |
|---|---|---|---|---|---|
| result1 | `温度`、`水分浓度` | `时间\到药材的中心距离` | **1** | 0, 0.1, 0.2, …, 2 | **2**（数值） |
| result2 | `温度`、`水分浓度` | 同上 | **1** | 同上 | **2**（数值） |
| result3 | `Sheet1` | 同上 | **60** | 同上 | **2**（数值） |
| result4 | `Sheet1` | 同上 | **60** | 同上 | **药材表面**（字符串） |

- 表头用 `0, 0.1, 0.2, '…', 2` 表示省略，实际应展开为 **21 列**：0.0, 0.1, …, 2.0
- 表头文字为 **`时间\到药材的中心距离`**（含反斜杠），须逐字复制
- 保留 **4 位小数**

### 1.4 数据特征对建模的三个含义

1. **观察时长不匹配**：附件1 只覆盖 0–4 h，而全程需 2–3 天 → **恒温段边界必须外推或假设**（§5.3）
2. **附件1 含波动**：建模时须决定"忠实插值"还是"受控平滑"，**并做对照**（§5.4）
3. **附件2 严格单调**：可安全使用保单调插值，但仍须**解析求导**（§7.3）

---

## 2. 模型分层与入口检查

### 2.1 三层结构

| 层次 | 目的 | 状态 | 交付 |
|---|---|---|---|
| **M0：题设基线** | 按附录与给定边界回答四问 | **必做** | result1—4.xlsx、题设表1—6 |
| **M1：可核查扩展** | 潜热、辐射、端面、参数/环境情景 | **条件充分时做**，与 M0 分开标识 | 对照图与误差表 |
| **M2：工程推广** | 烘房系统、结构力学、品质与批次 | **只给路线与数据需求**，无数据不报数值 | 局限讨论段落 |

> **原则**：不能按因素数量或"文献星级"决定模型复杂度。
> 「物理上存在」「本题必须显式建模」「已有数据足够定量计算」是**三件不同的事**。

### 2.2 ★ 变量定义与单位约定（第一顺位修正）

**必须严格区分两个 kg/kg：**

$$C=\frac{m_w}{m_d}\quad(\text{药材干基含水率}),\qquad
Y=\frac{m_v}{m_{da}}\quad(\text{空气含湿量})$$

> ⚠️ **二者即使同为 kg/kg，也不能自动相减。** 分母不同（干物质 vs 干空气）。

**附件1 的"水分浓度"如何进入边界？**

| 情形 | 处理 |
|---|---|
| **若确认附件采用题设等效约定**（即直接作为药材表面平衡含水率） | M0 按该约定建模，**在假设中明确写出其含义** |
| **若确认是真实空气含湿量 Y** | 建立物理界面模型还需要**平衡含水率或吸附等温关系**；**不能直接拿 Y 当药材表面平衡 C** |

**v2 的推荐做法**：
引入符号 $C_\infty^{eq}$（**已明确基准的等效环境值**），在论文中明确声明
"本文按题设等效约定，将附件1 的水分浓度作为药材表面的等效平衡含水率"，
**不假装这是已经由真实空气状态求出的吸附平衡结果**。

**合理性自检**：目标 0.15 kg/kg > $C_\infty^{eq}\approx0.05$，驱动势始终为正 ✅

### 2.3 入口检查清单（开工前必须完成）

- [ ] 核实附件1 的"水分浓度"究竟是**药材等效平衡含水率**还是**空气含湿量 Y**
- [ ] 核实附录2/3/4 中 **ρ 的物理含义**（湿物料体积密度？干基？热学等效参数？）
- [ ] 核实附录3/4 的 D 公式中 **T 的单位**（题面标注为 K，与附件1 的 °C 不同）
- [ ] 核实**长时环境外推**的合理方式（附件1 只到 4 h）
- [ ] 核实**半径数据覆盖范围**与问题4 达标时刻的相对关系
- [ ] 核实**模板规则**（§1.3 已核，仍需确认 21 列展开方式）

> **题目公式优先**，不用文献中的蔬菜/谷物参数替换附录给出的公式与系数。

---

## 3. 统一物理骨架

### 3.1 几何与降维

药材为圆柱，**长 $L_0=0.25$ m，初始半径 $R_0=0.02$ m**。

**建模选择：一维轴对称径向模型** $r\in[0,R(t)]$。

**降维依据（须如实表述）**
- 题目所有输出只到"距中心 2 cm"，即**最大距离恰为初始半径**
- 端面与侧面面积比：$\dfrac{2\pi R_0^2}{2\pi R_0L}=\dfrac{R_0}{L_0}=0.08$
- 端面方向的扩散时间尺度比（**须用半长 $L_0/2$**，两端同时交换）：
$$\frac{t_z}{t_r}\sim\left(\frac{L_0/2}{R_0}\right)^2=\left(\frac{0.125}{0.02}\right)^2=39.06$$

> ⚠️ **两处必须避免的过度表述**：
> ① **不能由"输出距离只到 2 cm"单独证明"题目已规定无限长圆柱"**——那只是输出范围；
> ② **面积比 8% ≠ 误差 8%**，也**不能**预先声称"端面影响 3%"。
> 正确表述：端面方向尺度比约 **39**，故一维径向近似**在时间尺度上可接受**；
> 最终以**二维轴对称对照计算**验证（§9.2）。

### 3.2 控制方程（M0，问题1—3）

**热量**（变物性时保持守恒形式，变系数**不得移出微分算子**）：
$$\rho(C)\,c_p(C)\,\frac{\partial T}{\partial t}
=\frac{1}{r}\frac{\partial}{\partial r}\!\left(k(C)\,r\,\frac{\partial T}{\partial r}\right)$$

**水分**（有效扩散模型，干基含水率）：
$$\frac{\partial C}{\partial t}
=\frac{1}{r}\frac{\partial}{\partial r}\!\left(D\,r\,\frac{\partial C}{\partial r}\right)$$

> **关于 ρ 的处理**：$C$ 定义在**干固体**基准上。不收缩体（问题1—3）中
> 干固体密度 $\rho_d$ 沿空间恒定，故水分方程中不出现密度。
> 但**附录给出的 ρ 是湿物料体积密度**，用于热容项 $\rho c_p$。
> 二者的关系为 $\rho_d=\rho/(1+C)$，**不可混用**（§7.3 会用到这个关系做诊断）。
>
> **关于有效扩散模型的边界**：文献【R01】指出机理模型与半经验模型的关系——
> 题设的 D 可能已折合了液相扩散、汽相迁移等多种机制。
> **新增迁移通道时必须避免与有效 D 重复计数。**

### 3.3 边界条件

**中心对称（$r=0$）**
$$\left.\frac{\partial T}{\partial r}\right|_{r=0}=0,\qquad
\left.\frac{\partial C}{\partial r}\right|_{r=0}=0$$

**表面对流传热（$r=R$，M0 形式）**
$$-k\left.\frac{\partial T}{\partial r}\right|_{r=R}=h\,(T_s-T_\infty)$$

**表面对流传质（$r=R$）**
$$-D\left.\frac{\partial C}{\partial r}\right|_{r=R}=h_m\,(C_s-C_\infty^{eq})$$

其中 $h=25$ W/(m²·K)，$h_m=8\times10^{-7}$ m/s。

**M1 扩展形式（含潜热与辐射，**须先闭合通量**，见 §9.1）**
$$-k\nabla T\cdot n=h(T_s-T_\infty)+\lambda(T_s)\,J_w-q_{rad,in}$$

- 左侧为**向外的导热通量**
- 正蒸发耗热**提高**向外需求；入射净辐射取**减号**
- $J_w$ 必须明确为**向外的水质量通量，单位 kg/(m²·s)**

> 🔴 **最常见错误**：把 $h_m(C_s-C_\infty)$ 直接乘潜热当热通量。
> **它单位不是 kg/(m²·s)**。若乘 $\rho_d$ 转成 $\rho_d h_m(C_s-C_\infty)$，
> 必须说明该密度与扩散通量、材料运动、水质量守恒的兼容性。

### 3.4 初始条件（四问相同）

$$T(r,0)=28\ ^\circ\text{C},\qquad C(r,0)=2.55\ \text{kg/kg},\qquad 0\le r\le R_0$$

### 3.5 物性（三套，注意适用问题与单位）

| 量 | 问题1（附录2） | 问题2/3（附录3） | 问题4（附录4） |
|---|---|---|---|
| ρ / (kg/m³) | 820（常数） | $650+128C$ | $760+90C$ |
| c_p / (J/(kg·K)) | 2600（常数） | $1450+2736\dfrac{C}{1+C}$ | $1850+2150\dfrac{C}{1+C}$ |
| k / (W/(m·K)) | 0.36（常数） | $0.21+0.38\dfrac{C}{1+C}$ | $0.12+0.20\dfrac{C}{1+C}$ |
| D / (m²/s) | $7\times10^{-9}e^{-0.89/C}$ | $2.4\times10^{-3}e^{-0.45/C}e^{-3850/T_K}$ | $4.2\times10^{-4}e^{-0.30/C}e^{-3850/T_K}$ |

> 🔴 **T 单位**：附录3/4 的 D 公式中 **T 必须是开尔文**。
> 转换只做一次：$T_K=T_C+273.15$，全局禁止混用。
>
> 🔴 **C 是场的函数**：ρ、c_p、k 中的 C 是**同一时刻同一空间点**的含水率，
> 不是全场平均，也不是初值。

**D 的量级（用于数值方法选型与自检）**

| C | 0.05 | 0.15 | 1.0 | 2.55 |
|---|---|---|---|---|
| D（附录2）/ (m²/s) | $1.3021\times10^{-16}$ | $1.86\times10^{-11}$ | $2.87\times10^{-10}$ | $4.94\times10^{-9}$ |
| D（附录3）/ (m²/s) | — | $7.9571\times10^{-10}$（@323 K） | $3.31\times10^{-9}$ | $5.3719\times10^{-9}$（@300 K） |

**跨约 7 个数量级 → 方程强刚性 → 必须用隐式方法。**

### 3.6 无量纲分析

问题1 特征量：$\alpha=k/(\rho c_p)=0.36/(820\times2600)=1.6886\times10^{-7}$ m²/s

| 数 | 定义 | 值 | 含义 |
|---|---|---|---|
| $Bi_T$ | $hR_0/k$ | **1.389** | 内外导热阻力相当 |
| $Bi_m$ | $h_mR_0/D$ | **3.24**（@C=2.55） | 内扩散阻力主导 |
| $Fo_T$（t=1800 s） | $\alpha t/R_0^2$ | **0.760** | 30 min 内中心被显著加热 |
| $Fo_m$（t=1800 s） | $Dt/R_0^2$ | **0.0222** | 水分变化远慢于温度 |
| 传热特征时间 | $R_0^2/\alpha$ | **2369 s ≈ 39.5 min** | 与题目 30 min 窗口吻合 |
| 传质特征时间 | $R_0^2/D$ | **≈ 8.1×10⁴ s ≈ 22.5 h** | 与"2–3 天"时间尺度吻合 |

> **模型选型依据**：$Bi_T\approx1.4$、$Bi_m\approx3.2$，**内外阻力均不可忽略**
> → **不能用集总参数模型**，必须解偏微分方程。这是本题必须用 PDE 的硬证据。

---

## 4. 问题1：常热物性 + 非线性水分扩散（0–1800 s）

### 4.1 模型

直接使用 §3.2—3.4，取**附录2 的常物性**：

$$\rho c_p\frac{\partial T}{\partial t}=\frac{k}{r}\frac{\partial}{\partial r}\!\left(r\frac{\partial T}{\partial r}\right),\qquad
\frac{\partial C}{\partial t}=\frac{1}{r}\frac{\partial}{\partial r}\!\left(D(C)\,r\frac{\partial C}{\partial r}\right)$$

$$D(C)=7\times10^{-9}\exp\!\left(-\frac{0.89}{C}\right)$$

边界用附件1 的 $T_\infty(t)$、$C_\infty^{eq}(t)$（取 0–1800 s 段，共 **31 个数据点**）。

### 4.2 ★ 结构性洞察：单向耦合

| 方向 | 是否耦合 | 原因 |
|---|---|---|
| 水分 → 温度 | **是** | 温度边界含蒸发通量（M1 时） |
| 温度 → 水分 | **否** | 附录2 的 $D(C)$ **不依赖 T** |

$$\boxed{\text{先解水分方程}\ \longrightarrow\ \text{再解温度方程}}$$

这是问题1 相比问题2 的**最大计算简化**，也是论文里值得点明的一句话。

> **M0 注释**：由于 M0 不含潜热项，温度方程实际上是**完全独立**的线性问题。
> 蒸发通量只在 M1 中进入温度边界。

### 4.3 数值方案

**空间离散（推荐有限体积法）**
- 同心环形控制体，**中心用半控制体**（半径 $\Delta r/2$ 的实心圆柱），
  **天然规避 $1/r$ 奇点**——优于有限差分的 L'Hôpital 处理
- 界面通量用**调和平均**（变 D 时保证通量正确）
- 基准网格：$\Delta r=0.5$ mm（40 格）；另取 0.25 / 0.125 mm 做无关性验证
- 若改用有限差分，中心节点须用
  $\frac{1}{r}\partial_r(r\partial_r T)|_{r=0}=2\partial_r^2T|_{r=0}$

**时间推进**
- 显式稳定性上限（**按选定离散算子确定**）：圆柱中心节点中心格式约
  $\Delta t\le\dfrac{\Delta r^2}{4\alpha}$，$\Delta r=0.5$ mm 时约 **0.37 s**
  → **显式格式无法满足 1 s 输出要求**，必须隐式
- 推荐 **Backward Euler 或 Crank–Nicolson + 非线性迭代（Picard / Newton）**
- 步长由**误差控制与边界变化**决定，**输出间隔 ≠ 计算步长**

**边界离散**：表面对流条件用**镜像节点**或**界面能量平衡**，
保证二阶精度，避免边界降阶污染整体精度。

### 4.4 结果输出（result1.xlsx）

| 项 | 规格 |
|---|---|
| sheet | `温度`、`水分浓度` |
| A 列 | 时间 1, 2, 3, …, 1800（**从 1 起**） |
| 首行 | `时间\到药材的中心距离`，后接 0.0, 0.1, …, 2.0（**21 列**） |
| 尺寸 | 1800 × 21 |
| 精度 | 4 位小数 |

**论文表1/表2**：抽取 t = 100/300/600/900/1200/1500/1800 s 与 r = 0/0.5/1/1.5/2 cm 的 **7×5 子块**

### 4.5 模型检验（本问可做到"教科书级"严格）

问题1 的 M0 温度方程是**线性、常系数**的，可用**无限长圆柱 Robin 边界解析解**验证：

$$T(r,t)=T_\infty+\sum_{n=1}^{\infty}A_nJ_0\!\left(\mu_n\frac{r}{R_0}\right)
\exp\!\left(-\frac{\alpha\mu_n^2t}{R_0^2}\right)$$

- 特征值 $\mu_n$ 为 $\mu J_1(\mu)=Bi_T\,J_0(\mu)$ 的**正根**（取前 30 个）
- 系数 $A_n$ 由初值经 Bessel 函数正交性确定

> **验证条件（三项必须同时满足）**：
> ① 固定热学物性 ② **关闭蒸发热效应** ③ **满足解析解要求的环境边界**（恒定 $T_\infty$）
>
> 做法：令 $T_\infty\equiv$ 常数、关闭蒸发项，与解析解逐点比对，
> 绘制**误差 vs 网格步长**的双对数图。**横轴为步长 h 时，二阶收敛对应斜率 +2**。
>
> 水分方程因 $D(C)$ 非线性无解析解，改用**守恒检验**（§8.3）。

---

## 5. 问题2：变参数热湿耦合（全流程）

### 5.1 相对问题1 的三个升级

| 升级 | 内容 | 后果 |
|---|---|---|
| ① 变物性 | ρ(C)、c_p(C)、k(C)、D(C,T) 全部随场变化 | 强非线性，**必须用守恒形式离散** |
| ② 双向耦合 | **D 依赖 T** | 水分场不再独立，**T 与 C 必须联立求解** |
| ③ 全流程边界 | 跨越 2–3 天，附件1 只给 4 h | 需要明确的边界延伸假设 |

### 5.2 控制方程与物性

$$\rho(C)c_p(C)\frac{\partial T}{\partial t}=\frac{1}{r}\frac{\partial}{\partial r}\!\left(k(C)r\frac{\partial T}{\partial r}\right),
\qquad
\frac{\partial C}{\partial t}=\frac{1}{r}\frac{\partial}{\partial r}\!\left(D(C,T)r\frac{\partial C}{\partial r}\right)$$

$$D=2.4\times10^{-3}\exp(-0.45/C)\exp[-3850/(T+273.15)]$$

**从 $t=0$ 统一采用附录3**，**不允许**先跑问题1 再在某时刻切换参数。

**物性取值要与离散设计一致**
- 物性在**控制体**上取平均（算术平均在光滑条件下可达二阶）
- **界面通量**用调和平均
- 节点值**并非一概错误**，但要求一致重构
- **整体验证**（守恒 + 收敛），而非孤立断言某一种取值方式正确

### 5.3 ★ 关键假设：恒温干燥阶段的边界条件

附件1 仅覆盖 0–14400 s（4 h），而全流程需 2–3 天。**实测**：$t\ge9600$ s 段
（n = 81 点）均值 T∞ = **50.0017 °C**，C∞ = **0.049998 kg/kg**，且已进入平台。

| 方案 | 做法 | 评价 |
|---|---|---|
| **A（推荐）** | $t\le t_{pre}$ 用**保形插值（PCHIP）**；$t>t_{pre}$ 取常数 $T_\infty\equiv50.0$ °C、$C_\infty^{eq}\equiv0.0500$ | 简单、可辩护。$t_{pre}$ 取 **9600 s** 或 **14400 s**，二者都试 |
| **B** | 拟合一阶饱和模型后外推 | 边界连续可微，但对波动敏感、外推无依据 |
| **C** | 整体双指数拟合 | **不推荐**，易过拟合波动 |

> **必须做敏感性分析**：$t_{pre}\in\{9600, 14400\}$ s、稳态值在
> $T_\infty\in[49.8,50.3]$、$C_\infty^{eq}\in[0.0495,0.0505]$ 内扰动，
> 观察对问题3 达标时间的**影响幅度**。**该结论本身写入论文**，
> 因为它直接决定问题3 答案的**情景区间**。

### 5.4 ★ 环境曲线的插值与平滑

**实测事实**：附件1 共 241 点，温度差分**变号 116 次**、水分浓度变号 **119 次**，
波动**遍布全程**。

| 做法 | 说明 |
|---|---|
| **默认：忠实插值** | 保留有效测量。实测波动是**真实边界条件**的一部分，**不天然属于噪声** |
| **对照：受控平滑** | 仅作为**对照**，且须说明平滑依据（如传感器精度、时间序列特征） |

> ⚠️ **不得推断控制器类型**（不能说"这是 PD 超调"）。
> **不得无证据强制平滑**。**也不得据此推断烘房结构。**

### 5.5 结果输出（result2.xlsx）

- 结构同 result1（双 sheet，A 列时间，21 列距离，4 位小数）
- ⚠️ **时间范围须先核实模板**：模板 A 列从 1 起，但**未规定终止行**
  - 题面表3/表4 只要求 **3 h** 内的结果
  - 但问题表述是"**整个烘干过程**"
  - **推荐**：按 **3 h（10800 s）** 输出并与表格一致，**在论文中明确说明**；
    如担心另一种解读，另存一份全流程版本
- **论文表3/表4**：t = 0.5/1.0/…/3.0 h，r = 0/0.5/1/1.5/2 cm，**6×5 子块**

### 5.6 与问题1 的关系（不是"正确性验证"）

用问题2 模型算到 t = 1800 s 与问题1 对比——**这属于「模型差异分析」，
不能当作两套实现必须相等的正确性验证**。

应量化并**解释成因**：常物性 vs 变物性、D 公式不同（附录2 的 D 跨 7 个数量级）。

---

## 6. 问题3：全域首次达标时间

### 6.1 判据定义

$$C_{max}(t)=\max_{r\in[0,R_0]}C(r,t),\qquad
t_*=\inf\{\,t:\ C_{max}(t)<0.15\,\}$$

**执行要点（逐条对应审查意见）**
1. 使用**未舍入数值**定位阈值通过时刻
2. **连续模型的阈值交点通常满足 $C_{max}=0.15$，而非严格小于**
   → 报告**「阈值通过时刻」**，并**单独验证其后的一个受控小时间增量**的全场值
3. **检查最大值出现的位置**——不以表面值或平均值代替
4. 若环境可能造成**回湿**，须区分**「首次达标」**与**「持续达标」**
5. 若输出规定"最早整数秒达标"，采用该规则并注明

> **不得使用平均含水率代替最大含水率。**

### 6.2 求解策略

| 方法 | 做法 | 用途 |
|---|---|---|
| **事件检测（主）** | 把 $C_{max}(t)-0.15$ 作为 ODE 求解器的终止事件，自适应精确定位零点 | 主结果 |
| **预估 + 二分（校核）** | 先粗算至明显过冲，再在区间内二分 | 交叉验证 |

### 6.3 误差报告（三类必须分开）

| 误差类型 | 说明 |
|---|---|
| **事件定位误差** | 零点搜索的收敛容差 |
| **空间离散误差** | 由 $\Delta r$ 引起（§8.4） |
| **时间积分误差** | 由格式阶数与步长引起（§8.4） |

> ⚠️ **定位到 1 s 不代表干燥时间整体准确到 1 s。**

### 6.4 输出（result3.xlsx）

| 项 | 规格 |
|---|---|
| sheet | `Sheet1` |
| A 列 | 时间 **60, 120, 180, …, $t_*$**（从 60 起） |
| 首行 | 21 列距离 |
| 末行 | **达标时刻**（**不一定落在 6 h 整数倍上**） |

**论文表5**：每 6 h 一行、每 0.5 cm 一列，末行填"烘干结束时间"及对应含水率。

### 6.5 合理性与稳健性

> ⚠️ **"烘干一般持续 2–3 天"是题设背景描述，不是必须通过的数值验收区间。**
> 偏离时检查**数据、单位、边界和误差**，**不应为了落入区间而调参**。

**排查清单（若结果异常）**，优先级从高到低：
1. 边界驱动势的解读（§2.2，$C_\infty^{eq}$ 的基准）
2. 恒温段边界条件的设定（§5.3）
3. D 公式中 T 的单位（§3.5）
4. 数值离散与收敛性（§8）

**稳健性分析（先少量情景，再全局抽样）**
- 优先扰动：**D 的倍率**、$h_m$、**长时环境外推**、**半径拟合方式**（问题4）
- 其次：h、初始半径
- 所有人为范围**标为"情景设定"**，**不称"测量置信限"**
- 对每个情景报告 $t_*$、$C_{max}(t)$ 曲线、**是否可达标**
- **不可达标的扰动方案单独标记，不能强行填有限时长**

---

## 7. 问题4：收缩模型与一致性诊断

### 7.1 收缩假设（明确声明）

附件2 只给**整体半径 $R(t)$**，不含内部位移信息。主模型采用：

> **固定长度 + 均匀径向仿射收缩**
> $$\xi=\frac{r}{R(t)},\qquad v_r=\frac{\dot R}{R}\,r$$

即在任一时刻，物料的**相对径向位置不变**。

> ⚠️ **必须如实声明**：这是**假设**，不是由数据验证的结论。
> **单个表面半径不能识别内部位移是否仿射。** 缺少内部标记或位移数据时，
> 只能说"在均匀仿射假设下"。

### 7.2 控制方程（材料导数形式）

在**材料导数**与有效扩散近似下：

$$\frac{\partial\widetilde C}{\partial t}
=\frac{1}{R(t)^2\xi}\frac{\partial}{\partial\xi}\!\left(\xi D\frac{\partial\widetilde C}{\partial\xi}\right),
\qquad
\rho c_p\frac{\partial\widetilde T}{\partial t}
=\frac{1}{R(t)^2\xi}\frac{\partial}{\partial\xi}\!\left(\xi k\frac{\partial\widetilde T}{\partial\xi}\right)$$

其中 $\widetilde C(\xi,t)=C(\xi R(t),t)$。

**坐标关系（用于理解，不用于主模型）**
$$\left.\frac{\partial C}{\partial t}\right|_r
=\left.\frac{\partial\widetilde C}{\partial t}\right|_\xi
-\frac{\dot R}{R}\xi\frac{\partial\widetilde C}{\partial\xi}$$

> **关键说明**：若物理方程采用**材料导数**且材料速度为 $v_r=(\dot R/R)r$，
> 则材料输运项与坐标项**相消**，得到上式。
> 若原方程仅采用**固定空间位置的偏导数**，则变换后**会出现坐标项**。
> **两种写法不能在未交代材料运动的情况下互相替换。**
>
> ✅ **主模型采用材料坐标形式**（与均匀仿射收缩假设自洽）。
> 两种形式**应给出相同结果**——这构成一致性检验（§8.5）。

**边界条件（含 $1/R$ 因子）**
$$-\frac{k}{R(t)}\left.\frac{\partial\widetilde T}{\partial\xi}\right|_{\xi=1}=h(\widetilde T_s-T_\infty),\qquad
-\frac{D}{R(t)}\left.\frac{\partial\widetilde C}{\partial\xi}\right|_{\xi=1}=h_m(\widetilde C_s-C_\infty^{eq})$$

**物性用附录4**（注意系数与指数均与附录3 不同）。

### 7.3 ★ 几何—密度一致性诊断（替代"验证仿射收缩"）

**诊断量的定义**

$$M_d^{diag}(t)=2\pi L(t)\int_0^{R(t)}\frac{\rho(C(r,t))}{1+C(r,t)}\,r\,\mathrm dr,
\qquad
e_d(t)=\frac{M_d^{diag}(t)-M_d^{diag}(0)}{M_d^{diag}(0)}$$

其中 $\rho_d(C)=\dfrac{\rho(C)}{1+C}=\dfrac{760+90C}{1+C}$（在**附录4 的 ρ 为湿物料体积密度**这一条件下）。

**诊断量的性质**
- 这是**条件性诊断**，**不是**基础有效扩散模型天然必须通过的严格质量守恒测试
- 命名为**"几何—密度一致性诊断"**，**不叫"质量守恒检验"**

**三个必须先说明的未决问题**
1. 附件末时刻**不必等于**首次达标时刻；全场最大含水率 0.15 **不等于**全场均匀 0.15
2. 单个表面半径**不能识别**内部位移是否仿射，**没有内部位移数据就不能说"验证了仿射收缩"**
3. **方向性矛盾**：在上述条件下，若终态全场均为 0.15 且半径 1.198 cm，
   干物质守恒要求 $L_f/L_0\approx1.155$，即**长度增加约 15.5%**；
   **长度缩短会使矛盾加重，不能作为补救解释**

**条件性上界检查（不依赖均匀终态假设）**

对 $C\ge0$ 有 $\rho_d(C)\le760$。若长度固定、无干物质损失，则

$$R_f\ge R_0\sqrt{\frac{\rho_{d,0}}{760}}
=2.000\times\sqrt{\frac{278.7324}{760}}\approx1.2112\ \text{cm}$$

而实测末值 $R_f=1.1980$ cm **略小于该下界**。

> **这说明什么**：提示**密度定义、数据误差、几何假设或干物质守恒的联合使用需要核查**。
> **这并不证明题目数据错误。**
> **若 ρ 只是热学等效参数，则不应强行用于严格干质量检验。**
> **若要讨论孔隙塌陷，必须相应修订密度/孔隙关系**，
> **不能在固定原密度关系的同时用"塌陷"解释一切。**

**论文中的报告方式**：把这三点写成"模型适用边界"的讨论，
给出 $e_d(t)$ 曲线与上界比较，而**不下"数据有误"或"假设已验证"的结论**。

### 7.4 输出格式（result4.xlsx / 表6）

| 项 | 规格 |
|---|---|
| sheet | `Sheet1` |
| A 列 | 时间 60, 120, …, $t_*$ |
| 首行 | 距离列 + **末列固定为字符串 `药材表面`** |

**输出转回实际 r 坐标**
1. 对每个时刻，输出当前半径内的 0.1 cm 间隔位置值
2. **单独计算当前表面 $r=R(t)$ 的值**填入「药材表面」列——**该列随半径变化，不能固定取初始的 2 cm**
3. **域外位置按模板约定留空或标记无效**
4. 🔴 **不得将药材外部位置填成零含水率**（0 的语义是"完全干燥"，完全错误）

> 模板表头用 `…` 表示省略，实际列方案需与模板展开方式一致，
> **并在论文中说明所选方案**。

### 7.5 对照实验（A→B→C 顺序差分）

| 方案 | 尺寸 | 物性 | 用途 |
|---|---|---|---|
| **A** | 固定 $R_0$ | 附录3 | 基线（即问题3） |
| **B** | 固定 $R_0$ | 附录4 | 隔离**物性公式**的差异 |
| **C** | 随时间收缩 | 附录4 | 加入**收缩**的影响（即问题4） |

**表述规范**
- ✅ 称为**「按 A→B→C 路径的顺序差分」**
- ❌ **不称为**"唯一、独立的因果贡献分解"
- 第四方案（收缩 + 附录3）可追加，但**降为 M1 可选**；无第四方案时**不声称完成了交互分析**

**退化检验（P0）**
- **问题4 固定半径 + 保留附录4 物性** → 应退化为 **B 方案**
- ⚠️ **不能直接要求它等于使用附录3 的问题3**；要与问题3 一致还需**统一物性等全部条件**

---

## 8. 数值求解与检验（四问共用）

### 8.1 离散方案

- **一种一致的径向有限体积离散 + 隐式时间推进**
- 重点控制**中心、表面、变系数界面通量**三处
- **区分计算网格与输出采样**：输出每 1 s **不等于**所有内部计算步长必须 1 s
- 输出采样**不自动守恒**：题目需要**点值采样**，应验证**采样误差与无过冲**；
  **三次插值不天然保正**
- 只有**重映射控制体平均量**时才要求保守映射

### 8.2 基准验证（先做，再扩展）

**常系数解析基准**：\(T\) 方程在固定物性、恒定 $T_\infty$、关闭蒸发时与
Bessel 级数解比对（§4.5）。

> ⚠️ **三个条件必须同时满足**：固定热学物性 + 关闭蒸发热效应 + 满足解析解要求的边界。
> 缺一不可。

### 8.3 守恒与通量收支

**优先检验所解方程的积分收支**

$$M_w(t)=\int_0^{R}2\pi r\,\rho_d\,C\,\mathrm dr,\qquad
M_w(0)-M_w(t)\ \overset{?}{=}\ \int_0^t 2\pi R\,J_w(\tau)\,\mathrm d\tau$$

> ⚠️ **面积口径必须一致**：
> - 整根圆柱侧面积是 $2\pi RL$
> - 使用 $2\pi R$ 时**必须明确是"单位长度总量"**

**离散守恒 vs 严格守恒**：区分「**离散方程守恒**」（内部界面通量成对抵消，应达机器精度）
与「**严格实际水质量守恒**」（涉及 $\rho_d$ 的物理定义，见 §7.3）。

**能量检验的边界**

> ⚠️ **变物性模型不宜直接把 $\rho c_p T$ 的差值当作完整内能变化。**
> 应**优先检验所解方程的积分收支**；严格的**热力学检验需另行定义一致的能量状态基准**。

**渐近检验**：$t\to\infty$ 时 $T\to T_\infty$、$C\to C_\infty^{eq}$（问题1—3）。

### 8.4 网格与时间误差（分开细化）

| 检验 | 做法 | 关注量 |
|---|---|---|
| **空间细化** | $\Delta r$ = 1.0 / 0.5 / 0.25 mm，时间格式固定 | 中心 T、中心 C、$C_{max}$、$t_*$ |
| **时间细化** | 格式固定，容差逐级收紧 | 同上 |

**阶数说明**
- **Backward Euler 时间一阶**
- **Crank–Nicolson 在足够光滑等条件下时间二阶**
- **不要求所有误差都二阶收敛**
- **横轴为步长 $h$** 且 $E\propto h^2$ 时，双对数图斜率应为 **+2**；
  横轴为 $1/h$ 或网格数 $N$ 时才是 **−2**

> **"四位小数"是输出格式要求，不代表模型天然具有四位小数的物理精度。**
> 结论应表明**基准网格的离散误差显著小于 4 位小数的舍入量级**。

### 8.5 退化与一致性检验（P0）

| # | 检验 | 正确条件 |
|---|---|---|
| 1 | **问题2 → 问题1** | 不仅固定热学物性，**还要把 D 替换为附录2 的 $D(C)$**，并**统一初值、环境边界与传递系数**。⚠️ 附录2 的 D **本身不是常数** |
| 2 | **问题4 固定半径** | 保留附录4 物性时 **→ 退化为 B 方案**，**不等于**问题3 |
| 3 | **纯导热解析基准** | 固定物性 + 关闭蒸发 + 满足解析解边界（§8.2） |
| 4 | **坐标变换一致性** | 材料导数形式 ↔ 固定坐标 + 坐标项，**应给出相同结果**；不等价说明**推导或离散有误**，而非"模型不同" |
| 5 | **物理合理性** | 非负性、对称性、无非物理过冲、内部通量与表面通量方向一致 |

> ⚠️ **删除无条件单调性断言**：含水率是否单调下降**取决于环境条件**，
> 在环境可能造成**回湿**时不能强制设定所有位置始终下降。

---

## 9. M1：可核查扩展（条件充分时做，与 M0 分开标识）

### 9.1 潜热：先闭合通量，再加边界

**前置条件（缺一不可）**
- [ ] 明确的**水质量通量** $J_w$（单位 kg/(m²·s)）
- [ ] 明确的**密度基准**（$\rho_d$ 与扩散通量、材料运动、水质量守恒的兼容性）
- [ ] **潜热 $\lambda(T)$** 按实际温度查证（不是固定取 2.26×10⁶）
- [ ] 明确的**蒸发发生位置**

**边界形式**
$$-k\nabla T\cdot n=h(T_s-T_\infty)+\lambda(T_s)J_w-q_{rad,in}$$

> 🔴 **避免重复计数**：**体源（内部相变）与表面潜热不可重复**。
> 仅表面蒸发模型中用上式；只有显式内部相变时才再加对应的体源。

**量级估算（可写入论文，标注为"本文估算"）**

沿用假设：附录3 密度为初始湿物料总体积密度、干物质不流失、
最终含水率全体均匀为 0.15，且仅估计 28 → 50 °C 的显热。

| 步骤 | 计算式 | 结果 |
|---|---|---|
| 体积 | $V_0=\pi(0.02)^2(0.25)$ | $3.14159\times10^{-4}$ m³ |
| 初始湿质量 | $m_0=976.4\,V_0$ | 0.306745 kg |
| 干物质质量 | $m_d=m_0/3.55$ | 0.086407 kg |
| 需蒸发水量 | $\Delta m_w=m_d(2.55-0.15)$ | 0.207377 kg |
| 蒸发耗热 | $\Delta m_w\times2.26\times10^6$ | **≈ 468.67 kJ** |
| 显热（粗估） | $m_0 c_p\Delta T$ | **≈ 23.05 kJ** |
| 比值 | | **≈ 20.33** |

> ⚠️ **该比值只能说明「蒸发能耗可能是重要能量尺度」**，
> **不能证明"前 1800 s 升温快了 20 倍"**——两者涉及不同过程的积分，
> 且实际最终分布也不全等于 0.15。
> **显热粗估不含变比热、质量流出、系统散热。**

**做法**：比较 M0 与 M1 的**表面/中心温度**和**达标时间**，
结论**只针对参数情景**。若通量或吸附关系无法确定，
**就保留上述方程、能量尺度和数据缺口，不输出虚构的修正时长**。

### 9.2 端面效应：二维对照优先于全面 CFD

**模型**：在 $0\le r\le R$、$-L/2\le z\le L/2$ 上增加轴向扩散

$$\frac{\partial C}{\partial t}=\frac{1}{r}\frac{\partial}{\partial r}\!\left(rD_r\frac{\partial C}{\partial r}\right)
+\frac{\partial}{\partial z}\!\left(D_z\frac{\partial C}{\partial z}\right)$$

**步骤**
1. **先令 $D_r=D_z=D$**，端面与侧面使用**同样明确的边界**，隔离**纯几何**影响
2. 比较**中心截面**、**体平均值**、**二维全域最大值**三者的达标时间
3. 随后如有材料证据再改变 $D_z/D_r$

> ⚠️ **可预先选择一个用途相关的误差容限，但不得把"3%"写成文献通用标准。**
> **资源不足时只给尺度分析，不写成"二维已验证"。**

### 9.3 辐射：条件性量级分析

**不预设壁温等于空气温度。**

$$q_{rad}=\varepsilon\sigma(T_w^4-T_s^4),\qquad
q_{conv}=h(T_\infty-T_s)$$

**条件性估算**（额外假设 $\varepsilon=0.9$、大包围壁面有效视角近 1、
$T_w=323.15$ K、$T_s=301.15$ K）：

| 项 | 值 |
|---|---|
| $q_{rad}$ | **≈ 136.76 W/m²** |
| $q_{conv}=25(50-28)$ | **550 W/m²** |
| 比值 | **≈ 24.87%** |

> ⚠️ **壁温、发射率及几何条件尚无题设依据**，该比值**仅对上述情景成立**，
> **不能作为普适判断**。
>
> 🔴 **符号标注须改正**：$\varepsilon\sigma T^4$ 是**辐射力**（中间量），
> 净辐射须**先做四次方差再乘 $\varepsilon$**，避免读者重复乘发射率。

**若壁温近似空气温度**，可写线性化辐射系数：
$$h_{rad}=\varepsilon\sigma(T_w+T_s)(T_w^2+T_s^2)$$
**若两者不同**，仅扰动对流系数**不能完整替代辐射边界**。

**主结果仍按题设对流系数计算，扩展明确另加辐射。**

### 9.4 稳健性与不确定性

**先少量情景，再全局抽样**

1. **优先级**：D 的倍率 → $h_m$ → 长时环境外推 → 半径拟合方式 → 必要时 h 与初始半径
2. 所有人为范围**标为"情景设定"**，**不称"测量置信限"**
3. 对每个情景报告 $t_*$、$C_{max}(t)$ 曲线及**是否可达标**
4. **不可达标情景单独标记**

**三种区间必须分开命名**

| 类型 | 含义 | 何时可用 |
|---|---|---|
| **情景区间** | 人为设定的参数范围对应的结果范围 | 随时可用 |
| **参数扰动分位区间** | 由参数分布传播得到 | 需明确分布假设 |
| **统计置信区间** | 由实测样本得到 | **需批次测量数据** |

> ⚠️ **无数据支撑的任意分布不能产生有实证意义的置信区间。**
> 取批次测量后，参考【R08】【R09】进行参数联合分布与全局敏感性分析；
> **检查参数相关性和计算量，而非强制所有因素相互独立。**

---

## 10. M2：工程推广（只给路线与数据需求）

### 10.1 烘房系统模型

**仅在需要预测新工况（改变装料量、换气量、加热策略）时启用。**

**简化为充分混合空气时，正确的水分收支形式**：

$$\frac{\mathrm d}{\mathrm dt}\!\left(M_{da}Y\right)
=\dot m_{da,in}Y_{in}-\dot m_{da,out}Y+\sum_iA_iJ_{w,i}-\dot m_{cond}$$

**还需干空气质量平衡。**

> ⚠️ **若 $M_{da}$ 不变，才可将左侧写成 $M_{da}\dot Y$。**
> **不能用 $V_{air}\dot Y$ 等于质量流率的形式**——量纲不闭合。

**能量收支**：按**统一焓基准**增加空气、壁面及物料的能量收支。

**验证方式**：选择**实测环境作为验证数据**，
**不能同时将环境固定与自由求解**（自相矛盾）。

**数据需求**：空气干质量、装料量、加热与排湿条件、
新风/排风干空气质量流量或除湿速率。

### 10.2 品质后处理

**有目标成分动力学后**，可沿材料轨迹计算成分保留分数：

$$\frac{\mathrm dq}{\mathrm dt}=-k_q(T,C)\,q,\qquad
\frac{q(t)}{q(0)}=\exp\!\left[-\int_0^t k_q(T(\tau),C(\tau))\,\mathrm d\tau\right]$$

> ⚠️ 这是**待标定的候选动力学**，**不是文献为本题药材提供的通用公式**。

| 扩展方向 | 额外数据需求 |
|---|---|
| 微生物 | 需 $a_w(C,T)$ 关系、菌种、初始菌量 |
| 结构（结壳/开裂） | 需应变、本构关系、强度参数 |
| 挥发成分保留 | 需成分种类、组织结构、动力学参数 |

**无水活度关系时**：只能说明"含水率达标 ≠ 微生物合格"，
**不能制定通用杀菌温度**。

### 10.3 结构力学

建模链路（**仅作路线描述**）：
```
湿度场 → 收缩应变 → 本构关系 → 应力场 → 断裂判据 → 诊断量
```

> ⚠️ **不能由湿度梯度直接算裂纹概率或裂纹位置**，
> 也**不能**把"多缩 7%"当作仿射收缩的验证（§7.3）。
> 可**输出梯度作为非标定诊断量**，但**不叫裂纹概率**。
>
> **题目半径数据不证明内部均匀收缩。**

---

## 11. 论文结构、图表与文献落位

### 11.1 章节结构

```
摘要（模型名 + 方法名 + 关键数值结论 + 灵敏度/情景结论）
1  问题分析        四问递进；题设计算与工程外推的区别
2  模型假设与符号   C/Y/ρ 定义、径向近似、能量边界限制
3  数据处理        环境与半径拟合、外推、真实波动与噪声的区分
4  问题1          常热物性、非线性扩散及 1800 s 结果
5  问题2          局部物性与阶段连续、给定边界
6  问题3          全域阈值与数值误差、情景稳健性
7  问题4          材料坐标、几何—密度诊断、A/B/C 比较
8  模型检验        基准、收敛、端面、可闭合的潜热对照
9  评价与推广      结构、系统、品质三组局限
参考文献 / 附录（代码、符号表）
```

### 11.2 图表清单（**主文约 10 组图，不堆 21 张**）

| 图组 | 内容 | 数据来源 | 优先级 |
|---|---|---|---|
| **G1** | 几何、边界与四问技术路线合图 | 手工绘制 | P0 |
| **G2** | 环境温度与水分浓度：原始点 + 插值曲线（含波动显示） | 附件1 | P0 |
| **G3** | 半径数据、拟合曲线与残差 | 附件2 | P0 |
| **G4** | 问题1 温度场与含水率场演化（**分子图，不用双纵轴**） | 问题1 结果 | P0 |
| **G5** | 问题2 前 3 小时径向分布 | 问题2 结果 | P0 |
| **G6** | 问题3：$C_{max}(t)$ 衰减曲线 + 0.15 阈值线 + 达标点 + 终态径向分布 | 问题3 结果 | P0 |
| **G7** | 问题4：物理坐标下的含水率演化 + 移动边界轨迹 | 问题4 结果 | P0 |
| **G8** | 基准误差与网格/时间收敛（**空间、时间分开**） | §8.2–8.4 | P0 |
| **G9** | A/B/C 顺序差分的 $C_{max}(t)$ 与达标时间比较 | §7.5 | P1 |
| **G10** | 关键参数与环境情景区间 | §9.4 | P1 |

**候选补充图（按需取用）**
- 几何—密度诊断 $e_d(t)$ 曲线 + 上界比较（§7.3）—— **对问题4 的论述价值高**
- 中心/表面/最大值三条含水率曲线对比（论证**必须用 max**）
- 二维 vs 一维端面对照（M1 时）
- 潜热开/关对照（M1 且通量闭合时）

**表格清单**

| 编号 | 表题 |
|---|---|
| TAB-01 | 符号说明表 |
| TAB-02 | **题设表 1—6** |
| TAB-03 | 三套物性公式对照（含单位与适用范围） |
| TAB-04 | 网格与时间误差收敛结果（**空间/时间分列**） |
| TAB-05 | 模型退化与一致性检验结果 |
| TAB-06 | 环境与参数**情景区间**表 |
| TAB-07 | A→B→C 顺序差分结果 |
| TAB-08 | 额外数据需求表（对应 M2 推广） |

> 图使用统一字体、单位与图例；保存 **PDF 矢量版 + PNG** 即可。

### 11.3 文献落位

| 章节 | 落位点 | 文献 |
|---|---|---|
| 1 问题分析 | 有效模型与机理模型的层次 | R01 |
| 2 假设与符号 | 方程假设、密度与守恒讨论；空气侧定义 | R01、R02；S01 |
| 3 数据处理 | 环境与半径拟合的辅助说明（**不能替代附件检查**） | R11、R15 |
| 4 问题1 | 潜热遗漏的讨论处 | R02 |
| 5 问题2 | 耦合影响讨论；系统模型对照背景 | R02；R10 |
| 6 问题3 | 不确定性与情景方法 | R08、R09 |
| 7 问题4 | 形变与传输耦合；方向扩散 | R06、R07 |
| 8 模型检验 | 分别贴近所借方法 | R02、R07、R09 |
| 9 评价与推广 | 结构、系统、品质三组 | R04、R05；R10、R13、R14；R16—R18 |

**优先深入阅读**：R01、R02、R06、R07、R08、R09。
篇幅允许再选 R04、R11、R16—R18。
**R03、R15、R19 只有在正文确实讨论对应因素时才保留**——
避免为了用完文献池增加无关段落。

**文献卡片**

| 编号 | 出处 | 匹配 | 关键限制 |
|---|---|---|---|
| **R01** | Datta A.K. (2007). *Porous media approaches to studying simultaneous heat and mass transfer in food processes. I: Problem formulations.* J. Food Eng., 80(1), 80–95. | 问题1/2 方程假设；因素1、2；密度与守恒 | 不提供本题专用系数 |
| **R02** | Kumar C., Millar G.J., Karim M.A. (2015). *Effective Diffusivity and Evaporative Cooling in Convective Drying of Food Material.* Drying Technology, 33(2), 227–237. | 因素1；潜热对照 | **不支持**本药材必有 10 °C 温差或潜热固定为显热 20 倍 |
| **R03** | Häussling Löwgren B. et al. (2020). *A Numerical Implementation of the Soret Effect in Drying Processes.* ChemEngineering, 4(1), 13. | 因素3 | 不能替本药材指定交叉系数或确定迁移方向 |
| **R04** | Gulati T., Datta A.K. (2015). *Mechanistic understanding of case-hardening and texture development during drying of food materials.* J. Food Eng., 166, 119–138. | 因素5、7、18 | **不支持**直接从含水率场确定裂纹概率 |
| **R05** | *Investigation of bound and free water in plant-based food material using NMR T₂ relaxometry* (2016). Innov. Food Sci. Emerg. Technol., 38, 252–261. | 因素6 | 水状态比例不能跨材料套用 |
| **R06** | Curcio S., Aversa M. (2014). *Influence of shrinkage on convective drying of fresh vegetables: A theoretical model.* J. Food Eng., 123, 36–49. | 问题4；因素19 | 物料与工况不同，**不能移植本构或收缩系数** |
| **R07** | Pacheco-Aguirre F.M. et al. (2014). *A method to estimate anisotropic diffusion coefficients for cylindrical solids: Application to the drying of carrot.* J. Food Eng., 125, 24–33. | 因素19、20；二维验证 | **扩散各向异性 ≠ 收缩各向异性**；胡萝卜系数不是药材参数 |
| **R08** | Cronin K., Kearney S. (1998). *Monte Carlo modelling of a vegetable tray dryer.* J. Food Eng., 35(2), 233–250. | 因素21 | 经验模型不能替换本题空间 PDE |
| **R09** | Mortier S.T.F.C. et al. (2014). *Global sensitivity analysis applied to drying models for one or a population of granules.* AIChE J., 60(5), 1700–1717. | 因素21 | 颗粒模型的敏感排序不能当作本药材结论 |
| **R10** | Sami S., Rahimi A., Etesami N. (2011). *Dynamic Modeling and a Parametric Study of an Indirect Solar Cabinet Dryer.* Drying Technology, 29(7), 825–835. | 因素8、9、12 | 太阳能集热的边界与参数不能原样用于热风烘房 |
| **R11** | Román F., Hensel O. (2014). *Real-time product moisture content monitoring in batch dryer using psychrometric and airflow measurements.* Comput. Electron. Agric., 107, 97–103. | 因素8、13、21 | 平均释水量不能证明每个位置均达标 |
| **R13** | Ziegler T., Jubaer H., Schütz M. (2016). *Increasing the Energy Efficiency of Batch-Type Drying with Partial Air Recirculation.* Chemie Ingenieur Technik, 88(1–2), 208–214. | 因素9 | 不能用本题给定湿度曲线反推唯一风门策略 |
| **R14** | Jain D. (2005). *Modeling the performance of greenhouse with packed bed thermal storage on crop drying application.* J. Food Eng., 71(2), 170–178. | 因素12 | 不能证明本题 2.7 h 升温由墙体造成 |
| **R15** | 李兵, 孙长应, 李为宁, 宋扬扬 (2018). 《基于 DMC-PID 串级控制的茶叶远红外烘干机设计与试验》. 茶叶科学, 38(4), 410–415. | 因素13 | 不能识别本题控制器，**不支持自动删去小幅波动** |
| **R16** | Kulshrestha R. et al. (2008). *The Effect of Water Activity and Storage Temperature on the Growth of Aspergillus flavus in Medicinal Herbs.* Planta Medica, 74(10), 1308–1315. | 因素14 | 储藏实验 ≠ 动态干燥；不能当全部真菌的安全标准 |
| **R17** | Hazarika U. et al. (2025). *Effect of lyophilization on the essential oil content and composition of herbs and spices with different volatile secretory structures.* LWT, 229, 118188. | 因素15 | 不能把损失百分比当作未确定药材种类的本题结果 |
| **R18** | Li R. et al. (2018). *Thermal inactivation kinetics and effects of drying methods on the phenolic profile and antioxidant activities of chicory leaves.* Sci. Rep., 8, 9529. | 因素16 | 不能为所有褐变反应与本药材提供统一动力学 |
| **R19** | Boina D., Subramanyam B. (2004). *Relative Susceptibility of Tribolium confusum Life Stages Exposed to Elevated Temperatures.* J. Econ. Entomol., 97(6), 2168–2173. | 因素17（非必写） | 不能当作"所有虫卵 50 °C 均不能杀灭" |
| **S01** | ASHRAE Handbook—Fundamentals, Ch.1, *Psychrometrics* | 湿空气性质定义 | 纠正露点/湿球混淆；**不是药材参数来源** |

---

## 12. 风险清单

| # | 风险 | 后果 | 对策 |
|---|---|---|---|
| 1 | D 公式中 T 用摄氏度 | 结果完全错误，可能溢出 | 全局统一 K；打印三档关键值自检 |
| 2 | 把 C（干基含水率）与 Y（空气含湿量）直接相减 | 边界驱动势错误 | §2.2，引入 $C_\infty^{eq}$ 并声明基准 |
| 3 | 附件1 只到 4 h 却要算 2–3 天 | 恒温段边界无依据 | §5.3 方案 A + 情景区间分析 |
| 4 | 把 $h_m(C_s-C_\infty)$ 直接乘潜热当热通量 | **量纲错误** | §9.1，必须先闭合 $J_w$ 到 kg/(m²·s) |
| 5 | 显式格式用 Δt = 1 s | 稳定性上限约 0.37 s，会发散 | 用隐式；**检查稳定性条件并拒绝不满足的设置** |
| 6 | 问题1 温度方程也做非线性联立 | 浪费算力，掩盖单向耦合洞察 | §4.2，先解水分再解温度 |
| 7 | 用"多缩 7%"论证仿射收缩 | **逻辑错误**，且与干物质守恒矛盾 | §7.3，改为几何—密度一致性诊断 |
| 8 | 端面尺度用全长 | 尺度比算成 156 而非 39 | §3.1，**必须用半长 $L/2$** |
| 9 | 声称"Lu ≪ 1 故可忽略交叉效应" | **判据错误** | 改为比较交叉通量与主通量 |
| 10 | 把"2–3 天"当验收区间而调参 | **科学不端** | §6.5，仅作背景描述 |
| 11 | 直接对附件2 差分求 $\dot R$ | 导数噪声放大成振荡 | 先拟合（PCHIP / 单指数趋近）再求导 |
| 12 | 中心 $r=0$ 用普通差分 | 伪奇点、精度崩塌 | 半控制体 FVM（首选）或 L'Hôpital |
| 13 | result4 域外位置填 0 | **语义完全错误**（0 = 完全干燥） | 按模板留空 |
| 14 | result4「药材表面」列固定取 2 cm | 与移动边界不符 | 该列随 $R(t)$ 变化 |
| 15 | 置信区间/情景区间混用 | 误导 | §9.4，三种区间分开命名 |
| 16 | 为用完文献池而增加无关段落 | 稀释重点 | §11.3，R03/R15/R19 按需保留 |

---

## 附录 A · 符号表

| 符号 | 含义 | 单位 | 备注 |
|---|---|---|---|
| $r$ | 径向坐标 | m | |
| $z$ | 轴向坐标 | m | 二维扩展用 |
| $t$ | 时间 | s | |
| $R(t)$ | 当前半径 | m | $R_0=0.02$ m |
| $L_0$ | 药材长度 | m | 0.25 m；端面尺度用 $L_0/2$ |
| $T$ | 药材温度 | °C（D 公式中须为 K） | |
| $T_\infty$ | 烘房温度 | °C | 附件1 |
| $C$ | 药材**干基含水率** $m_w/m_d$ | kg/kg | 初值 2.55 |
| $Y$ | 空气**含湿量** $m_v/m_{da}$ | kg/kg | **与 C 不可混用** |
| $C_\infty^{eq}$ | **等效**环境平衡含水率 | kg/kg | 基准须声明 |
| $\rho$ | 湿物料体积密度（附录给出） | kg/m³ | 用于热容项 |
| $\rho_d=\rho/(1+C)$ | 干固体密度 | kg/m³ | 用于一致性诊断 |
| $c_p$ | 比热容 | J/(kg·K) | |
| $k$ | 热传导系数 | W/(m·K) | |
| $D$ | 水分有效扩散系数 | m²/s | |
| $h$ | 对流换热系数 | W/(m²·K) | 25 |
| $h_m$ | 对流传质系数 | m/s | $8\times10^{-7}$ |
| $\lambda$ | 汽化潜热 | J/kg | **按温度查证** |
| $J_w$ | 向外的水质量通量 | **kg/(m²·s)** | 潜热项用此量 |
| $\alpha=k/(\rho c_p)$ | 热扩散率 | m²/s | |
| $\xi=r/R(t)$ | 归一化径向坐标 | — | 问题4 |
| $v_r=(\dot R/R)r$ | 材料径向速度 | m/s | 均匀仿射收缩 |
| $t_*$ | 阈值通过时刻 | s | $\inf\{t:C_{max}<0.15\}$ |
| $C_{max}(t)$ | 全域最大含水率 | kg/kg | 达标判据 |
| $e_d(t)$ | 几何—密度一致性诊断量 | — | §7.3 |
| $Bi_T,\ Bi_m$ | 传热、传质毕渥数 | — | 1.389 / 3.24 |

---

## 附录 B · 落实顺序

1. **先修正** C/Y 定义、ρ 定义、收缩一致性；**删除所有未经验证的确定性数值结论**
2. **完成 M0 四问**，保存题设规定结果及数值验证记录
3. 做**几何—密度诊断**与少量参数/边界情景；有资源做**端面对照**
4. **潜热通量闭合后**再做 M1，不凭任意密度或平衡浓度增加潜热
5. 品质、系统与力学推广**写清"需要什么数据才能算"**，
   **不将候选模型写成已完成模型**
6. **每一条引文检查"具体支持哪个句子"**——
   把作者的结果、本题算式和人为情景**分开**

> **最终应能回答**：本题算出了什么、哪些结论通过了检验、
> 哪些效应仅作了量级分析、哪些仍是待补数据的推广。
>
> 21 项因素的价值在于**明确模型边界和验证重点**，
> 而不是让四问主模型一次性承担 21 套不可辨识的新参数。
