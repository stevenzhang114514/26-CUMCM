# A 题「药材的烘干问题」三人三天分工

> **配套文件**：《A题_代码与插图清单_v2.md》（代码 / 插图 / 表格的编号与验收标准）
>
> **成员代称**：甲 · 乙 · 丙。Day 1—Day 3 为**相对日期**，不代表官方比赛起止时间。
>
> **本文只安排工作，不含实现代码。**

---

## 一、角色与职责边界

| 成员 | 主责 | 独立交付 | 交叉复核 |
|---|---|---|---|
| **甲** | 公共内核、问题1—3、事件定位 | 固定域求解结果、误差与迭代记录 | 复核乙的收缩算子与固定半径对照 |
| **乙** | 方程与单位审核、半径数据、问题4、验证 | 收缩模型、result4 源数据、基准与对照报告 | 复核甲的边界、公式与阈值条件 |
| **丙** | 附件/模板、导出、图表、论文整合 | 四个 Excel、题设六表、图集、论文 | 核对源结果、图表、论文与文件一致性 |

**文件所有权**（避免多人同时改同一文件）：

- 甲维护 `numerics/` 与 `models/p1~p3`
- 乙维护 `models/p4` 与 `validation/`
- 丙维护 `data_prep/`、`io/`、`figs/`、`report/`

> 乙需要修改公共接口时，**先与甲确定再实施**。

---

## 二、Day 1 上午必须冻结的接口

这是全天任务的前置条件，**三人共同确认后才各自开工**：

| 类别 | 冻结项 |
|---|---|
| 单位 | 时间 **s**；距离 **m**；温度接口单位（**°C 还是 K，必须写死**） |
| 数组 | 场数组轴顺序；**节点型 / 单元中心型**约定 |
| 函数签名 | 半径函数 `R(t)`；边界函数 `T_inf(t)`、`C_inf(t)` |
| 输出 | 输出采样规则；result 文件的 sheet / 列 / 格式 |
| 运行 | **配置标识**（每次运行唯一，写入所有结果文件） |

**对外结果的最小字段**：时间轴 · 空间轴 · 温度 · 含水率 · 半径 · 收敛状态 · 停止原因

**每次结果交接必须附带**：配置与版本标识 · 计算网格 · 时间精度设置 · 覆盖时间 · 最大残差 · 验证状态

> ⚠️ 丙**只用已标记的结果**生成正式图表。
> **模型修改后，受影响结果全部作废重跑。**

---

## 三、甲 —— 数值计算负责人

| 时段 | 代码 | 图 | 交付物 |
|---|---|---|---|
| **D1 上午** | `CODE-01` 配置中心与单位约定（与乙共同冻结接口）<br>`CODE-06` FVM 离散内核（**单一可靠实现**）<br>`CODE-07` 物性求值 + 三档 D 关键点核算 | — | 可被乙调用的公共内核；接口规格 |
| **D1 下午** | `CODE-08` 隐式时间推进（**稳定性按选定算子判定**）<br>`CODE-09` 非线性迭代（归一化残差 + 失败缩步）<br>跑简单基准 | `FIG-08` 有限体积离散示意图 | 内核基准运行记录 |
| **D1 晚间** | `CODE-10` 问题1 温度场<br>`CODE-11` 问题1 水分场<br>交付粗细网格结果 | `FIG-41` 迭代次数/步长历史（草稿） | **result1 源数据** |
| **D2 上午** | `CODE-12` 问题2 双向耦合求解<br>`CODE-13` 边界条件模块（潜热项关闭）<br>`CODE-15` 阈值通过时间求解器初版<br>启动长时积分 | `FIG-16` 无量纲数图 | 问题2 稳定运行记录 |
| **D2 下午** | 获取问题3 首版时长<br>检查事件定位与末时刻场<br>启动细网格验证 | `FIG-17` 变物性四联图<br>`FIG-19` 全流程云图（草稿） | **result3 源数据**；问题3 首版时长 |
| **D2 晚间** | `CODE-19` 问题1—3 空间/时间误差检查（**分开细化**）<br>`CODE-25` D 系数、传质系数少量扰动 | `FIG-15/40` 收敛图（**横轴标注核对**） | 误差检查记录；敏感性初版 |
| **D3 上午** | 最终配置补跑必要计算<br>`CODE-19` 时间/网格误差表<br>确认停止时刻 | `TAB-05` 收敛表 | 冻结的数值结果；版本标识 |
| **D3 下午** | 核对论文中的数值方法、参数与最终数值 | — | 方法章节定稿意见 |
| **D3 晚间** | 配合 `CODE-30` 复核复现入口，支持必要修复 | — | 复现验证记录 |

### 甲的关键提醒

- **D1 下午**：稳定性上限**不要套用直角坐标的 0.74 s**。圆柱中心节点中心格式约
  $\Delta r^2/(4\alpha)$，Δr = 0.5 mm 时约 **0.3701 s**；且**实际限制须按选定算子确定**。
- **D1 下午**：验收**不是**「必须观察到发散」，而是**检查稳定性条件，不满足即拒绝该设置**。
- **D2 晚间**：空间与时间误差**分开细化**。后向欧拉时间**一阶**，CN 在光滑条件下**二阶**，
  **不要求所有误差都二阶**。双对数图横轴为**步长 h** 时斜率应为 **+2**，横轴为 1/h 或 N 才是 −2。
- **D2 晚间**：敏感性**不预设「D 最敏感、h 最不敏感」**，作为待检验猜想处理。

---

## 四、乙 —— 物理模型与验证负责人

| 时段 | 代码 | 图 | 交付物 |
|---|---|---|---|
| **D1 上午** | 与甲共同冻结接口<br>**核对原始方程、边界、浓度与密度基准**<br>（明确 ρ 用于热容/导热，ρ_d 用于水质量与干物质守恒，**不设开关**）<br>推导收缩坐标关系（材料速度、守恒口径） | — | 假设说明；收缩坐标推导 |
| **D1 下午** | `CODE-04` 半径拟合（PCHIP / 单指数趋近 / 样条）<br>`CODE-05` 外推框架（**不预判风险**）<br>搭建 `CODE-14` 问题4 模块 | `FIG-31` 坐标变换示意图<br>`FIG-33` R(t)、R′(t)、收缩率 | 半径拟合函数；问题4 模块骨架 |
| **D1 晚间** | `CODE-20` 通量收支检查（**面积口径一致**）<br>复核 `CODE-06` 中心/边界离散<br>`CODE-14` 初步运行 | `FIG-38` 质量守恒残差 | 离散与通量检查报告；**问题4 不再是待办事项** |
| **D2 上午** | `CODE-14` 问题4 联调<br>**先用相同物性做固定半径一致性检验**<br>再运行收缩模型 | `FIG-44` 坐标变换一致性检验（初稿） | 收缩模型稳定运行记录 |
| **D2 下午** | 获取问题4 首版时长<br>检查越界位置与表面重构<br>`CODE-24` 运行 B 方案 | `FIG-32` 材料速度场示意 | **result4 源数据**；问题4 首版时长 |
| **D2 晚间** | `CODE-18` 解析基准（三条件齐备）<br>`CODE-21` 物理合理性检查<br>`CODE-23` 退化检验（**P0，三组条件写准**）<br>`CODE-24` A/B/C 比较 | `FIG-14` 数值 vs Bessel<br>`FIG-45` 退化检验汇总 | 基准与对照报告；A/B/C 初版 |
| **D3 上午** | `CODE-28` 坐标变换一致性检验（**等价性，非模型分歧**）<br>`CODE-05` 半径外推敏感性（**仅超范围时**）<br>`CODE-20` 守恒口径复核 | `FIG-39` 能量守恒残差 | 最终验证记录 |
| **D3 下午** | 核对物理假设、结论边界与 A/B/C 解释 | — | 物理章节定稿意见 |
| **D3 晚间** | 抽查四问关键点及终止判据，确认修复未破坏一致性 | — | 抽查记录 |

### 乙的关键提醒

- **D1 下午**：$R_\infty+(R_0-R_\infty)e^{-kt}$ 是**单指数趋近模型，不是双指数**。
  求导方式不限（拟合函数解析求导 / 误差受控的数值导数均可），
  **唯一禁止**对噪声原始点直接差分；**不做「导数相对差 < 10%」这类不稳健的断言**。
- **D1 晚间**：**面积口径必须一致**——整根圆柱侧面积是 $2\pi RL$；
  用 $2\pi R$ 时**必须明确是「单位长度总量」**。
- **D2 晚间**：退化检验**三组条件写准**（这是 P0）：
  1. 问题2 → 问题1：不仅固定热学物性，**还要把 D 换成附录2 的 $D(C)$**（**附录2 的 D 也不是常数**），
     并统一初值、环境边界与传递系数
  2. 问题4 固定半径 + 附录4 物性 → 退化为 **B 方案**，**不等于**问题3
  3. 纯导热基准：需**同时**固定物性、关闭蒸发、满足解析解要求的环境边界
- **D3 上午**：`CODE-28` 是**一致性检验**——同一物理模型在两种坐标下**应当等价**；
  不等价说明推导或离散有误，**不是「模型不同」**。

---

## 五、丙 —— 数据交付与论文负责人

| 时段 | 代码 | 图 | 交付物 |
|---|---|---|---|
| **D1 上午** | 清点附件并读取模板<br>`CODE-31` 输出模板解析器<br>`CODE-02` 环境插值<br>`CODE-03` 阶段识别（解释性）<br>形成**数据规格表 + 缺失项列表** | — | 数据规格表；附件核实结论 |
| **D1 下午** | `CODE-16` 采样/重映射（**验证采样误差与无过冲**）<br>`CODE-17` Excel 导出与回读自检<br>用**模拟结果**测试整条输出管线 | `G1` 几何/边界/路线合图<br>`G2` 环境数据及插值<br>`G3` 半径数据与拟合（等乙） | 可独立验收的输出管线；论文骨架 |
| **D1 晚间** | 生成 result1<br>`CODE-32` 题设表1/2<br>撰写问题1 模型与求解说明 | `G4` 问题1 温度与含水率演化双图 | **result1 首版**；表1/2 |
| **D2 上午** | 导出 result2、题设表3/4<br>撰写问题2 章节<br>**核实完整输出覆盖时长** | `G5` 问题2 前3小时径向结果 | result2；表3/4 |
| **D2 下午** | 导出 result3、result4、题设表5/6<br>统一四问表格与单位 | `G6` 问题3 最大含水率/阈值/终态<br>`G7` 问题4 物理坐标与移动边界 | **四个结果文件齐全**；表5/6 |
| **D2 晚间** | 核对四问数据一致性<br>形成完整论文初稿 | `G8` 基准误差与收敛<br>`G9` A/B/C 比较 | **含四问结果、检验与局限的完整论文初稿** |
| **D3 上午** | 从**冻结结果**重新生成全部 Excel、六表与主文图<br>核对文件规格 | `G10` 关键参数敏感性<br>全部图统一字体/单位/图例，导出 PDF+PNG | 最终 Excel 与图表 |
| **D3 下午** | 完成摘要、结论、图题及排版<br>检查表格、引用与符号 | — | 完整论文 |
| **D3 晚间** | `CODE-30` 最小复现材料与运行记录<br>回读四个 Excel；检查论文显示<br>整理、备份、交付包 | — | 交付包 |

### 丙的关键提醒

- **D1 上午**：**读模板文件反向确认规格**，不要凭题面文字猜测。
  附件点数、时间跨度、模板起点、末列名称等**均为待核实信息**，
  核实结果回填数据规格表，**差异记入风险表**。
- **D1 下午**：**输出插值不自动守恒**。题目需要**点值采样**，
  验证**采样误差与无过冲**；**三次插值不天然保正**。
- **D3 上午**：从**冻结结果**重建，不要手工改表。
  **结果变更时：先更新配置标识和结果 → 自动重建表图 → 最后改论文**，
  **禁止只手改论文中的干燥时间**。

---

## 六、每日共同检查点

| 日 | 检查点 |
|---|---|
| **D1** | 冻结单位、接口与基础假设 · 确认缺失附件是否阻塞计算 · 公共内核可被乙调用 · 输出管线可独立验收 · **result1 可打开且与源数据一致** · 问题4 不再只是待办事项 |
| **D2** | 固定域与收缩域均有稳定运行记录 · **四个结果文件齐全，不能仍缺问题4** · 明确哪些结果可靠、哪些需重跑 · **冻结基础模型** |
| **D3** | 冻结最终数值结果并记录版本 · 完整论文交叉通读 · 删除无结果支持的结论 · 三人按自检表签核 |

**每天两次简短同步**：上午确认依赖与阻塞；晚间按实际交付物验收。

> **进度以「可运行、可读取、通过何种检查」描述，不以「完成百分之多少」代替证据。**

### Day 1 交付
基础假设说明 · 接口规格 · result1 首版 · 表1/2 · 问题4 推导及初步模块 · 论文骨架
> 若基准不通过，**先修内核，不扩大实验**。

### Day 2 交付
四个 Excel 首版 · 题设六表 · 完整论文初稿 · 主模型验证记录 · A/B/C 对照初版
> 此时**优先解决错误，不增加新的物理扩展**。

### Day 3 交付
最终论文 · result1—4.xlsx · 题设六表 · 采用的图文件 · 最小复现材料与运行记录
> 最后至少留出约 **3 小时**用于修复、备份和提交准备，**不安排新的长耗时实验**。

---

## 七、依赖关系与延期处理

**主路径**：
附件确认 → 公共内核 → 问题1 验证 → 问题2/3 与问题4 **并行** → 四份结果 →
必要验证 → 冻结数值 → 图表与论文最终核对

| 风险 | 处理 |
|---|---|
| **Day 1 内核延迟** | 甲乙共同定位错误；丙继续模板、示意图与论文文字；**取消第二套数值内核** |
| **Day 2 四问未齐** | **停止 P1/P2 扫描**，三人集中完成缺失模型、验证与导出 |
| **长时计算较慢** | 自适应隐式步长，按所需时刻采样；**不因输出每 1 s 就强制全程固定 1 s 步长** |
| **附件缺失** | 继续推导、接口、基准与论文骨架，明确阻塞项；**不得用未经核实的数字或人为数据替代正式附件结果** |
| **结果变更** | 先更新配置标识与结果 → 自动重建表图 → 再改论文；**禁止只手改论文中的干燥时间** |

---

## 八、提交前三人签核

- [ ] **甲**：数值方法、参数、最终数值与论文一致；复现入口可用
- [ ] **乙**：物理假设、结论边界、A/B/C 解释无误；四问关键点抽查通过
- [ ] **丙**：四个 Excel 可重新打开；六表与源结果一致；图表与论文显示正常
- [ ] **三人**：附件已核实 · 退化检验通过 · 收敛性已报告（空间/时间分开）·
      达标判定用最大含水率且基于未舍入数据 · 结果与论文同一配置版本
