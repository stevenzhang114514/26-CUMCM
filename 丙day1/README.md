# 丙 day1 —— 数据交付 · 规格表 · 出图

> 上游：`../A题题目和附件/`（题目原件）
> 并列：`../甲day1` · `../甲day2` · `../甲day3` · `../甲做第四问` · `../乙day3` · `../乙day12`
> 本目录原名 `project/`，与其他成员的并列命名不一致，已改名。

## 一分钟定位

| | |
|---|---|
| **职责** | 题目附件的解析与模板规格、环境数据插值、阶段识别、**题设表 1–6**、**论文插图 G1–G12** |
| **权威边界** | 本目录 **不含**任何四个问题的数值答案；答案在甲的目录里 |
| **已剔除** | 6 份与甲逐字节相同的副本 + 4 份空白占位 + 2 份题目附件副本 → 见 [`docs/00_被剔除副本清单.md`](docs/00_被剔除副本清单.md) |

## 目录

```
丙day1/
├── docs/00_被剔除副本清单.md   ← 先看这份：哪些是副本、哪些是被推翻的旧值
├── report/     TAB-01…TAB-12、表1…表6 CSV、入口检查、数据规格表、复现记录
├── io/         code16 采样 · code17 导出 · code30 复现 · code31 模板解析 · code32 表格
├── data_prep/  code02 环境插值（faithful/smoothed 双版本）· code03 阶段识别
├── figs/       21 张论文图的 PNG/PDF + 10 个出图脚本（**版本存档，非最终版**）
├── data/       env_interp.csv · env_interp_smooth.csv · output_spec.json · result*_template.xlsx
├── validation/ check_output.py
└── _superseded/  非权威副本（保留可追溯，勿引用）
```

## ⚠️ 引用纪律

1. **四问答案一律引甲的目录**，不要引本目录（本目录本来也没有）：
   - 问题1 → `甲day1/results/M0/result1.xlsx`
   - 问题2/3 → `甲day2/results/M0/result{2,3}.xlsx`
   - 问题4 → `甲做第四问/results/result4.xlsx`
2. **`figs/` 里的图不是最终版**。论文用图已由 `../论文/scripts/` 用统一 6 色板重新生成，
   输出到 `../论文/figs/`。其中 **`G7`、`G9` 的数据已被推翻**
   （基于乙的 `t_dry = 73.03 h`，实为空间网格 N=20 欠分辨所致，正确值 51.0905 h），
   **不得直接引用**。
3. **`data/result*_template.xlsx` 是空模板**（由题目附件3 解析而来），
   标题行与距离轴格式可直接复用；其中 `result4_template.xlsx` 的末列是
   字符串「药材表面」、距离轴只到 1.9 cm，与其余三份（21 列到 2.0 cm）**不同**。
