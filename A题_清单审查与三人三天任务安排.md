# A题：清单审查与三人三天任务安排

## 一、审查结论与使用范围

原清单的优点是覆盖了数据、四问模型、数值验证、图表和结果导出，并特别关注了全域达标、温度单位、收缩后的表面位置等关键细节。建议保留整体结构，但先修正模型判断和验收条件，再压缩工作量。

本文件依据提供的《A题_代码与插图清单.md》、此前解题框架及题目正文审查；未独立读取清单所引用的另一份补充框架，也未取得附件1—3。因此，清单中关于附件点数、时间跨度、模板起点、末列名称的说法均为待核实信息，不作为已验证事实。

排期沿用原清单的 **3天**，以甲、乙、丙代指三位成员；Day 1—Day 3为相对日期，不代表官方比赛起止时间。本文仅安排工作，不提供实现代码。

## 二、必须修改的问题

### 2.1 模型分歧应先推导，不能一律做开关

| 原清单位置 | 存在的问题 | 建议修改 |
|---|---|---|
| D2、CODE-14/28、FIG-44 | 将收缩对流项设为开关，再按数值差异判断模型 | 先明确坐标、材料速度与守恒关系。同一物理模型在两种坐标下应等价；不同材料运动假设才属于不同模型。将CODE-28改为“坐标变换一致性检验” |
| D4、CODE-07 | 将总密度与干基密度设为可互换开关 | 取消该开关。两者定义不同，必须明确各自用于热容、水质量或干物质守恒的哪一部分 |
| D1、CODE-13/27 | 潜热对照被规定为必做，且“影响大则必须保留” | 潜热模型需要可靠的潜热、质量通量和浓度基准。资料不足时保留题设基础模型，将扩展列为P2；结果差异大只能说明模型敏感，不能单独证明扩展正确 |
| D3、CODE-06 | 基础内核强制同时实现FVM和FD | 先完成一种可靠实现。解析基准、退化检验和收敛检验通过后，第二套实现再作为P2 |

收缩项的关键关系可以写为：

$$
\left.\frac{\partial C}{\partial t}\right|_r
=\left.\frac{\partial\widetilde C}{\partial t}\right|_\xi
-\frac{\dot R}{R}\xi\frac{\partial\widetilde C}{\partial\xi},
\qquad \xi=\frac r{R(t)}.
$$

如果物理方程采用材料导数，且材料速度为 $v_r=(\dot R/R)r$，材料输运项与上述坐标项相消。如果原方程仅采用固定空间位置的偏导数，变换后会出现坐标项。两种写法不能在未交代材料运动的情况下互相替换。

### 2.2 修正退化检验和“交叉验证”的含义

1. **问题2退化为问题1：**不仅要固定热学物性，还要将扩散系数替换为附录2的 $D(C)$，并统一初始条件、环境边界和传递系数。附录2的扩散系数本身不是常数。
2. **问题4固定半径：**保留附录4物性时，应退化为“A/B/C对照中的B方案”，不能直接等于使用附录3的原问题3。若要与问题3一致，还需统一物性等全部条件。
3. **原问题1与原问题2在1800 s的对比：**属于模型差异分析，不能当作两套实现必须相等的正确性验证。
4. **纯导热解析基准：**需同时固定热学物性、关闭蒸发热效应，并满足解析解要求的环境边界。

建议将CODE-22改名为“问题1与问题2差异分析”；将CODE-23的上述退化检验升为P0。

### 2.3 数值验收条件需要纠正

| 项目 | 原清单问题 | 正确处理 |
|---|---|---|
| CODE-01：扩散系数范围 | 要求所有D落在 $10^{-11}$ 至 $10^{-8}$，与公式不符 | 在 $C=0.05$ 时，附录2给出约 $1.3021\times10^{-16}\ \mathrm{m^2/s}$。按公式、状态范围和温度分别核对，不能用统一数量级硬断言 |
| CODE-07：关键点数值 | 附录3在 $C=2.55,T_K=300$ 的值略有偏差 | 应约为 $5.3718662\times10^{-9}$；在 $C=0.15,T_K=323$ 时约为 $7.9570613\times10^{-10}$。温度接口必须标清°C或K |
| CODE-08：显式稳定性 | 直接套用一维直角坐标上限0.74 s | 圆柱中心离散、边界和变系数会改变限制。常见节点中心格式在中心处给出约 $\Delta r^2/(4\alpha)=0.3701$ s；实际限制应依据选定离散算子确定 |
| CODE-08：必须发散 | 要求步长1 s时一定出现负温度或振荡 | 不稳定不保证在某次短运行中出现该现象。应检查稳定性条件；不满足时拒绝该设置，不以“必须观察到发散”为验收 |
| CODE-08：预热步长 | 强制隐式方法预热段不超过1 s | 输出间隔不等于计算步长。用误差控制和边界变化决定步长，必要时在输出时刻精确落点 |
| CODE-09：非线性迭代 | 固定要求Newton不超过5次；预设D跨7个数量级 | 使用经过尺度归一的残差、更新量与最大迭代数；失败则缩步重试。D变化范围应由实际状态确定，不默认对log D线性化就能改善条件数 |
| CODE-19、FIG-15 | 误差对网格步长图要求斜率−2 | 若横轴为步长 $h$ 且误差 $E\propto h^2$，斜率应为 **+2**；若横轴为 $1/h$ 或网格数N，则为−2 |
| CODE-19 | 把所有误差都要求二阶收敛 | 后向欧拉时间一阶，Crank–Nicolson在足够光滑等条件下时间二阶。空间、时间应分开细化，避免一个误差掩盖另一个 |
| CODE-15/19 | 事件算法差1 s与网格误差允许0.5 h并列，易误解 | 分开报告事件定位、空间离散和时间积分误差；定位到1 s不代表干燥时间整体准确到1 s |

### 2.4 数据拟合与输出要求应避免过度约束

- **附件信息待核实：**241点、145点、72 h、58 h平台化、模板从1 s/60 s起等，均需实际读取后确认。
- **阶段分界不能预设：**删除“必须落在9000—14000 s”。若连续环境函数已足够描述边界，阶段识别可以仅用于解释，无须成为求解必需模块。
- **插值导数要求需匹配方法：**分段线性插值允许导数跳变；原始数据存在拐点时，导数变号也不必然错误。验收应关注过点、范围、形状和外推行为。
- **修正公式名称：**$R_\infty+(R_0-R_\infty)e^{-kt}$ 是单指数趋近模型，不是双指数。
- **半径拟合不应比较“导数差必须低于10%”：**平台段导数接近零时相对差不稳健。应检查拟合误差、物理约束，以及最终干燥时间对拟合选择的敏感性。
- **解析求导不是唯一途径：**可以求拟合函数或分段多项式的导数，也可使用经过误差控制的数值导数。避免对噪声原始点直接差分即可；均匀收缩的材料坐标主模型未必需要显式使用 $R'(t)$。
- **不预判外推风险低：**只有超出数据覆盖时间时才执行外推分析，并由结果量化影响。
- **计算网格不禁止0.1 cm：**能否采用该步长应由收敛检验决定。
- **输出插值不自动守恒：**题目需要点值采样，宜验证采样误差和无过冲；只有重映射控制体平均量时才要求保守映射。三次插值也不天然保正。
- **FVM未必有边界节点：**先确定节点型还是单元中心型。后者通常需要由边界条件重构中心、表面值，不能要求一律“直接取边界节点”。
- **非线性物性的取值要与离散设计一致：**节点值并非一概错误，“算术平均即二阶”也需光滑性及一致重构等条件。界面通量和边界处理应整体验证。

### 2.5 守恒、阈值和结论需要更严谨

**质量与能量守恒：**若采用实际水质量，应使用 $M_w=\int\rho_d C\,\mathrm dV$。表面通量积分中的面积要与总量口径一致：整根圆柱侧面积是 $2\pi RL$，使用 $2\pi R$ 时应明确计算单位长度总量。变物性模型不宜直接将 $\rho c_pT$ 的差当作完整内能变化；优先检验所解方程的积分收支，严格热力学检验另行定义一致的能量状态。

**单调性：**删除问题1“总水量必须下降”这一无条件断言。在环境可能造成回湿时，需按边界通量和初值判断；不要与CODE-21的提醒相互矛盾。

**严格阈值：**连续模型的阈值交点通常满足 $C_{\max}=0.15$，而不是严格小于。报告阈值通过时间，并单独验证其后一个受控小时间增量的全场值；若输出规定最早整数秒达标，则采用该规则并注明。不能在事件根处强制断言严格小于。

**可行性：**最终平衡值不低于阈值，是持续稳定达标的障碍；但若前期环境更干，不能据此排除曾经短暂达标。只有附加恒定边界或单调趋近平衡等条件，才能提前判定不存在首次达标时间。

**2—3天：**是背景描述，不是必须通过的数值验收区间。偏离时检查数据、单位、边界和误差，不应为了落入区间而调参。

**A/B/C比较：**保留该设计，但称为“按A→B→C路径的顺序差分”，不称为唯一、独立的因果贡献。若需分析交互，可追加“收缩尺寸+附录3”的第四方案，降为P2。

**敏感性：**删除预设“D最敏感、h最不敏感”的结论，将其作为待检验猜想。扰动幅度需有解释；不可达标的扰动方案单独标记，不能强行填有限时长。

## 三、调整后的优先级与工作规模

### 3.1 P0：必须完成

- 附件、公式、单位、边界与模板规格核实。
- 一套统一数值内核，以及四问模型、阈值检测和输出处理。
- 问题4的收缩推导和固定半径一致性检验。
- 一个常系数基准，基本网格/时间误差检查，非负性及离散通量收支检查。
- result1—4.xlsx、题设表1—6、关键结果图和完整论文。
- 输出文件重新读入核对，最小复现说明与固定配置记录。

将CODE-17′论文表格生成、CODE-19的基本收敛检验、CODE-21合理性检查、CODE-23退化检验和CODE-30最小复现升为P0。建议将带撇编号统一改成CODE-31（模板解析）、CODE-32（论文表格）、CODE-33（严格干基扩展），减少文件名与编号混淆。

### 3.2 P1：主结果稳定后完成

- A/B/C三方案对照。
- 少量关键参数和必要的环境/半径外推敏感性。
- 更细的误差量化、模型差异讨论和验证图。

### 3.3 P2：不占用交付时间

- 第二套FD内核、二维端面模型。
- 有充分参数依据的潜热扩展、严格干基质量守恒扩展。
- 完整参数扫描、第四方案交互分析和程序结构展示图。

原47张图作为候选库保留，不要求全部进入正文。主文建议约10组图：

| 新图组 | 内容 | 原编号参考 | 优先级 |
|---|---|---|---|
| G1 | 几何、边界和四问路线合图 | FIG-01/02/03 | P0 |
| G2 | 环境温度与浓度数据及插值 | FIG-04 | P0 |
| G3 | 半径数据、拟合与误差 | FIG-06/33 | P0 |
| G4 | 问题1温度与含水率演化双图 | FIG-09/10，径向图酌情替换 | P0 |
| G5 | 问题2前3小时径向结果 | FIG-21 | P0 |
| G6 | 问题3最大含水率、阈值与终态分布 | FIG-24/27 | P0 |
| G7 | 问题4物理坐标含水率及移动边界 | FIG-34 | P0 |
| G8 | 基准误差与网格/时间收敛 | FIG-14/15/40 | P0 |
| G9 | A/B/C最大含水率与干燥时间比较 | FIG-36/37 | P1 |
| G10 | 关键参数敏感性 | FIG-28/29 | P1 |

温度和含水率尽量分子图展示，避免双纵轴造成视觉误读。图使用统一字体、单位和图例；保存PDF矢量版与PNG即可，除非排版流程需要，不必额外制作EPS。

## 四、三人职责与接口

| 成员 | 主责 | 独立交付 | 交叉复核 |
|---|---|---|---|
| 甲：数值计算负责人 | 公共内核、问题1—3、事件定位 | 固定域求解结果、误差与迭代记录 | 复核乙的收缩算子和固定半径对照 |
| 乙：物理模型与验证负责人 | 方程和单位审核、半径数据、问题4、验证 | 收缩模型、result4源数据、基准和对照报告 | 复核甲的边界、公式与阈值条件 |
| 丙：数据交付与论文负责人 | 附件/模板、导出、图表、论文整合 | 四个Excel文件、题设六表、图集和论文 | 核对源结果、图表、论文和文件的一致性 |

甲维护公共数值模块，乙维护收缩模块与验证模块，丙维护数据、导出、绘图和论文，避免多人同时改同一文件。乙需要修改公共接口时，先与甲确定再实施。

**Day 1上午冻结接口：**时间单位s、距离单位m、温度接口单位、场数组轴顺序、节点/单元中心约定、半径函数、边界函数、输出采样规则，以及每次运行的配置标识。对外结果至少包含时间轴、空间轴、温度、含水率、半径、收敛状态和停止原因。

每次结果交接必须附带：配置与版本标识、计算网格、时间精度设置、覆盖时间、最大残差和验证状态。丙只用已标记的结果生成正式图表；模型修改后，受影响结果全部作废重跑。

## 五、每日任务分配

### Day 1：冻结基础模型，完成问题1，提前启动问题4

| 时段 | 甲 | 乙 | 丙 | 共同检查点 |
|---|---|---|---|---|
| 上午 | 确定FVM网格与求解接口；实现物性求值及关键点核算 | 核对原始方程、边界、浓度和密度基准；推导收缩坐标关系 | 清点附件并读取模板；形成数据规格表、环境插值和缺失项列表；搭建论文目录 | 冻结单位、接口和基础假设；确认缺失附件是否阻塞计算 |
| 下午 | 完成固定域扩散内核、隐式推进和非线性迭代；跑简单基准 | 拟合半径；搭建问题4模块；准备常系数基准及固定半径测试 | 用模拟结果测试采样、Excel导出与回读；制作G1/G2/G3；起草数据和假设章节 | 公共内核可被乙调用；输出管线可独立验收 |
| 晚间 | 完成问题1温度与含水率计算；交付粗细网格结果 | 检查中心和边界离散、通量及基本收敛；完成收缩模块初步运行 | 生成result1、表1/2、G4；填入问题1模型与求解说明 | result1可打开且与源数据一致；问题4不再只是待办事项 |

**Day 1交付：**基础假设说明、接口规格、result1首版、表1/2、问题4推导及初步模块、论文骨架。若基准不通过，先修内核，不扩大实验。

### Day 2：跑通四问，形成完整初稿

| 时段 | 甲 | 乙 | 丙 | 共同检查点 |
|---|---|---|---|---|
| 上午 | 完成问题2耦合求解及问题3停止判据；启动长时积分 | 完成问题4联调；先用相同物性做固定半径检验，再运行收缩模型 | 导出result2、表3/4、G5；撰写问题2内容；核实完整输出覆盖时长 | 固定域和收缩域均有稳定运行记录 |
| 下午 | 获得问题3首版时长；检查事件定位和末时刻场；启动细网格验证 | 获得问题4首版时长；检查越界位置和表面重构；运行B方案 | 导出result3/4、表5/6、G6/G7；统一四问表格与单位 | 四个结果文件齐全，不能仍缺问题4 |
| 晚间 | 完成问题1—3主要误差检查；运行D系数、传质系数的少量扰动 | 完成收缩模型误差检查和A/B/C比较；必要时比较半径外推 | 制作G8/G9；形成含四问结果、检验和局限的完整论文初稿 | 明确哪些结果可靠、哪些需重跑；冻结基础模型 |

**Day 2交付：**四个Excel首版、题设六表、完整论文初稿、主模型验证记录、A/B/C对照初版。此时优先解决错误，不增加新的物理扩展。

### Day 3：验证、统一与提交准备

| 时段 | 甲 | 乙 | 丙 | 共同检查点 |
|---|---|---|---|---|
| 上午 | 对最终配置补跑必要计算；完成时间/网格误差表；确认停止时刻 | 独立复核公式、坐标、守恒口径；完成必要敏感性和模型解释 | 从冻结结果重新生成所有Excel、六表及主文图；核对文件规格 | 冻结最终数值结果，记录版本 |
| 下午 | 核对论文中的数值方法、参数和最终数值 | 核对物理假设、结论边界与A/B/C解释 | 完成摘要、结论、图题及排版；检查表格、引用和符号 | 完整论文交叉通读；删除无结果支持的结论 |
| 晚间/提交前缓冲 | 复核最终运行记录与复现入口，支持必要修复 | 抽查四问关键点及终止判据，确认修复未破坏一致性 | 回读四个Excel；检查论文显示；整理、备份并准备交付包 | 三人按自检表签核；正式提交由指定成员负责 |

**Day 3交付：**最终论文、result1—4.xlsx、题设六表、采用的图文件、最小复现材料与运行记录。最后至少留出约3小时用于修复、备份和提交准备，不安排新的长耗时实验。

## 六、依赖关系与延期处理

主路径：附件确认 → 公共内核 → 问题1验证 → 问题2/3与问题4并行 → 四份结果 → 必要验证 → 冻结数值 → 图表和论文最终核对。

- 丙从第一天就编写论文和导出模块，不等待全部数值完成。
- 乙从第一天启动收缩模型，不把问题4压到第三天上午。
- Day 1内核延迟：甲乙共同定位错误，丙继续模板、示意图和论文文字；取消第二套数值内核。
- Day 2四问未齐：停止P1/P2扫描，三人集中完成缺失模型、验证及导出。
- 长时计算较慢：在误差允许范围内采用自适应隐式步长，按所需时刻采样；不要仅因输出每1 s就强制全程固定1 s步长。
- 附件缺失：继续推导、接口、基准和论文骨架，并明确阻塞项；不得用清单中的未经核实数字或人为数据替代正式附件结果。
- 结果变更：先更新配置标识和结果，再自动重建表图，最后修改论文；禁止只手改论文中的干燥时间。

每天安排两次简短同步：上午确认依赖和阻塞，晚间按实际交付物验收。进度以“可运行、可读取、通过何种检查”描述，不以“完成百分之多少”代替证据。

## 七、最终验收清单

- [ ] 附件点数、单位、覆盖范围、模板规格已实际核实。
- [ ] 附录公式及K/°C转换经过独立复核，无密度基准开关替换概念。
- [ ] 问题4材料速度、坐标变换和边界尺度因子一致。
- [ ] 相同物性与边界条件下，模型退化检验通过。
- [ ] 基准和网格/时间误差已检查，未把事件定位精度当作总精度。
- [ ] 最大含水率用于达标判定，阈值比较基于未舍入数据。
- [ ] result4仅在实际药材内部输出点值，表面随半径变化，越界按模板约定处理。
- [ ] 四个Excel能重新打开，题设表1—6与源结果一致，四位小数格式正确。
- [ ] 主文图均支持具体结论，不存在重复图、预设结论或未经解释的差异。
- [ ] A/B/C比较明确物性差异与收缩差异的顺序路径。
- [ ] 最终论文与结果使用同一配置版本，复现说明和必要数据齐全。
- [ ] 三人已分别完成数值、物理和交付核对，预留了最终修复与提交时间。
