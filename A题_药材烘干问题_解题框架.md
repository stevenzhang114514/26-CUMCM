# A题“药材的烘干问题”解题框架

## 一、总体思路

建议采用**一维轴对称热湿传递模型**，依次完成以下四层建模：

| 问题 | 核心任务 | 建模方法 |
|---|---|---|
| 问题1 | 描述预热阶段温度和含水率变化 | 常热物性参数下的径向导热与非线性水分扩散 |
| 问题2 | 描述整个烘干过程 | 温度、含水率相关的变参数热湿耦合模型 |
| 问题3 | 确定各处均达标的时间 | 延长问题2计算，设置全域含水率停止条件 |
| 问题4 | 考虑药材收缩后确定烘干时间 | 半径拟合、移动边界与坐标变换 |

四问应在同一套模型上逐步扩展，以便对比结果、检验一致性。

> 当前提供的 PDF 包含题目和经验公式，但不包含附件1的烘房环境数据、附件2的半径数据及附件3的结果模板。因此，下面给出完整建模路线，不填写数值结果，也不预设缺失数据。

## 二、统一准备工作

### 2.1 明确变量与单位

设：

- \(r\)：到圆柱中心轴的径向距离，单位为 m；
- \(t\)：时间，单位为 s；
- \(T(r,t)\)：药材温度；
- \(C(r,t)\)：药材干基含水率，单位为 kg/kg；
- \(T_\infty(t)\)、\(C_\infty(t)\)：烘房环境温度和题设水分浓度；
- \(R_0=0.02\ \mathrm m\)：初始半径；
- \(L=0.25\ \mathrm m\)：初始长度。

初始条件为：

\[
T(r,0)=28^\circ\mathrm C,\qquad C(r,0)=2.55.
\]

**温度单位必须统一处理：**导热方程可使用摄氏温度，但扩散系数经验公式中的温度必须使用

\[
T_K=T+273.15.
\]

### 2.2 基本假设

1. 药材初始温度和含水率均匀，材料近似各向同性。
2. 烘房环境在药材周围均匀，但可随时间变化。
3. 基础模型忽略轴向变化，仅考虑径向传热、传质。
4. 问题1—3保持药材尺寸不变；问题4引入径向收缩。
5. 水分迁移以题目给定的有效扩散关系描述。
6. 基础模型采用题设提供的热学参数，不额外引入无法确定的蒸发潜热项。

圆柱两端面积与侧面积之比为：

\[
\frac{2\pi R_0^2}{2\pi R_0L}
=\frac{R_0}{L}=0.08.
\]

因此，可以将忽略端面作为基础近似，但不能认为端部影响严格为零。若需加强模型可信度，可用二维轴对称模型检查端面效应对干燥时间的影响。

### 2.3 处理环境数据

对附件1中的离散数据构造连续函数：

\[
T_\infty(t),\qquad C_\infty(t).
\]

优先采用分段线性插值或保形插值，避免高阶多项式产生不合理振荡。

需特别核实：

- 预热结束和恒温阶段开始的时间；
- 恒温阶段的环境温度；
- 后续环境水分浓度是否保持不变。

**恒温不等于湿度恒定。**若附件只覆盖初期，后续环境条件必须作为明确假设，并进行敏感性分析。

此外，药材干基含水率与空气含湿量虽均可写成 kg/kg，物理基准可能不同。基础模型若直接使用 \(C_s-C_\infty\) 作为传质驱动力，应说明采用题设的等效浓度约定；若附件提供平衡含水率关系，则应先进行换算。

## 三、问题1：预热平衡阶段模型

### 3.1 温度模型

采用圆柱坐标下的一维径向导热方程：

\[
\rho c_p\frac{\partial T}{\partial t}
=
\frac{1}{r}\frac{\partial}{\partial r}
\left(
rk\frac{\partial T}{\partial r}
\right),
\qquad 0<r<R_0.
\]

使用附录2参数：

\[
\rho=820,\qquad c_p=2600,\qquad k=0.36.
\]

中心满足对称条件：

\[
\left.\frac{\partial T}{\partial r}\right|_{r=0}=0.
\]

表面采用对流换热条件：

\[
-k\left.\frac{\partial T}{\partial r}\right|_{r=R_0}
=
h_T\left[T(R_0,t)-T_\infty(t)\right],
\]

其中：

\[
h_T=25\ \mathrm{W/(m^2\cdot K)}.
\]

热学 Biot 数约为：

\[
Bi_T=\frac{h_TR_0}{k}\approx1.39.
\]

这说明径向温差不能直接忽略，采用空间分布模型比集总温度模型更合适。

### 3.2 水分模型

采用非线性扩散方程：

\[
\frac{\partial C}{\partial t}
=
\frac{1}{r}\frac{\partial}{\partial r}
\left[
rD(C)\frac{\partial C}{\partial r}
\right],
\]

其中：

\[
D(C)=7\times10^{-9}\exp\left(-\frac{0.89}{C}\right).
\]

中心条件：

\[
\left.\frac{\partial C}{\partial r}\right|_{r=0}=0.
\]

表面传质条件：

\[
-D(C_s)\left.\frac{\partial C}{\partial r}\right|_{r=R_0}
=
h_m(C_s-C_\infty(t)),
\]

其中：

\[
C_s=C(R_0,t),\qquad
h_m=8\times10^{-7}\ \mathrm{m/s}.
\]

在问题1的参数设定下，温度模型和水分模型可以分别求解。

### 3.3 求解与输出

推荐采用**径向有限体积法与隐式时间推进**：

1. 将圆柱截面划分为同心环形控制体；
2. 按真实环形体积和表面积计算通量；
3. 隐式求解温度；
4. 迭代更新 \(D(C)\)，求解含水率；
5. 提取题目规定的时间和位置结果。

有限体积法能自然处理中心位置，避免直接计算 \(1/r\) 引起的奇点。

需要输出：

- 100、300、600、900、1200、1500、1800 s 的指定径向结果；
- 按每 1 s、每 0.1 cm 采样的完整结果。

计算网格和输出网格应分开设置，不能将输出间隔直接视为足够准确的计算步长。

## 四、问题2：全程变参数热湿耦合模型

### 4.1 替换物性关系

按附录3统一采用：

\[
\rho(C)=650+128C,
\]

\[
c_p(C)=1450+2736\frac{C}{C+1},
\]

\[
k(C)=0.21+0.38\frac{C}{C+1},
\]

\[
D(T,C)
=
2.4\times10^{-3}
\exp\left(-\frac{0.45}{C}\right)
\exp\left(-\frac{3850}{T_K}\right).
\]

这些公式应在各个空间位置按局部状态计算，不能直接用平均温度、平均含水率替代。

### 4.2 建立耦合方程

基础方程为：

\[
\rho(C)c_p(C)\frac{\partial T}{\partial t}
=
\frac{1}{r}\frac{\partial}{\partial r}
\left[
rk(C)\frac{\partial T}{\partial r}
\right],
\]

\[
\frac{\partial C}{\partial t}
=
\frac{1}{r}\frac{\partial}{\partial r}
\left[
rD(T,C)\frac{\partial C}{\partial r}
\right].
\]

耦合关系体现为：

- 温度改变水分扩散速度；
- 含水率改变密度、比热容和导热系数；
- 热物性变化进一步影响温度分布。

**不能将变系数直接移出空间微分算子。**例如，\(k(C)\) 随位置变化时，不能直接把导热项写成常系数形式。

上述含水率方程属于有效扩散模型。若要进一步构造严格的干物质质量守恒模型，应单独定义单位体积干物质质量及其守恒关系，不能直接将题设总密度当作干物质密度使用。

### 4.3 处理阶段衔接

用环境边界函数描述预热与恒温阶段，阶段切换时保持状态连续：

\[
T(r,t_s^-)=T(r,t_s^+),
\]

\[
C(r,t_s^-)=C(r,t_s^+).
\]

问题2要求统一使用附录3公式，因此应从初始时刻使用该组公式完成计算，不能默认先运行问题1，再在某时刻突然替换参数。

若后续未给出新的换热、传质系数，可将沿用附录2数值列为假设。

### 4.4 数值求解流程

每个时间步内：

1. 根据当前温度、含水率更新物性；
2. 求解温度方程；
3. 更新扩散系数并求解含水率方程；
4. 重复迭代，直至温度和含水率均收敛；
5. 进入下一时间步。

提取前3小时每0.5小时的指定结果。完整结果的终止时刻应结合附件3模板核实，避免将“前3小时的论文表格”自动理解为“全程模型只需计算3小时”。

## 五、问题3：烘干达标时间

### 5.1 使用全域判据

题目要求药材**各处**含水率均低于 \(0.15\)，应定义：

\[
C_{\max}(t)=\max_{0\le r\le R_0}C(r,t).
\]

烘干完成时间定义为阈值通过时刻：

\[
t_{\mathrm{dry}}
=
\inf\left\{
t:C_{\max}(t)<0.15
\right\}.
\]

不能使用平均含水率代替最大含水率。

中心往往最后干燥，但应先检查径向分布；只有确认最大值始终位于中心后，才能用中心含水率简化判据。

### 5.2 求解步骤

1. 延长问题2的计算时间；
2. 每个计算时间步检查全域最大含水率；
3. 找到阈值两侧的时间区间；
4. 用事件定位或时间细分确定达标时间；
5. 换算为小时，并验证终止时刻所有位置均达标。

达标判断必须使用未舍入数值，不能依据四位小数后的结果。

### 5.3 检查达标可行性

在题设等效边界模型下，如果最终环境对应的平衡含水率不低于 \(0.15\)，药材可能无法在有限时间内满足要求。

因此，长时间积分前应先检查最终环境条件，不能预设一定会在2—3天内达标。

### 5.4 结果展示

建议给出：

- 中心、表面及全域最大含水率曲线；
- 含水率随时间和半径变化的分布图；
- 每6小时的指定径向结果；
- 达标时间及终止时刻含水率分布。

完整结果按每60 s、每0.1 cm输出，并补充不落在整分钟上的终止时刻。

## 六、问题4：考虑药材收缩

### 6.1 构造半径函数

根据附件2得到：

\[
R=R(t).
\]

可采用保形插值或受约束的经验拟合，重点保证：

- 半径始终为正；
- 不出现无依据的振荡；
- 收缩趋势与观测一致；
- 需要的变化率 \(R'(t)\) 合理。

若数据未覆盖实际达标时间，应明确半径外推方式，并比较不同外推方式对时长的影响。

### 6.2 将移动区域转为固定区域

引入无量纲坐标：

\[
\xi=\frac{r}{R(t)},\qquad 0\le\xi\le1.
\]

定义：

\[
\widetilde C(\xi,t)=C(\xi R(t),t),\qquad
\widetilde T(\xi,t)=T(\xi R(t),t).
\]

这样可以在固定网格上计算收缩过程。

### 6.3 明确收缩运动假设

建议采用**均匀径向收缩**假设，材料速度为：

\[
v_r(r,t)=\frac{R'(t)}{R(t)}r.
\]

在物理坐标中，对随材料移动的状态，应采用物质导数：

\[
\frac{\mathrm D}{\mathrm Dt}
=
\frac{\partial}{\partial t}
+
v_r\frac{\partial}{\partial r}.
\]

在这一假设及有效扩散近似下，变换后水分方程为：

\[
\frac{\partial\widetilde C}{\partial t}
=
\frac{1}{R(t)^2\xi}
\frac{\partial}{\partial\xi}
\left[
\xi D(\widetilde T,\widetilde C)
\frac{\partial\widetilde C}{\partial\xi}
\right].
\]

温度方程相应为：

\[
\rho(\widetilde C)c_p(\widetilde C)
\frac{\partial\widetilde T}{\partial t}
=
\frac{1}{R(t)^2\xi}
\frac{\partial}{\partial\xi}
\left[
\xi k(\widetilde C)
\frac{\partial\widetilde T}{\partial\xi}
\right].
\]

这里的简洁形式依赖于材料随网格均匀收缩的假设。若采用其他材料运动模型，需要重新推导输运项，不能只把固定半径替换为 \(R(t)\)。

干基含水率是水质量与干物质质量之比，不能因为体积缩小就直接给它乘上体积压缩倍数。若进一步要求严格质量守恒，还需引入干物质密度及其连续性方程。

### 6.4 使用附录4参数与新边界条件

物性关系替换为：

\[
\rho(C)=760+90C,
\]

\[
c_p(C)=1850+2150\frac{C}{C+1},
\]

\[
k(C)=0.12+0.20\frac{C}{C+1},
\]

\[
D(T,C)
=
4.2\times10^{-4}
\exp\left(-\frac{0.30}{C}\right)
\exp\left(-\frac{3850}{T_K}\right).
\]

表面边界变为：

\[
-\frac{k}{R(t)}
\left.\frac{\partial\widetilde T}{\partial\xi}\right|_{\xi=1}
=
h_T(\widetilde T_s-T_\infty),
\]

\[
-\frac{D}{R(t)}
\left.\frac{\partial\widetilde C}{\partial\xi}\right|_{\xi=1}
=
h_m(\widetilde C_s-C_\infty).
\]

仍使用全域最大含水率作为终止判据。

### 6.5 处理实际距离输出

计算使用 \(\xi\)，但题目要求实际径向距离，因此输出时应转换：

\[
\xi=\frac{r}{R(t)}.
\]

每个时刻：

1. 输出当前半径内的0.1 cm间隔位置；
2. 单独计算当前表面 \(r=R(t)\) 的结果；
3. 对已经位于药材外部的位置，按模板约定留空或标记无效；
4. 不得将药材外部位置填成零含水率。

表6中的“药材表面”应对应随时间变化的位置，不能始终取初始的2 cm处。

### 6.6 区分收缩与物性变化的影响

问题4同时改变几何尺寸和经验参数。若要解释干燥时间差异，建议增加对照：

| 方案 | 尺寸 | 参数 |
|---|---|---|
| A | 固定 | 附录3 |
| B | 固定 | 附录4 |
| C | 随时间收缩 | 附录4 |

A与B比较物性公式的影响，B与C比较收缩的影响。不能把问题3和问题4之间的全部时间差归因于收缩。

## 七、模型验证与误差分析

### 7.1 数值收敛性

分别缩小空间网格和时间步长，比较：

- 中心温度；
- 中心含水率；
- 全域最大含水率；
- 最终烘干时间。

四位小数是输出格式要求，不代表模型天然具有四位小数的物理精度。

### 7.2 物理合理性

重点检查：

- 含水率是否非负；
- 中心是否满足对称条件；
- 温度是否出现无依据的过冲；
- 内部变化与表面通量是否一致；
- 变参数和收缩模型是否在简化条件下退化为基础模型。

含水率是否单调下降取决于环境条件，不能在检查前强制设定所有位置始终下降。

### 7.3 守恒检查

对离散方程检查内部通量能否相互抵消，总变化是否与边界通量一致。

若使用近似的干基含水率扩散模型，应区分“离散方程守恒”与“严格的实际水质量守恒”，并说明密度和干物质基准的处理。

### 7.4 敏感性分析

优先考察：

- 扩散系数；
- 表面传质系数；
- 恒温阶段温度与环境水分浓度；
- 半径拟合及外推；
- 忽略端面传递的影响。

以最终烘干时间的变化幅度作为主要比较指标。

## 八、建议的论文组织

1. **问题分析**：说明四问的递进关系。
2. **模型假设与符号说明**：明确几何简化、单位和浓度定义。
3. **数据预处理**：说明环境插值和半径拟合。
4. **问题1模型与结果**：常热物性下的预热模型。
5. **问题2模型与结果**：变参数热湿耦合与阶段衔接。
6. **问题3达标时间**：全域判据、事件定位与结果。
7. **问题4收缩模型**：材料运动、坐标变换与实际距离输出。
8. **模型检验与评价**：收敛性、守恒性、敏感性和适用范围。

整份解答的重点应放在：**边界条件有依据、经验公式使用正确、达标判据覆盖所有位置、收缩模型与材料运动保持一致。**
