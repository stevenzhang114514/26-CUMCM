# A 题「药材的烘干问题」代码实现 + 插图位置 全清单（v2）

> **v2 修订依据**：《A题_清单审查与三人三天任务安排.md》。凡与该审查冲突之处，**一律以审查意见为准**。
>
> **配套文件**：《A题_三人三天分工.md》（甲/乙/丙逐日任务与接口冻结，本文不再重复）
>
> **前置声明**：附件1—3 的点数、时间跨度、模板起点、末列名称等**均属待核实信息**，
> 不作为已验证事实。清单中出现的具体数值一律标注「**待核实**」，须由丙在 Day 1 上午读入后确认。
>
> **编号约定**：`CODE-xx` 代码 / `G1–G10` 主文图组 / `FIG-xx` 候选图库 / `TAB-xx` 表格
> **优先级**：P0 必须完成 · P1 主结果稳定后 · P2 不占用交付时间
> **负责人**：甲 数值内核 / 乙 物理与验证 / 丙 数据交付与论文

---

## 0. v1 → v2 修订摘要

### 0.1 模型判断的修正（最重要）

| 项 | v1 做法 | v2 修正 |
|---|---|---|
| **收缩对流项** | 设为开关，按数值差异判断模型 | ❌ 取消开关。**先推导坐标、材料速度与守恒关系**。同一物理模型在两种坐标下**必然等价**，不构成模型分歧；只有**材料运动假设不同**才是不同模型。`CODE-28` 改为**「坐标变换一致性检验」** |
| **密度基准** | `rho_basis ∈ {total, dry}` 开关 | ❌ **取消该开关**。ρ 与 ρ_d 定义不同，必须明确各自用于**热容项 / 水质量守恒 / 干物质守恒**的哪一部分，不允许互换 |
| **潜热项** | 列为必做对照，P1 | ⬇️ 降为 **P2 扩展**。潜热模型需可靠的潜热、质量通量与浓度基准；资料不足时保留题设基础模型。**结果差异大只能说明模型敏感，不能单独证明扩展正确** |
| **第二套 FD 内核** | 与 FVM 强制并做 | ⬇️ 降为 **P2**。先完成一种可靠实现；解析基准、退化检验、收敛检验通过后，第二套再作补充 |

### 0.2 优先级变动

**升为 P0**：`CODE-32`（论文表格）、`CODE-19`（基本收敛检验）、`CODE-21`（合理性检查）、
`CODE-23`（退化检验）、`CODE-30`（最小复现）

**降为 P2**：`CODE-06` 第二套 FD 内核、`CODE-26` 二维端面模型、`CODE-27` 潜热扩展、
`CODE-33` 严格干基扩展、`CODE-25` 完整参数扫描、第四对照方案

### 0.3 编号调整

| v1 | v2 | 名称 |
|---|---|---|
| `CODE-06'` | **`CODE-31`** | 输出模板解析器 |
| `CODE-17'` | **`CODE-32`** | 论文表格生成器 |
| `CODE-28'` | **`CODE-33`** | 严格干基质量守恒扩展（P2） |
| `CODE-22` | `CODE-22`（改名） | **问题1与问题2差异分析** |

### 0.4 被删除的错误验收条件

- ❌「所有 D 落在 1e-11 ~ 1e-8」→ 附录2 在 C=0.05 时 D ≈ **1.3021×10⁻¹⁶**，与该断言不符
- ❌「显式格式上限 0.74 s」→ 圆柱中心节点中心格式约为 **Δr²/(4α) = 0.3701 s**
- ❌「Δt=1 s 必须观察到发散」→ 不稳定不保证在某次短运行中显现；应**检查稳定性条件并拒绝该设置**
- ❌「预热段强制 dt ≤ 1 s」→ 输出间隔 ≠ 计算步长
- ❌「Newton 迭代 ≤ 5 次」→ 固定次数无意义
- ❌「误差—步长双对数图斜率 −2」→ 横轴为 h 且 E∝h² 时斜率应为 **+2**
- ❌「所有误差均二阶收敛」→ 后向欧拉时间一阶；空间、时间须**分开细化**
- ❌「t_pre 必须落在 9000–14000 s」
- ❌「半径拟合导数相对差 < 10%」
- ❌「问题1 总水量必须下降」（无条件的单调性断言）
- ❌「t_dry 必须落在 2–3 天」
- ❌「D 最敏感、h 最不敏感」（改为待检验猜想）

---

## 1. 编号 · 负责人 · 优先级 总表

| 编号 | 名称 | 负责 | 优先级 | 日次 |
|---|---|---|---|---|
| `CODE-01` | 全局配置中心与单位约定 | 甲(主)/乙(审) | **P0** | D1上午 |
| `CODE-02` | 附件1 环境数据插值 | 丙 | **P0** | D1上午 |
| `CODE-03` | 阶段识别（解释性，非求解必需） | 丙 | P1 | D1上午 |
| `CODE-04` | 附件2 半径拟合 | 乙 | **P0** | D1下午 |
| `CODE-05` | 半径外推（仅超范围时执行） | 乙 | P1 | D1下午 |
| `CODE-06` | 一维圆柱 FVM 离散内核（单一可靠实现） | 甲(主)/乙(验) | **P0** | D1上午 |
| `CODE-07` | 物性求值模块 | 甲(主)/乙(审) | **P0** | D1上午 |
| `CODE-08` | 隐式时间推进器 | 甲 | **P0** | D1下午 |
| `CODE-09` | 非线性迭代器 | 甲 | **P0** | D1下午 |
| `CODE-10` | 问题1 温度场求解 | 甲 | **P0** | D1晚间 |
| `CODE-11` | 问题1 水分场求解 | 甲 | **P0** | D1晚间 |
| `CODE-12` | 问题2 双向耦合求解 | 甲 | **P0** | D2上午 |
| `CODE-13` | 边界条件模块（含 P2 潜热开关） | 甲(主)/乙(审) | **P0** | D2上午 |
| `CODE-14` | 移动边界 / 坐标变换内核 | 乙 | **P0** | D1下午→D2上午 |
| `CODE-15` | 阈值通过时间求解器 | 甲 | **P0** | D2上午→下午 |
| `CODE-16` | 计算网格 → 输出采样 | 丙 | **P0** | D1下午 |
| `CODE-17` | 结果导出器（四个 xlsx） | 丙 | **P0** | D1下午 |
| `CODE-18` | 解析基准解（Bessel 级数） | 乙 | P1 | D2晚间 |
| `CODE-19` | 网格 / 时间误差收敛检验 | 甲 | **P0** | D2晚间 |
| `CODE-20` | 守恒与通量收支检验 | 乙 | **P0** | D1晚间 |
| `CODE-21` | 物理合理性检查器 | 乙 | **P0** | D2晚间 |
| `CODE-22` | 问题1与问题2**差异分析** | 乙 | P1 | D2晚间 |
| `CODE-23` | 模型退化检验 | 乙 | **P0** | D2晚间 |
| `CODE-24` | A→B→C 顺序差分对照 | 乙 | P1 | D2下午→晚间 |
| `CODE-25` | 关键参数与环境敏感性 | 甲(主)/乙(佐) | P1 | D2晚间 |
| `CODE-26` | 二维轴对称端面模型 | — | **P2** | 富余时 |
| `CODE-27` | 潜热扩展对照 | 乙 | **P2** | 富余时 |
| `CODE-28` | **坐标变换一致性检验** | 乙 | P1 | D3上午 |
| `CODE-29` | 绘图脚本集 | 丙 | **P0** | D1下午起 |
| `CODE-30` | 最小复现与运行记录 | 丙(主)/甲(佐) | **P0** | D3晚间 |
| `CODE-31` | 输出模板解析器 | 丙 | **P0** | D1上午 |
| `CODE-32` | 论文表格生成器 | 丙 | **P0** | D1晚间 |
| `CODE-33` | 严格干基质量守恒扩展 | — | **P2** | 富余时 |
| `CODE-FD` | 第二套 FD 内核（交叉验证） | — | **P2** | 富余时 |

---

## 2. 工程目录结构

```
26-CUMCM/A题/
├── data/
│   ├── raw/                     附件1.xlsx 附件2.xlsx 附件3/
│   └── processed/               env_interp.pkl  R_fit.pkl  output_spec.json
├── src/
│   ├── config.py                CODE-01        【甲】
│   ├── data_prep/               CODE-02~05, 31 【丙/乙】
│   ├── numerics/                CODE-06~09     【甲】
│   ├── models/                  CODE-10~15     【甲/乙】
│   ├── io/                      CODE-16,17,32  【丙】
│   ├── validation/              CODE-18~23     【乙/甲】
│   └── experiments/             CODE-24~28,33  【乙】
├── figs/                        G1–G10 主文图 + FIG-* 候选库
├── results/                     result1~4.xlsx
├── scripts/                     CODE-29, 30, run_all.py
└── report/                      main.tex  figures/  tables/
```

> **文件所有权**：甲维护 `numerics/` 与 `models/p1~p3`；乙维护 `models/p4` 与 `validation/`；
> 丙维护 `data_prep/`、`io/`、`figs/`、`report/`。**避免多人同时改同一文件。**
> 乙需要改公共接口时，先与甲确定再实施。

---

## 3. 代码清单（详细）

### 3.1 P0 — 必须完成

#### `CODE-01` 全局配置中心与单位约定 ·【甲主 / 乙审】· D1上午
- **文件**：`src/config.py`
- **内容**：几何（R₀=0.02 m、L=0.25 m）、初值（T₀=28 °C、C₀=2.55）、三套物性公式、
  h、h_m、输出规格、**配置标识（每次运行唯一）**
- **要点**：
  - **温度接口必须标清 °C 还是 K**，`T_K = T + 273.15` 只在此模块内部转一次
  - **不设统一的 D 数量级断言**。改为按**公式、状态范围、温度**分别核对：
    | 公式 | 状态 | D / (m²/s) |
    |---|---|---|
    | 附录2 | C=0.05 | ≈ **1.3021×10⁻¹⁶** |
    | 附录2 | C=2.55 | ≈ 4.94×10⁻⁹ |
    | 附录3 | C=2.55, T_K=300 | ≈ **5.3718662×10⁻⁹** |
    | 附录3 | C=0.15, T_K=323 | ≈ **7.9570613×10⁻¹⁰** |
  - 这三档**必须逐点打印核对**，任一不符即为公式录入错误
- **验收**：三档数值全部命中；`check_units()` 输出通过

#### `CODE-02` 附件1 环境数据插值 ·【丙】· D1上午
- **要点**：
  - PCHIP 保形插值 或 分段线性（**禁止高阶多项式**）
  - **待核实**：点数、时间跨度、覆盖范围 —— 读入后回填，不得沿用清单中的数字
- **验收**（**不要求导数无跳变**）：
  - 过所有原始点
  - 值域合理（不超出原始数据的物理范围）
  - 形状合理、外推行为明确
  - 分段线性**允许**导数跳变；原始数据有拐点时导数变号**不必然错误**

#### `CODE-03` 阶段识别 ·【丙】· P1 · D1上午
- **要点**：**非求解必需模块**。若连续环境函数已足够描述边界，本模块**仅用于解释**
- **删除**：「t_pre 必须落在 9000–14000 s」的硬约束
- **验收**：输出斜率/曲率证据链，作为论文叙述材料即可

#### `CODE-04` 附件2 半径拟合 ·【乙】· D1下午 ★
- **要点**：
  - 候选：PCHIP / **单指数趋近模型** $R_\infty+(R_0-R_\infty)e^{-kt}$（**不是双指数**）/ 受约束平滑样条
  - **求导方式不限**：可对拟合函数或分段多项式解析求导，也可用**误差受控**的数值导数。
    唯一禁止：对噪声原始点直接差分
  - 均匀收缩的材料坐标主模型**未必需要显式使用 R′(t)**（见 `CODE-14`）
  - **待核实**：点数、时间跨度、是否平台化
- **验收**（**不做「导数相对差 < 10%」这类不稳健的断言**）：
  - 拟合误差（残差图无系统性偏差）
  - 物理约束：R(t) > 0、单调趋势与观测一致
  - **最终由「干燥时间对拟合选择的敏感性」来量化影响**，而非比较导数值

#### `CODE-05` 半径外推 ·【乙】· P1 · D1下午
- **要点**：**不预判外推风险低**。**仅当 `t_dry` 超出附件2 覆盖时间时**才执行本模块，
  并由结果量化影响；未超出则本模块不启动

#### `CODE-06` 一维圆柱 FVM 离散内核 ·【甲主 / 乙验】· D1上午 ★
- **要点**：
  - **先确定节点型还是单元中心型**。单元中心型通常需由边界条件**重构中心/表面值**，
    不能一律要求「直接取边界节点」
  - **只实现一种可靠版本**（FVM）。第二套 FD 内核降为 **P2**（`CODE-FD`）
  - 界面通量用调和平均；中心用半控制体规避 1/r 奇点
- **验收**：常系数基准通过；内部界面通量**成对抵消**（离散守恒）；中心与边界离散经乙复核

#### `CODE-07` 物性求值模块 ·【甲主 / 乙审】· D1上午
- **要点**：
  - ❌ **取消 `rho_basis` 开关**。明确：**ρ 用于热容/导热项，ρ_d 用于水质量与干物质守恒**，
    两者定义不同，**不允许互换**（见 `CODE-20`）
  - **非线性物性的取值要与离散设计一致**：
    - 节点值**并非一概错误**
    - 「算术平均即二阶」**需要光滑性及一致重构等条件**
    - 界面通量与边界处理应**整体验证**，而非孤立断言
- **验收**：三档 D 关键点数值（见 `CODE-01`）全部命中；温度单位标注清晰

#### `CODE-08` 隐式时间推进器 ·【甲】· D1下午
- **要点**：
  - **修正稳定性上限**：圆柱中心节点中心格式约为 $\Delta r^2/(4\alpha)$。
    Δr = 0.5 mm 时 α = 1.6893×10⁻⁷ m²/s → 约 **0.3701 s**。
    ⚠️ **实际限制必须依据所选定的离散算子确定**，不得直接套用直角坐标的一维上限 0.74 s
  - **输出间隔 ≠ 计算步长**。删除「预热段强制 dt ≤ 1 s」
  - 步长由**误差控制与边界变化**决定；必要时**在输出时刻精确落点**
- **验收**：
  - ❌ 删除「必须观察到负温度或振荡」
  - ✅ 改为：**检查稳定性条件**；不满足即**拒绝该设置**并报错

#### `CODE-09` 非线性迭代器 ·【甲】· D1下午
- **要点**：
  - ❌ 删除固定的「Newton 迭代 ≤ 5 次」
  - ✅ 使用**经过尺度归一化的残差、更新量与最大迭代数**；失败则**缩步重试**
  - ❌ 删除「D 跨 7 个数量级」的预设；**D 变化范围由实际状态确定**
  - ❌ **不默认对 log D 线性化就能改善条件数**
- **验收**：记录每步迭代次数与残差历史；失败回退机制生效

#### `CODE-10` / `CODE-11` 问题1 温度 / 水分场 ·【甲】· D1晚间
- **要点**：问题1 温度方程为**线性**，水分方程与温度**解耦**（D 只依赖 C），可利用该结构分步求解
- **验收**：`CODE-21` 合理性检查通过；**不设「总水量必须下降」的无条件断言**（见 `CODE-21`）

#### `CODE-12` 问题2 双向耦合求解 ·【甲】· D2上午 ★
- **要点**：T 与 C **联立隐式求解**；物性随场局部更新；**不能把变系数移出微分算子**；
  阶段切换处保持状态连续；**从 t=0 就用附录3 公式**

#### `CODE-13` 边界条件模块 ·【甲主 / 乙审】· D2上午
- **要点**：
  - 表面 Robin 换热/传质；表面通量用**表面值**
  - `include_latent_heat` 开关**默认关闭**，作为 **P2 扩展**（见 `CODE-27`）
  - 符号约定写清并断言
- **验收**：纯导热无蒸发时退化为标准 Robin 条件；通量收支（`CODE-20`）通过

#### `CODE-14` 移动边界 / 坐标变换内核 ·【乙】· D1下午 → D2上午 ★★
- **要点**：
  - **先明确坐标、材料速度与守恒关系，再写代码**
  - 关键关系式：
    $$\left.\frac{\partial C}{\partial t}\right|_r=\left.\frac{\partial\widetilde C}{\partial t}\right|_\xi-\frac{\dot R}{R}\xi\frac{\partial\widetilde C}{\partial\xi},\qquad \xi=\frac{r}{R(t)}$$
  - 若物理方程采用**材料导数**且材料速度 $v_r=(\dot R/R)\,r$，材料输运项与坐标项**相消**
  - 若原方程仅用**固定空间位置的偏导数**，变换后**会出现坐标项**
  - ⚠️ **两种写法不能在未交代材料运动的情况下互相替换**
  - **主模型采用材料坐标（均匀收缩）形式**，此时**未必需要显式 R′(t)**
- **验收**：R(t) ≡ R₀ 时**精确退化**为固定域模型；与 `CODE-28` 一致性检验联合通过

#### `CODE-15` 阈值通过时间求解器 ·【甲】· D2上午→下午 ★
- **要点**：
  - 判据用**全域最大含水率** $C_{\max}(t)=\max_r C(r,t)$，**不得用平均值代替**
  - **连续模型的阈值交点通常满足 $C_{\max}=0.15$，而非严格小于**
  - ✅ **报告「阈值通过时间」**
  - ✅ **单独验证其后一个受控小时间增量的全场值**
  - 若输出规定「最早整数秒达标」，则采用该规则并注明
  - ❌ **不能在事件根处强制断言严格小于**
  - 阈值比较**基于未舍入数据**
  - **可行性判断需谨慎**：最终平衡值不低于阈值是**持续稳定达标**的障碍；
    但**若前期环境更干，不能据此排除曾经短暂达标**
- **验收**：报告三类误差**分开**——事件定位误差 / 空间离散误差 / 时间积分误差。
  **定位到 1 s 不代表干燥时间整体准确到 1 s**

#### `CODE-16` 计算网格 → 输出采样 ·【丙】· D1下午
- **要点**：
  - **计算网格不禁止 0.1 cm**：能否采用该步长**由收敛检验决定**
  - **输出插值不自动守恒**：题目需要**点值采样**，应验证**采样误差**与**无过冲**
  - 只有**重映射控制体平均量**时才要求保守映射
  - **三次插值不天然保正**
  - **FVM 未必有边界节点**：取决于 `CODE-06` 的节点型/单元中心型选择
- **验收**：采样误差量化；无过冲；端点无外插

#### `CODE-17` 结果导出器 ·【丙】· D1下午 ★
- **要点**：
  - 严格按 `CODE-31` 解析出的模板写：sheet 名、A 列起点、表头、**4 位小数**
  - **result4 越界处理**：超出当前表面的列按模板约定处理，**绝不能填 0**
  - **result4「药材表面」列**随半径变化，**不能固定取初始半径处**
- **验收**：`validate_output()` 自检——行列数、时间起止、非负、越界单元格处理；
  **四个 Excel 必须能重新读入**

#### `CODE-19` 网格 / 时间误差收敛检验 ·【甲】· **P0** · D2晚间
- **要点**：
  - **空间、时间分开细化**，避免一个误差掩盖另一个
  - 后向欧拉时间**一阶**；Crank–Nicolson 在足够光滑等条件下时间**二阶**
  - **不要求所有误差都二阶收敛**
  - **斜率符号**：横轴为**步长 h** 且 E ∝ h² 时，双对数图斜率应为 **+2**；
    横轴为 **1/h 或网格数 N** 时才是 **−2**
- **验收**：空间与时间收敛阶**分别报告**；`t_dry` 在基准网格下已稳定

#### `CODE-20` 守恒与通量收支检验 ·【乙】· **P0** · D1晚间
- **要点**：
  - 若采用**实际水质量**，用 $M_w=\int\rho_d C\,\mathrm dV$
  - **面积口径必须一致**：整根圆柱侧面积是 $2\pi RL$；
    使用 $2\pi R$ 时**必须明确是「单位长度总量」**
  - **变物性模型不宜直接把 $\rho c_p T$ 的差当作完整内能变化**
  - ✅ **优先检验所解方程的积分收支**
  - 严格热力学检验**需另行定义一致的能量状态**
  - 区分「**离散方程守恒**」与「**严格实际水质量守恒**」
- **验收**：离散通量成对抵消达机器精度；积分收支相对误差量化报告

#### `CODE-21` 物理合理性检查器 ·【乙】· **P0** · D2晚间
- **要点**（逐条断言）：
  - C ≥ 0；T 无非物理过冲；中心对称条件满足
  - ⚠️ **删除「总水量必须下降」这一无条件断言**。在环境可能造成**回湿**时，
    需按边界通量与初值判断；**不要与「含水率不必处处单调」的提醒自相矛盾**
- **验收**：全部断言在四问结果上通过

#### `CODE-23` 模型退化检验 ·【乙】· **P0 升格** · D2晚间 ★
- **要点**（**三组退化条件必须写准**）：
  1. **问题2 → 问题1**：不仅固定热学物性，**还要把扩散系数替换为附录2 的 $D(C)$**，
     并**统一初始条件、环境边界和传递系数**。
     ⚠️ **附录2 的扩散系数本身不是常数**（$7\times10^{-9}e^{-0.89/C}$）
  2. **问题4 固定半径**：若**保留附录4 物性**，退化为 **A/B/C 中的 B 方案**，
     **不等于**使用附录3 的原问题3。**要与问题3 一致还需统一物性等全部条件**
  3. **纯导热解析基准**：需**同时**固定热学物性、**关闭蒸发热效应**、
     **并满足解析解要求的环境边界**
- **验收**：三组退化各自在其**正确的对照目标**上通过

#### `CODE-30` 最小复现与运行记录 ·【丙主 / 甲佐】· **P0** · D3晚间
- **要点**：一键复现四个 `result*.xlsx` 与全部主文图；记录配置标识、版本号、运行时间；
  生成 `RUN_LOG.md`
- **验收**：在干净环境下按说明可复现

#### `CODE-31` 输出模板解析器 ·【丙】· **P0** · D1上午
- **要点**：**读模板文件反向确认规格**，而非凭题面文字猜测。
  核实项：sheet 名、A 列起点、表头列名与步长、末列特殊列名、数值格式
- **验收**：解析结果与题面附录1 描述**逐条比对**；差异记入风险表

#### `CODE-32` 论文表格生成器 ·【丙】· **P0 升格** · D1晚间
- **要点**：从完整结果抽取题设表1—6，直接输出 LaTeX 片段，避免手工誊抄出错
- **验收**：六表与源结果**逐格一致**；四位小数格式正确

---

### 3.2 P1 — 主结果稳定后完成

#### `CODE-18` 解析基准解（Bessel 级数）·【乙】· D2晚间
- **要点**：无限长圆柱 Robin 问题，$T=T_\infty+\sum_n A_nJ_0(\mu_n r/R)e^{-\alpha\mu_n^2 t/R^2}$，
  $\mu J_1(\mu)=\mathrm{Bi}\,J_0(\mu)$
- **前提**：**必须同时**固定热学物性、关闭蒸发热效应、满足解析解要求的环境边界
- **验收**：数值 vs 解析偏差随网格加密按预期阶收敛

#### `CODE-22` 问题1与问题2**差异分析** ·【乙】· P1 · D2晚间
- **要点**（改名）：
  ⚠️ **属于模型差异分析，不能当作两套实现必须相等的正确性验证**
- **验收**：量化温度差与水分差，**并解释成因**（常物性 vs 变物性、D 公式不同）

#### `CODE-24` A→B→C 顺序差分对照 ·【乙】· P1 · D2下午→晚间
- **要点**：
  | 方案 | 尺寸 | 物性 |
  |---|---|---|
  | A | 固定 R₀ | 附录3 |
  | B | 固定 R₀ | 附录4 |
  | C | 随时间收缩 | 附录4 |
  - ✅ **称为「按 A→B→C 路径的顺序差分」**，
    ❌ **不称为唯一、独立的因果贡献**
  - 第四方案「收缩尺寸 + 附录3」可追加，降为 **P2**
- **验收**：三方案 `C_max(t)` 曲线族与顺序差分表

#### `CODE-25` 关键参数与环境敏感性 ·【甲主 / 乙佐】· P1 · D2晚间
- **要点**：
  - ❌ **删除预设「D 最敏感、h 最不敏感」，将其作为待检验猜想**
  - 扰动对象：D 系数、h_m、恒温阶段 T∞ 与 C∞、**半径拟合/外推方式**
  - **扰动幅度需有解释**
  - **不可达标的扰动方案单独标记**，**不能强行填有限时长**
- **验收**：以 `t_dry` 变化幅度为主要指标；给出敏感性表

#### `CODE-28` 坐标变换一致性检验 ·【乙】· P1 · D3上午 ★
- **要点**（**本项已从「模型开关」改为「一致性检验」**）：
  - **同一物理模型在两种坐标下应等价**：
    「固定空间位置偏导数 + 坐标项」 ↔ 「材料导数形式」应给出**相同结果**
  - 两者不等价即说明**推导或离散有误**，而非「模型不同」
  - **只有材料运动假设不同，才属于不同模型**
- **验收**：两种写法结果一致到离散精度；差异超出即排查离散实现

---

### 3.3 P2 — 不占用交付时间

| 编号 | 内容 | 备注 |
|---|---|---|
| `CODE-FD` | 第二套 FD 内核 | 仅在 FVM 通过全部基准与收敛检验后启动 |
| `CODE-26` | 二维轴对称端面模型 | 依据 $2\pi R_0^2/(2\pi R_0L)=R_0/L=0.08$；量化端面影响 |
| `CODE-27` | 潜热扩展对照 | **需先有充分参数依据**；差异大只说明模型敏感 |
| `CODE-33` | 严格干基质量守恒扩展 | 单独定义 ρ_d 及其连续性方程 |
| — | 第四对照方案（收缩 + 附录3） | 交互分析用 |
| — | 完整参数扫描、程序结构展示图 | — |

---

## 4. 插图：主文 10 组图（G1–G10）

> **原 47 张候选图保留为库，不要求全部进入正文。**
> **温度与含水率尽量分子图展示，避免双纵轴造成视觉误读。**
> **统一字体、单位、图例；保存 PDF 矢量版 + PNG 即可**，除非排版流程需要，不必额外制作 EPS。

| 图组 | 内容 | 原编号参考 | 优先级 | 主责 |
|---|---|---|---|---|
| **G1** | 几何、边界和四问路线合图 | FIG-01/02/03 | **P0** | 丙 |
| **G2** | 环境温度与浓度数据及插值 | FIG-04 | **P0** | 丙 |
| **G3** | 半径数据、拟合与误差 | FIG-06/33 | **P0** | 丙（数据：乙） |
| **G4** | 问题1 温度与含水率演化双图 | FIG-09/10（径向图酌情替换） | **P0** | 丙（数据：甲） |
| **G5** | 问题2 前 3 小时径向结果 | FIG-21 | **P0** | 丙（数据：甲） |
| **G6** | 问题3 最大含水率、阈值与终态分布 | FIG-24/27 | **P0** | 丙（数据：甲） |
| **G7** | 问题4 物理坐标含水率及移动边界 | FIG-34 | **P0** | 丙（数据：乙） |
| **G8** | 基准误差与网格/时间收敛 | FIG-14/15/40 | **P0** | 丙（数据：甲乙） |
| **G9** | A/B/C 最大含水率与干燥时间比较 | FIG-36/37 | P1 | 丙（数据：乙） |
| **G10** | 关键参数敏感性 | FIG-28/29 | P1 | 丙（数据：甲） |

### 候选图库（备用，按需取用）

| 编号 | 图题 | 类型 | 主责 |
|---|---|---|---|
| `FIG-05` | 附件1 斜率证据链（阶段识别解释用） | 折线 | 丙 |
| `FIG-07` | 不同拟合方式对比 | 双子图 | 乙 |
| `FIG-08` | 有限体积离散示意（含中心半控制体） | 示意图 | **甲** |
| `FIG-11/12` | 7 个规定时刻的径向分布 | 多线折线 | 丙 |
| `FIG-13` | 中心与表面 T、C 随时间变化 | 双子图 | 丙 |
| `FIG-16` | 无量纲数分析（Bi / Bi_m / Fo） | 条形图 | **甲** |
| `FIG-17` | 变物性随含水率变化四联图 | 2×2 子图 | 丙 |
| `FIG-18` | D(C,T) 等值线（对数色标） | 等值线 | 丙 |
| `FIG-19` | 全流程 T(r,t) 与 C(r,t) 演化 | 双幅云图 | 丙 |
| `FIG-20` | 两阶段边界条件与衔接连续性 | 双子图 | 丙 |
| `FIG-23` | k(r) 空间分布演化 | 多线折线 | 丙 |
| `FIG-25` | 中心/表面/平均含水率对比（论证用 max） | 多线折线 | 丙 |
| `FIG-26` | 干燥前沿推进轨迹 | 等值线 | 丙 |
| `FIG-30` | 每 6 小时径向含水率分布族 | 多线折线 | 丙 |
| `FIG-31` | 移动边界坐标变换示意 | 示意图 | **乙** |
| `FIG-32` | 均匀径向收缩与材料速度场示意 | 示意图 | **乙** |
| `FIG-35` | 实际距离输出示意与表面轨迹 | 折线+阴影 | 丙 |
| `FIG-38/39` | 质量 / 能量守恒残差 | 半对数折线 | **乙** |
| `FIG-41` | 自适应步长与迭代次数历史 | 双子图 | **甲** |
| `FIG-42` | 二维 vs 一维端面效应 | 双子图 | P2 |
| `FIG-43` | 潜热扩展对照 | 双子图 | P2 |
| `FIG-44` | 坐标变换一致性检验 | 双子图 | **乙** |
| `FIG-45` | 模型退化检验汇总 | 多子图 | **乙** |
| `FIG-46` | 数值求解算法框图 | 流程图 | 丙 |
| `FIG-47` | 程序模块结构图 | 框图 | P2 |

---

## 5. 表格清单

| 编号 | 表题 | 负责 | 优先级 |
|---|---|---|---|
| `TAB-01` | 符号说明表 | 丙 | **P0** |
| `TAB-02` | 环境数据关键节点摘录（**核实后**填） | 丙 | P1 |
| `TAB-03` | 三套物性经验公式汇总 | 甲 | **P0** |
| `TAB-04` | **题设表 1—6** | 丙 | **P0** |
| `TAB-05` | 网格与时间误差收敛结果（**空间/时间分列**） | 甲 | **P0** |
| `TAB-06` | 参数灵敏度汇总（对 t_dry 的影响幅度） | 甲 | P1 |
| `TAB-07` | A→B→C 顺序差分结果 | 乙 | P1 |
| `TAB-08` | 模型退化检验结果（三组） | 乙 | **P0** |
| `TAB-09` | 输出文件规格核对表 | 丙 | **P0** |
| `TAB-10` | 复现说明（环境、依赖、命令、耗时） | 丙 | **P0** |

---

## 6. 追踪矩阵

| 论文章节 | 代码 | 图组 | 表格 |
|---|---|---|---|
| 1 问题分析 | — | G1 | — |
| 2 假设与符号 | CODE-01 | G1, FIG-46 | TAB-01, TAB-03 |
| 3 数据预处理 | CODE-02, 03, 31 | G2, G3 | TAB-02, TAB-09 |
| 4 问题1 | CODE-06~11, 16, 17 | G4, FIG-08/11/12 | TAB-04 |
| 5 问题2 | CODE-12, 13 | G5, FIG-17/19/20 | TAB-04 |
| 6 问题3 | CODE-15 | G6, FIG-25/26 | TAB-04, TAB-06 |
| 7 问题4 | CODE-14, 04, 05 | G7, FIG-31/35 | TAB-04, TAB-07 |
| 8 模型检验 | CODE-18~24, 28 | G8, G9, G10 | TAB-05, TAB-08 |
| 附录 | CODE-29, 30 | — | TAB-10 |

---

## 7. 交付前自检清单

- [ ] 附件点数、单位、覆盖范围、模板规格**已实际核实**（非沿用清单数字）
- [ ] 附录公式及 K/°C 转换经过独立复核；**无密度基准开关替换概念**
- [ ] 问题4 材料速度、坐标变换和边界尺度因子**一致**；坐标变换一致性检验通过
- [ ] **相同物性与边界条件下**，三组模型退化检验通过（条件按 §3.1 `CODE-23` 写准）
- [ ] 基准和网格/时间误差已检查；**未把事件定位精度当作总精度**；**空间与时间误差分开报告**
- [ ] **最大含水率**用于达标判定；阈值比较基于**未舍入数据**；报告的是**阈值通过时间**
- [ ] result4 仅在**实际药材内部**输出点值；表面随半径变化；越界按模板约定处理
- [ ] 四个 Excel 能重新打开；题设表 1—6 与源结果一致；四位小数格式正确
- [ ] 主文图均支持具体结论；**不存在重复图、预设结论或未经解释的差异**
- [ ] A/B/C 比较明确为**顺序差分路径**，未宣称唯一因果贡献
- [ ] 敏感性结论为**实证结果**，未预设「D 最敏感」
- [ ] 最终论文与结果使用**同一配置版本**；复现说明与必要数据齐全
- [ ] 三人已分别完成数值、物理和交付核对；**预留了最终修复与提交时间**
