# A 题「药材的烘干问题」代码实现 + 插图位置 全清单

> **定位**：本文是《A题_药材烘干问题_解题框架.md》（核心）与《A题/A题_求解框架.md》（补充）的**施工图**。
> 只列「哪里要写代码」「哪里要插图」，不含实现代码。
>
> **优先级约定**
> - **P0** — 不做就交不出合格答卷
> - **P1** — 决定论文档次（模型检验、对照实验、灵敏度）
> - **P2** — 时间富余时的加分项
>
> **编号约定**：`CODE-xx` 代码模块 / `FIG-xx` 插图 / `TAB-xx` 表格。
> 追踪矩阵见 §6，可直接按 §7 的流水线排期。

---

## 0. 两份框架的分歧与合并原则（先看这一节）

两份框架在以下 **4 处**结论不同。不要二选一，**在代码里做成开关，做对照实验后写进论文**——
这几个对照本身就是一个独立的得分点。

| # | 分歧点 | 核心框架（`A题_药材烘干问题_解题框架.md`） | 补充框架（`A题/A题_求解框架.md`） | 落地方式 |
|---|---|---|---|---|
| **D1** | **蒸发潜热项** λ·j | §2.2 假设 6：**不引入**「无法确定的蒸发潜热项」，基础模型只用题设参数 | §2.3：λj 是 T–C 耦合的关键通道，**必须含** | 边界条件加开关 `include_latent_heat`。**默认 False**（尊重核心框架），另跑一版 True 做对照 → `FIG-43` |
| **D2** | **问题4 变换方程是否含 Ṙ 对流项** | §6.3：均匀径向收缩下取**材料导数**，对流项相消，方程为纯扩散形式（**无** Ṙ 项） | §6.2：含 $+\frac{\dot R}{R}\xi\frac{\partial C}{\partial\xi}$ 对流项 | 移动边界内核加开关 `advection_term`。**默认 False**（材料导数式），另跑 True → `FIG-44`。**这个差异会显著影响问题4 结果，必须验证** |
| **D3** | **中心 r=0 的离散** | §3.3：推荐**有限体积法**，同心环形控制体天然规避 1/r 奇点 | §3.3：顶点中心差分 + L'Hôpital 两点式 | **FVM 为主**（核心优先），FD 版作为独立交叉验证 → `CODE-06` 双实现 |
| **D4** | **干基密度基准** | §4.2 / §6.3：**不能把题设总密度 ρ 当干物质密度用**，严格守恒需单独定义 ρ_d 及其连续性方程 | 未展开，直接用了 ∂C/∂t 形式 | 物性模块提供 `rho_basis ∈ {total, dry}` 两档 → `CODE-07`、`CODE-28` |

> ⚠️ **D1 与 D2 是必须做对照的两个点**，因为它们直接改变问题2/3/4 的核心数值结论。
> 论文里用一张「模型变体 → 干燥时间」的对比表交代，比含糊其辞强得多。

---

## 1. 工程目录结构（建议）

```
26-CUMCM/A题/
├── data/
│   ├── raw/                     附件1.xlsx 附件2.xlsx 附件3/
│   └── processed/               env_interp.pkl, R_fit.pkl, output_grid.json
├── src/
│   ├── config.py                CODE-01
│   ├── data_prep/               CODE-02 ~ CODE-05
│   ├── numerics/                CODE-06 ~ CODE-09
│   ├── models/                  CODE-10 ~ CODE-15
│   ├── io/                      CODE-16 ~ CODE-17
│   ├── validation/              CODE-18 ~ CODE-23
│   └── experiments/             CODE-24 ~ CODE-28
├── figs/                        FIG-*（同时导出 png + pdf/eps 供 LaTeX）
├── results/                     result1~4.xlsx
├── scripts/                     CODE-29 ~ CODE-30, run_all.py
└── report/                      main.tex, figures/, tables/
```

**一个入口跑通全流程**：`scripts/run_all.py --stage {prep,p1,p2,p3,p4,valid,exp,figs,all}`

---

## 2. 代码清单

### 2.1 阶段 A — 数据与配置（第 3 章 数据预处理）

#### `CODE-01` 全局配置中心 · P0
- **文件**：`src/config.py`
- **内容**：几何（R₀=0.02 m、L=0.25 m）、初值（T₀=28 °C、C₀=2.55）、
  三套物性公式（附录2/3/4）、h=25、h_m=8e-7、输出规格（时间轴、距离轴、sheet 名、小数位）
- **要点**：**单位统一写死在这里**；提供一个 `check_units()` 自检函数，
  打印各物性在 C∈[0.05, 2.55] 上的量级范围
- **验收**：`check_units()` 输出的 D 量级落在 1e-11 ~ 1e-8 m²/s（跨 3 个数量级即为正常）

#### `CODE-02` 附件1 环境数据插值 · P0
- **文件**：`src/data_prep/interp_env.py`
- **输入**：附件1（241 点，60 s 间隔）
- **输出**：`T_inf(t)`, `C_inf(t)` 连续函数（PCHIP 保形插值，或分段线性）
- **要点**：**禁止高阶多项式**（会产生不合理振荡）；同时实现两种插值以便对比
- **验收**：插值曲线过所有原始点；导函数无符号跳变；外推区不产生负值
- **对应图**：`FIG-04`、`FIG-05`

#### `CODE-03` 阶段分界点识别 · P0
- **文件**：`src/data_prep/phase_split.py`
- **输入**：附件1 插值曲线
- **输出**：预热→恒温的分界时刻 `t_pre` 及其判据（斜率阈值 / 曲率拐点 / 落入平台带）
- **要点**：输出**证据链**（局部斜率序列），不要只给一个数
- **验收**：`t_pre` 落在 9000–14000 s 区间且给出理由
- **对应图**：`FIG-05`

#### `CODE-04` 附件2 半径拟合 · P0（问题4 的成败关键）
- **文件**：`src/data_prep/fit_radius.py`
- **输入**：附件2（145 点，1800 s 间隔，R: 2.000 → 1.198 cm）
- **输出**：`R(t)` 与**解析求导**的 `R'(t)`；拟合参数与残差
- **要点**：
  - 三选一或全做：PCHIP / 双指数 `R∞+(R₀−R∞)e^{−kt}` / 受约束平滑样条
  - **硬约束**：R(t) > 0 恒成立；R'(t) 光滑有界
  - **绝对禁止**对原始数据直接做数值差分当作 R'(t)
- **验收**：R'(t) 无振荡；残差图无系统性偏差；不同方法给出的 R'(t) 差异 < 10%
- **对应图**：`FIG-06`、`FIG-07`、`FIG-33`

#### `CODE-05` 半径外推策略 · P1
- **文件**：`src/data_prep/extrapolate_radius.py`
- **要点**：附件2 覆盖到 259200 s（72 h）。若 `t_dry` 超出该范围需外推；
  附件2 在 ~58 h 后已平台化，**外推风险低**，但仍需给出 2–3 种外推并比较
- **验收**：不同外推方式对 `t_dry` 的影响 < 若干小时（量化写入论文）
- **对应图**：`FIG-33`

#### `CODE-06'` 输出模板解析器 · P0
- **文件**：`src/data_prep/parse_template.py`
- **输入**：`附件3/result1~4.xlsx`
- **输出**：`output_grid.json`（每个文件的 sheet 名、A 列起点、表头列名与步长、末列特殊列名）
- **要点**：读模板**反向确认**规格，而不是凭题面文字猜测：
  result1/2 时间列从 **1 s** 起、result3/4 从 **60 s** 起；result4 末列为字符串「药材表面」
- **验收**：解析结果与题面附录1 描述逐条一致
- **对应表**：`TAB-09`

---

### 2.2 阶段 B — 数值内核（第 4–7 章共用）

#### `CODE-06` 一维圆柱有限体积离散内核 · P0 ★
- **文件**：`src/numerics/fvm_cylinder.py`
- **输入**：网格数 N、当前半径 R、边界通量函数
- **输出**：稀疏扩散算子 L、边界修正项、控制体面积/体积数组
- **要点**：
  - 同心环形控制体，界面取**调和平均**（变 D 时保证通量正确）
  - **中心用半控制体**（半径 Δr/2 的实心圆柱），天然规避 1/r 奇点 → 对应 `D3`
  - 提供**第二套**顶点中心差分 + L'Hôpital 两点式实现，用于交叉验证
- **验收**：常系数下与解析解误差二阶收敛；内部界面通量**成对抵消**（离散守恒）

#### `CODE-07` 物性求值模块 · P0
- **文件**：`src/numerics/properties.py`
- **内容**：三套物性（附录2 常数 / 附录3 函数 / 附录4 函数）统一接口
- **要点**：
  - 🔴 **`T_K = T + 273.15` 必须在此模块内部完成**，对外接口统一用 °C 或统一用 K，不混用
  - 物性按**控制体平均**取值（算术平均即可，二阶精度），**不能取节点值**
  - `rho_basis ∈ {total, dry}` 开关 → 对应 `D4`
  - 边界处 D 用**表面含水率**求值：$D(C_s)$，不能取邻点值
- **验收**：`D(2.55, 300K)=5.35e-9`、`D(0.15, 323K)=7.95e-10`（附录3）等关键点数值打印核对

#### `CODE-08` 隐式时间推进器 · P0
- **文件**：`src/numerics/time_stepper.py`
- **内容**：Backward Euler / Crank–Nicolson 通用框架，支持自适应步长
- **要点**：
  - 显式格式稳定性上限 Δt ≤ Δr²/(2α)：Δr=0.5 mm 时仅 **0.74 s** ——
    **绝不能直接用 Δt=1 s 的显式格式**，必须在代码里写断言防呆
  - 提供局部误差估计驱动的变步长（预热段强制 dt ≤ 1 s）
- **验收**：显式格式在 Δt=1 s 下**必须**触发发散告警（负温度或振荡）

#### `CODE-09` 非线性迭代器 · P0
- **文件**：`src/numerics/nonlinear_solver.py`
- **内容**：Picard 迭代 / Newton 迭代，含收敛判据与迭代上限
- **要点**：D(C) 跨 7 个数量级 → 建议对 log D 做线性化以改善条件数
- **验收**：典型时间步 Newton 迭代 ≤ 5 次收敛；记录每步迭代次数供 `FIG-41`

---

### 2.3 阶段 C — 四个问题的模型（第 4–7 章）

#### `CODE-10` 问题1 温度场求解器 · P0
- **文件**：`src/models/p1_temperature.py`
- **要点**：常物性线性方程；**利用单向耦合**，先由 `CODE-11` 得到水分场与表面通量 j(t)，
  再单独解温度（`D1` 关闭时 j(t) 不进边界）
- **验收**：`D1=False` 时与 `CODE-18` 解析解逐点比对

#### `CODE-11` 问题1 水分场求解器 · P0
- **文件**：`src/models/p1_moisture.py`
- **要点**：D(C)=7e-9·exp(−0.89/C)，**与温度场完全解耦**（问题1 的关键洞察）
- **验收**：C 场非负；总水量单调下降；质量守恒残差 < 1e-6

#### `CODE-12` 问题2 双向耦合求解器 · P0 ★
- **文件**：`src/models/p2_coupled.py`
- **要点**：
  - T 与 C **联立隐式求解**（D 依赖 T，不可再单向耦合）
  - 物性随场局部更新，**不能把变系数移出微分算子**
  - 阶段衔接处保持 $T(t_s^-)=T(t_s^+)$、$C(t_s^-)=C(t_s^+)$ 连续
  - **从 t=0 就用附录3 公式**，不允许先跑问题1 再切换参数
- **验收**：与 `CODE-23` 的退化检验通过；与 `CODE-10/11` 在 t=1800 s 的交叉验证完成
- **对应图**：`FIG-19`~`FIG-23`

#### `CODE-13` 边界条件模块（含潜热开关） · P0
- **文件**：`src/models/boundary.py`
- **要点**：
  - 表面 Robin 换热/传质；表面通量用**表面值**而非邻点值
  - `include_latent_heat` 开关 → 对应 `D1`（默认 False）
  - 蒸发通量 $j = h_m\rho_d(C_s-C_\infty)$ 的符号约定要写清并做断言
- **验收**：纯导热无蒸发时退化为标准 Robin 条件；能量守恒检验通过

#### `CODE-14` 移动边界 / 坐标变换内核 · P0 ★★
- **文件**：`src/models/p4_moving_boundary.py`
- **要点**：
  - ξ = r/R(t) 归一化坐标，固定计算网格
  - 变换后算子含 **1/R(t)²** 因子与 **1/R(t)** 的边界系数（见核心框架 §6.4）
  - `advection_term` 开关 → 对应 `D2`（默认 False = 材料导数式，**无** Ṙ 对流项）
  - 若开启对流项，需严格处理其离散与守恒修正
- **验收**：R(t)≡R₀ 时**必须精确退化**为问题3 的模型（这是最强的一致性检验）
- **对应图**：`FIG-31`、`FIG-34`、`FIG-44`

#### `CODE-15` 干燥达标时间求解器 · P0
- **文件**：`src/models/dry_time.py`
- **要点**：
  - 判据：$t_{dry}=\inf\{t: \max_r C(r,t) < 0.15\}$，**必须用全域 max**
  - **必须用未舍入数值**判断，不能拿四位小数后的结果比阈值
  - 两种实现：事件检测法（推荐）+ 二分法（交叉验证），结果需一致到 1 s
  - 附**可行性前置检查**：若最终环境对应的平衡含水率 ≥ 0.15，直接报「无法在有限时间达标」
- **验收**：两种方法给出的 `t_dry` 差 < 1 s；终止时刻全场 max C < 0.15 且**接近但不超**
- **对应图**：`FIG-24`、`FIG-26`

---

### 2.4 阶段 D — 输出与导出（全章）

#### `CODE-16` 计算网格 → 输出网格插值器 · P0
- **文件**：`src/io/regrid.py`
- **要点**：
  - 计算网格与输出网格**必须分开设置**（不能用 0.1 cm 当计算步长）
  - 用二阶（三次）插值；中心点与表面点**不插值**，直接取边界节点值
  - 问题4 需按 ξ = r/R(t) 反算：给定物理距离 r，先算 ξ，再在 ξ 网格上取值
- **验收**：插值前后总量守恒；端点无外插

#### `CODE-17` 结果导出器（严格按模板） · P0 ★
- **文件**：`src/io/export_results.py`
- **要点**：
  - 严格按 `CODE-06'` 解析出的模板写：sheet 名、A 列起点、表头、**4 位小数**
  - **result4 越界处理**：t 较大时 R(t) < 2.0 cm，超出表面的列按模板约定**留空**，
    **绝不能填 0**（填 0 会被判为"水分浓度为零"，语义完全错误）
  - **result4「药材表面」列**：每行填该时刻 R(t) 处的值，**不能固定取 r=2 cm**
  - result3/4 末行需追加**干燥终止时刻**（不落在整分钟上）
- **验收**：写一个 `validate_output()` 自检：行列数、时间起止、单调性、非负、越界单元格为空
- **对应表**：`TAB-04`、`TAB-09`

#### `CODE-17'` 论文表格生成器 · P1
- **文件**：`src/io/make_tables.py`
- **要点**：从完整结果中抽取表1/2（7×5）、表3/4（6×5）、表5/6（每6 h × 每0.5 cm）
  并直接输出 LaTeX 片段，避免手工誊抄出错
- **对应表**：`TAB-04`

---

### 2.5 阶段 E — 验证（第 8 章 模型检验，P1 全部）

#### `CODE-18` 解析基准解（Bessel 级数） · P1 ★
- **文件**：`src/validation/analytic_bessel.py`
- **要点**：
  - 无限长圆柱 Robin 问题：$T=T_\infty+\sum_n A_n J_0(\mu_n r/R)e^{-\alpha\mu_n^2 t/R^2}$
  - 特征值由 $\mu J_1(\mu)=\mathrm{Bi}\,J_0(\mu)$ 求正根（至少前 30 个）
  - 系数 $A_n$ 用 Bessel 正交性由初值确定
  - **仅在关闭蒸发项（`D1=False`）时适用**
- **验收**：数值 vs 解析最大偏差 < 0.01 °C（细网格）
- **对应图**：`FIG-14`

#### `CODE-19` 网格 / 步长收敛性实验 · P1
- **文件**：`src/validation/convergence.py`
- **要点**：Δr = 1.0 / 0.5 / 0.25 / 0.125 mm × 三档时间容差；
  比较中心温度、中心含水率、全域 max C、`t_dry`
- **验收**：双对数图斜率 ≈ −2；`t_dry` 在基准网格下已稳定到 < 0.5 h
- **对应图**：`FIG-15`、`FIG-40`、`TAB-05`

#### `CODE-20` 守恒检验 · P1
- **文件**：`src/validation/conservation.py`
- **要点**：
  - **离散守恒**：内部界面通量成对抵消（机器精度级）
  - **物理质量守恒**：$M_w(0)-M_w(t)$ vs $\int_0^t 2\pi R j\,d\tau$
  - **能量守恒**：对流供热 − 蒸发耗热（若 `D1=True`）= 内能增量
  - 明确区分「离散方程守恒」与「严格水质量守恒」，并说明 ρ_d 基准处理（对应 `D4`）
- **验收**：离散守恒达机器精度；物理守恒相对误差 < 1e-4
- **对应图**：`FIG-38`、`FIG-39`

#### `CODE-21` 物理合理性检查器 · P1
- **文件**：`src/validation/sanity_checks.py`
- **要点**（逐条断言，失败即报警）：
  - C ≥ 0；T 无非物理过冲；中心对称条件满足
  - 内部变化与表面通量方向一致
  - ⚠️ **含水率是否单调下降取决于环境条件，不要强制设定所有位置始终下降**
- **验收**：所有断言在四个问题的结果上通过

#### `CODE-22` 问题1 ↔ 问题2 交叉验证 · P1
- **文件**：`src/validation/cross_check.py`
- **要点**：用问题2 模型算到 t=1800 s，与问题1 结果对比；
  **量化温度差与水分差并解释成因**（常物性 vs 变物性、D 公式不同）
- **对应图**：`FIG-22`

#### `CODE-23` 模型退化检验 · P1
- **文件**：`src/validation/degeneration.py`
- **三组退化实验**：
  1. 问题2 模型把物性固定为附录2 常数 → 应退化为问题1
  2. 问题4 模型把 R(t) 固定为 R₀ → 应退化为问题3
  3. 关闭全部变化项 → 应退化为纯导热解析解
- **验收**：退化后与目标模型结果一致到数值精度
- **对应图**：`FIG-44`

---

### 2.6 阶段 F — 对照实验与灵敏度（第 6、8 章，P1）

#### `CODE-24` 三方案对照实验 · P1 ★
- **文件**：`src/experiments/ablation.py`
- **要点**（来自核心框架 §6.6）：

  | 方案 | 尺寸 | 物性公式 | 用途 |
  |---|---|---|---|
  | **A** | 固定 R₀ | 附录3 | 基线 = 问题3 |
  | **B** | 固定 R₀ | 附录4 | 隔离**物性变化**的影响 |
  | **C** | 随时间收缩 | 附录4 | 加入**收缩**的影响 = 问题4 |

  **A→B 差值 = 物性贡献；B→C 差值 = 收缩贡献。**
  **不能把问题3 与问题4 的全部时间差归因于收缩** —— 这是核心框架明确点出的易错点
- **验收**：三方案的 `C_max(t)` 曲线族；贡献分解柱状图
- **对应图**：`FIG-36`、`FIG-37`、`TAB-07`

#### `CODE-25` 参数灵敏度扫描 · P1 ★
- **文件**：`src/experiments/sensitivity.py`
- **扫描对象**（核心框架 §7.4 优先级）：
  1. 扩散系数 D 的系数（±20%）
  2. 表面传质系数 h_m（±20%）
  3. 恒温阶段 T∞ 与 C∞（在平台带内扰动）
  4. 对流换热系数 h（±20%）
  5. 半径拟合方式与外推方式
  6. 阶段分界点 `t_pre`（9600 s vs 14400 s）
  7. 忽略端面的影响
- **输出**：以 **`t_dry` 变化幅度**为主要指标 → tornado 图数据
- **预期定性结论**：`t_dry` 对 D 最敏感、对 h 最不敏感（传热快于传质）
- **对应图**：`FIG-28`、`FIG-29`、`TAB-06`

#### `CODE-26` 二维轴对称（r,z）模型 · P2
- **文件**：`src/experiments/axisym_2d.py`
- **要点**：25 cm 有限长柱体，量化端面效应对中心截面的影响
- **依据**：$2\pi R_0^2/(2\pi R_0 L)=R_0/L=0.08$，端面占比不小，值得验证
- **验收**：与一维结果对比，若差异 < 3% 则一维简化成立，结论写进论文
- **对应图**：`FIG-42`

#### `CODE-27` 潜热项开关对照 · P1（对应 `D1`）
- **文件**：`src/experiments/latent_heat_toggle.py`
- **要点**：`include_latent_heat ∈ {False, True}` 跑问题2/3，
  比较温度场与 `t_dry`
- **若影响很小** → 成为一条有力的简化依据；**若影响大** → 说明该项必须保留
- **对应图**：`FIG-43`

#### `CODE-28` 收缩模型对流项开关对照 · P1 ★（对应 `D2`）
- **文件**：`src/experiments/advection_toggle.py`
- **要点**：`advection_term ∈ {False（材料导数式）, True（固定坐标系式）}` 跑问题4，
  比较含水率场与 `t_dry`
- **这是两份框架最大的一处分歧，必须实算验证并给结论**
- **对应图**：`FIG-44`

#### `CODE-28'` 严格干基质量守恒模型 · P2（对应 `D4`）
- **文件**：`src/experiments/dry_basis.py`
- **要点**：单独定义单位体积干物质质量 ρ_d 及其连续性方程，与有效扩散模型对照
- **对应图**：`FIG-38`

---

### 2.7 阶段 G — 绘图与流程

#### `CODE-29` 绘图脚本集 · P0
- **文件**：`scripts/make_figures.py`，每个 `FIG-xx` 一个函数
- **要点**：
  - 统一字体（中文需设置 SimHei 等，注意 **负号显示** 的经典坑）
  - 每张图同时导出 **PNG（300 dpi）** 与 **PDF/EPS** 供 LaTeX 用
  - 图号与本文编号一一对应，便于回溯
- **验收**：`make_figures.py --list` 能列出全部图及其生成状态

#### `CODE-30` 汇总与复现打包 · P1
- **文件**：`scripts/run_all.py` + `requirements.txt`
- **要点**：一键复现全部 `result*.xlsx` 与全部图；记录随机种子（若有）、版本号、
  运行时间；生成 `RUN_LOG.md`
- **对应表**：`TAB-10`

---

## 3. 插图清单（按论文章节顺序）

> 标注 ★ 的是**最能提分**的图，时间紧张时优先保证这些。

### 第 1 章 问题分析

| 编号 | 图题 | 类型 | 数据来源 | 优先级 |
|---|---|---|---|---|
| `FIG-01` | 四问递进关系与技术路线图 | 流程图 / 思维导图 | 手工绘制 | P1 |
| `FIG-02` | 药材圆柱几何与一维轴对称简化示意（含端面面积比 R₀/L=0.08 标注） | 示意图 | 手工绘制 | P0 |

### 第 2 章 模型假设与符号说明

| 编号 | 图题 | 类型 | 数据来源 | 优先级 |
|---|---|---|---|---|
| `FIG-03` | 物理模型与边界条件示意图（控制体、中心对称、表面换热+传质箭头） | 示意图 | 手工绘制 | P0 |

### 第 3 章 数据预处理

| 编号 | 图题 | 类型 | 数据来源 | 优先级 |
|---|---|---|---|---|
| `FIG-04` | 附件1 环境温度与水分浓度：原始散点 + 插值曲线 + 阶段分界标注 | 双子图折线 | `CODE-02` | P0 |
| `FIG-05` | 附件1 局部斜率序列（论证 `t_pre` 选取的证据） | 折线 | `CODE-03` | P1 |
| `FIG-06` | 附件2 半径 R(t)：原始点 + 拟合曲线 + **残差子图** | 双子图 | `CODE-04` | P0 |
| `FIG-07` | 不同拟合方式对比及对 **R′(t)** 的影响 | 双子图折线 | `CODE-04/05` | P1 |

### 第 4 章 问题1模型与结果

| 编号 | 图题 | 类型 | 数据来源 | 优先级 |
|---|---|---|---|---|
| `FIG-08` | 有限体积离散示意（环形控制体 + 中心半控制体） | 示意图 | 手工绘制 | P1 |
| `FIG-09` | 温度场 T(r,t) 演化 | **t–r 等值线云图**（首选）或三维曲面 | `CODE-10` | P0 |
| `FIG-10` | 含水率场 C(r,t) 演化 | t–r 等值线云图 | `CODE-11` | P0 |
| `FIG-11` | 7 个规定时刻的温度径向分布（100~1800 s） | 多线折线 | `CODE-10` | P0 |
| `FIG-12` | 7 个规定时刻的含水率径向分布 | 多线折线 | `CODE-11` | P0 |
| `FIG-13` | 中心与表面 T、C 随时间变化（双 y 轴） | 折线 | `CODE-10/11` | P1 |
| **`FIG-14`** ★ | **数值解 vs Bessel 解析解对比 + 误差曲线** | 双子图 | `CODE-18` | **P1** |
| **`FIG-15`** ★ | **误差 vs 网格步长 双对数收敛图（斜率应为 −2）** | 双对数折线 | `CODE-19` | **P1** |
| `FIG-16` | Bi / Bi_m / Fo 无量纲数分析图（论证"必须用 PDE 而非集总模型"） | 条形图 / 数轴图 | `CODE-01` | P2 |

### 第 5 章 问题2模型与结果

| 编号 | 图题 | 类型 | 数据来源 | 优先级 |
|---|---|---|---|---|
| `FIG-17` | 变物性随含水率的变化：ρ(C)、c_p(C)、k(C)、D(C) 四联图 | 2×2 子图 | `CODE-07` | P0 |
| `FIG-18` | 扩散系数 D(C,T) 等值线图（**对数色标**） | 等值线 | `CODE-07` | P1 |
| `FIG-19` | 全流程 T(r,t) 与 C(r,t) 演化（0–3 天） | 双幅 t–r 云图 | `CODE-12` | P0 |
| `FIG-20` | 两阶段边界条件对比 + **衔接处连续性放大图** | 双子图 | `CODE-02/03` | P1 |
| `FIG-21` | 前 3 小时各 0.5 h 的径向温度/含水率分布 | 双子图多线 | `CODE-12` | P0 |
| `FIG-22` | 问题1 vs 问题2 交叉验证（t=1800 s 时刻场对比 + 差异） | 双子图 | `CODE-22` | P1 |
| `FIG-23` | 热传导系数 k(r) 的空间分布演化（变物性的直接可视化证据） | 多线折线 | `CODE-12` | P2 |

### 第 6 章 问题3达标时间

| 编号 | 图题 | 类型 | 数据来源 | 优先级 |
|---|---|---|---|---|
| **`FIG-24`** ★ | **全域最大含水率 C_max(t) 衰减曲线 + 阈值 0.15 线 + 达标点标注** | 单折线（半对数可选） | `CODE-15` | **P0** |
| `FIG-25` | 中心 / 表面 / 平均含水率三曲线对比（论证**必须用 max 而非平均**） | 多线折线 | `CODE-15` | P1 |
| `FIG-26` | 干燥前沿推进：C=0.15 等值线在 r–t 平面的轨迹 | 等值线 / 折线 | `CODE-12/15` | P1 |
| `FIG-27` | 达标时刻的径向含水率分布（验证**全域**达标） | 单折线 | `CODE-15` | P0 |
| **`FIG-28`** ★ | **参数灵敏度 tornado 图（各因素对 t_dry 的影响）** | 水平条形图 | `CODE-25` | **P1** |
| **`FIG-29`** ★ | **恒温阶段边界假设（t_pre / T∞ / C∞）对 t_dry 的影响曲线族** | 多线折线 | `CODE-25` | **P1** |
| `FIG-30` | 每 6 小时的径向含水率分布族（对应表5） | 多线折线 | `CODE-15` | P0 |

### 第 7 章 问题4收缩模型

| 编号 | 图题 | 类型 | 数据来源 | 优先级 |
|---|---|---|---|---|
| `FIG-31` | 移动边界坐标变换示意（物理域 [0,R(t)] → 计算域 ξ∈[0,1]） | 示意图 | 手工绘制 | P1 |
| `FIG-32` | 均匀径向收缩与材料速度场 $v_r=\frac{R'}{R}r$ 示意 | 示意图 | 手工绘制 | P2 |
| `FIG-33` | R(t)、R′(t)、收缩率曲线 | 三子图 | `CODE-04/05` | P0 |
| `FIG-34` | 收缩域上的含水率演化（坐标轴标**物理 r**，叠加 R(t) 边界轨迹） | t–r 云图 + 边界线 | `CODE-14` | P0 |
| `FIG-35` | 实际距离输出示意：不同时刻可用列范围 + 「药材表面」轨迹 | 折线 + 阴影带 | `CODE-16/17` | P1 |
| **`FIG-36`** ★ | **A/B/C 三方案 C_max(t) 曲线对比（分离物性与收缩的贡献）** | 多线折线 | `CODE-24` | **P1** |
| **`FIG-37`** ★ | **问题3 vs 问题4 干燥时间对比 + 贡献分解（物性 vs 收缩）** | 柱状图（堆叠） | `CODE-24` | **P1** |

### 第 8 章 模型检验与误差分析

| 编号 | 图题 | 类型 | 数据来源 | 优先级 |
|---|---|---|---|---|
| `FIG-38` | 质量守恒相对误差随时间变化 | 半对数折线 | `CODE-20` | P1 |
| `FIG-39` | 能量守恒残差随时间变化 | 半对数折线 | `CODE-20` | P1 |
| `FIG-40` | 网格/步长收敛性：关键量 vs 网格尺度 | 双对数折线 | `CODE-19` | P1 |
| `FIG-41` | 自适应时间步长历史 / Newton 迭代次数分布 | 双子图 | `CODE-08/09` | P2 |
| `FIG-42` | 二维轴对称 vs 一维：端面效应量化对比 | 双子图 | `CODE-26` | P2 |
| `FIG-43` | 潜热项开/关对照（温度场与 t_dry 差异）· 对应分歧 `D1` | 双子图 | `CODE-27` | P1 |
| **`FIG-44`** ★ | **收缩模型对流项开/关对照 · 对应分歧 `D2`** | 双子图 | `CODE-28` | **P1** |
| `FIG-45` | 模型退化检验汇总（三组退化实验的一致性） | 多子图 | `CODE-23` | P2 |

### 求解流程类（可放第 2 或第 4 章）

| 编号 | 图题 | 类型 | 优先级 |
|---|---|---|---|
| `FIG-46` | 数值求解算法框图（时间步内的物性更新→温度→含水率→收敛判定） | 流程图 | P1 |
| `FIG-47` | 程序模块结构与数据流图 | 框图 | P2 |

---

## 4. 表格清单

| 编号 | 表题 | 来源 | 优先级 |
|---|---|---|---|
| `TAB-01` | 符号说明表 | 手工 | P0 |
| `TAB-02` | 附件1 环境数据关键节点摘录 | `CODE-02` | P1 |
| `TAB-03` | 三套物性经验公式汇总（附录2/3/4 对照） | `CODE-01` | P0 |
| `TAB-04` | **题目要求的表1~表6** | `CODE-17'` | **P0** |
| `TAB-05` | 网格与时间步长收敛性结果 | `CODE-19` | P1 |
| `TAB-06` | 参数灵敏度汇总（对 t_dry 的影响幅度） | `CODE-25` | P1 |
| `TAB-07` | A/B/C 三方案对照（物性贡献 vs 收缩贡献分解） | `CODE-24` | P1 |
| `TAB-08` | 模型变体对照（`D1` 潜热、`D2` 对流项、`D4` 干基基准） | `CODE-27/28/28'` | P1 |
| `TAB-09` | 输出文件规格核对表（sheet/时间轴/距离轴/末列/越界处理） | `CODE-06'/17` | P0 |
| `TAB-10` | 复现说明（环境、依赖、运行命令、耗时） | `CODE-30` | P1 |

---

## 5. 最容易漏掉的 8 个"隐藏工作项"

这些往往不在直觉里，但会直接决定成败：

1. **`CODE-06'` 反向解析模板** —— 不要凭题面文字猜 result 文件的规格，要读模板文件确认
2. **`CODE-17` result4 越界留空** —— 填 0 是语义错误（0 表示"完全干燥"）
3. **`CODE-17` result4 末列「药材表面」随时间移动** —— 不能固定取 r=2 cm
4. **`CODE-15` 用未舍入值判阈值** —— 拿四位小数后的结果比 0.15 可能得出错误的达标时刻
5. **`CODE-15` 达标可行性前置检查** —— 先确认平衡含水率 < 0.15，再谈"2–3 天"
6. **`CODE-04` R′(t) 必须解析求导** —— 数值差分的噪声会被 Ṙ/R 项放大成振荡
7. **`CODE-07` T_K = T + 273.15** —— 只在物性模块内部转一次，全局禁止混用
8. **`CODE-08` 显式格式的稳定性断言** —— Δr=0.5 mm 时 Δt 上限仅 0.74 s，写个断言防呆

---

## 6. 追踪矩阵（章节 ↔ 代码 ↔ 图 ↔ 表）

| 论文章节 | 代码 | 插图 | 表格 |
|---|---|---|---|
| 1 问题分析 | — | FIG-01, FIG-02 | — |
| 2 假设与符号 | CODE-01 | FIG-03, FIG-46 | TAB-01, TAB-03 |
| 3 数据预处理 | CODE-02~05, 06' | FIG-04~07 | TAB-02, TAB-09 |
| 4 问题1 | CODE-06~11, 16, 17 | FIG-08~16 | TAB-04 |
| 5 问题2 | CODE-12, 13 | FIG-17~23 | TAB-04 |
| 6 问题3 | CODE-15 | FIG-24~30 | TAB-04, TAB-06 |
| 7 问题4 | CODE-14, 05 | FIG-31~37 | TAB-04, TAB-07 |
| 8 模型检验 | CODE-18~28' | FIG-38~45 | TAB-05, TAB-06, TAB-08 |
| 附录 | CODE-29, 30 | FIG-47 | TAB-10 |

---

## 7. 建议排期（3 天）

### Day 1 — 地基（做完必须能出一版 result1）
- 上午：`CODE-01/02/03/06'`（配置、环境插值、阶段分界、模板解析）→ `FIG-02/03/04`
- 下午：`CODE-06/07/08/09`（FVM 内核、物性、时间推进、非线性迭代）
- 晚上：`CODE-10/11/16/17`（问题1 双场 + 输出）→ **result1.xlsx 完成** → `FIG-09~13`
- 收尾：`CODE-18/19/20/21`（解析解 + 收敛 + 守恒）→ **`FIG-14/15` 是当天的最高价值产出**

### Day 2 — 主攻问题2、3
- 上午：`CODE-12/13`（双向耦合 + 边界）→ **result2.xlsx** → `FIG-17~21`
- 下午：`CODE-15`（达标时间，事件检测+二分）→ **result3.xlsx** → `FIG-24~27/30`
- 晚上：`CODE-22/23`（交叉验证、退化检验）+ `CODE-25` 灵敏度 → **`FIG-28/29`**
- 阶段小结：**用 `FIG-24` 核对 t_dry 是否落在 2–3 天**，若偏离立即按核心框架 §5.3 排查

### Day 3 — 问题4 + 收尾
- 上午：`CODE-04/05`（半径拟合）+ `CODE-14`（移动边界）→ **result4.xlsx** → `FIG-06/07/31/33~35`
- 下午：`CODE-24`（A/B/C 对照）→ **`FIG-36/37`**；`CODE-27/28`（分歧点对照）→ **`FIG-43/44`**
- 晚上：`CODE-29/30`（全部图 + 复现包）+ 论文排版 + `TAB-04` 表格誊抄核对

### 时间富余（P2）
`CODE-26`（二维端面效应）、`CODE-28'`（严格干基守恒）、`FIG-16/23/32/41/42/45/47`

---

## 8. 交付前自检清单（打印出来逐项打勾）

- [ ] `result1~4.xlsx` 的 sheet 名、A 列时间起点（1 s / 60 s）、表头与模板**完全一致**
- [ ] 全部数值保留 **4 位小数**；result4 越界单元格为**空**而非 0
- [ ] result3/4 末行包含**干燥终止时刻**（可为非整分钟）
- [ ] 四个 result 均由 `run_all.py` 一键复现
- [ ] 图全部导出 PNG(300dpi) + PDF/EPS，中文与负号显示正常
- [ ] `FIG-14/15`（解析解与收敛性）已确认误差二阶收敛
- [ ] `FIG-24` 的 `t_dry` 已与"2–3 天"做自洽性论证（或给出偏离原因）
- [ ] `FIG-36/37` 已把**物性贡献**与**收缩贡献**分开，未混为一谈
- [ ] `FIG-43/44` 已完成两份框架分歧点（`D1` 潜热、`D2` 对流项）的对照实验
- [ ] `TAB-01` 符号表与正文符号**逐一对应**，无遗漏、无多余
